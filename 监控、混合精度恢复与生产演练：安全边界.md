# 监控、混合精度恢复与生产演练：安全边界

1. Prometheus/Grafana 的角色是观测、关联和告警；告警不得直接执行 RDMA/rail 故障注入、变更网络、重启共享集群、推进数据游标或发布模型。任何自动动作最多可冻结新的 UpdatePlan、标记作业 SUSPECT、保留工件并请求人工响应。
2. 只从账本标记 COMMITTED 且文件哈希、模型、EMA、optimizer、scheduler、RNG、AMP/FP8 状态完整的 checkpoint 恢复。任何 in-flight update 的 AdamW 状态、GradScaler、FP8 recipe/AMAX 历史和数据 reservation 都不是恢复事实来源。
3. BF16 通常不依赖 PyTorch GradScaler；FP16 依赖 scaler；FP8 的缩放/AMAX 状态由所选 FP8 实现管理，不能假定通用 PyTorch GradScaler 即可覆盖。恢复合同必须按实际 precision backend 和版本明确状态键与数值比较规则。
4. 生产演练只在批准的隔离环境和窗口中进行。上线前检查或告警恢复成功不等于自动生产发布；仍须经过影子、灰度、HITL 和现有安全门控。
5. 指标标签禁止包含 work_id、轨迹、人员、原始错误文本、完整 checkpoint URI 或高基数 hash。高维证据应写入审计账本/对象存储，指标仅携带低基数 job、集群、阶段和受控 experiment ID。
