# GPU 超时与高不确定性下的 LLM 安全托管状态机

## 核心原则

这里的“LLM 安全托管”不是让 LLM 接管机械臂控制，而是让 LLM 在机器人已经进入**局部安全保持**后，协助解释上下文、选择受限恢复选项、向用户提问或终止任务。低层保持、撤退、急停、保护区、速度/力限幅和轨迹到期检查必须独立运行，不等待 LLM。

## 状态与转换

| 状态 | 进入条件 | 机器人实际动作 | LLM 可做的事 | 退出条件 |
|---|---|---|---|---|
| `NORMAL` | 状态新鲜、模型按 deadline 返回、风险/不确定性低、安全门批准 | 跟踪有效短轨迹窗口 | 低频管理子目标，不在控制回路。 | 任一软/硬异常。 |
| `CAUTIOUS_OBSERVE` | 高不确定性但无硬危险；预测残差上升 | 停止发新进取轨迹；补充观测或切换预验证的低风险姿态。 | 解释“为何需要更多观察”；选择已注册的观察技能。 | 置信恢复、或转保持。 |
| `LOCAL_HOLD` | GPU deadline miss、模型进程繁忙/故障、状态过期、轨迹 TTL 到期 | 继续当前未过期安全段；否则平滑减速并保持到安全姿态。 | 尚未调用或仅异步接收事件。 | 进入托管、恢复或硬停。 |
| `LLM_SUPERVISION` | 局部安全状态已确认，且需要语义恢复/用户判断 | 保持；不进行自由移动。 | 仅输出 `OBSERVE`、`ASK_USER`、`SELECT_PREAPPROVED_SKILL`、`END_TASK`、`RETRY_AFTER_HEALTH` 等白名单决策。 | 通过 Recovery Gate，或用户终止。 |
| `REPLAN_PENDING` | 新鲜观测、模型健康、确定性安全条件满足 | 维持安全姿态，等待新的短期已批准窗口。 | 更新 PlanSpec/子目标，但不发控制命令。 | 新轨迹由 SafetyKernel 批准。 |
| `HARD_STOP` | 人/宠物近场、力异常、碰撞、急停、厂商安全告警 | 厂商控制域执行安全停止。 | 仅生成说明与事件摘要。 | 人工复位及独立安全检查。 |

## 平滑过渡的顺序

1. **本地立即裁决。** 在 GPU 超时或不确定性超过阈值时，安全内核原子化禁止新轨迹进入队列；此步骤不调用 LLM。
2. **安全消耗或保持。** 仅允许执行剩余仍有效的短期安全轨迹；到期或近场风险出现时，轨迹桥接器以受限减速度进入已验证的保持/撤退姿态。
3. **冻结事实快照。** 记录状态版本、传感器时间范围、当前动作、模型/标定版本、超时/不确定性原因和已执行轨迹；事实不可由 LLM 覆盖。
4. **异步托管。** LLM 接收结构化 `FallbackEvent`，只能给出白名单内的恢复建议；它不得输出关节/力/速度、解除安全锁或复用过期状态。
5. **确定性恢复验证。** Recovery Gate 检查观察是否新鲜、人员/宠物距离、硬件健康、预测器恢复、动作 DSL、权限和轨迹 TTL；全部通过才进入 `REPLAN_PENDING`。
6. **渐进恢复。** 只恢复到低速、短窗口、低风险已注册技能。连续健康窗口和事后验证通过后，才退出保守模式。

## GPU 超时的工程限制

在 Python/PyTorch 中，超时返回不能可靠中止正在运行的 CUDA kernel。因此推理应在独立、单飞（single-flight）、有界队列的工作进程中执行；超时请求的结果必须按 `request_id`/`state_version` 丢弃，不得在稍后写回轨迹队列。控制域只消费未过期且通过安全门的窗口。
