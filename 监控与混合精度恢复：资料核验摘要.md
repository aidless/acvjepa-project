# 监控与混合精度恢复：资料核验摘要

1. Prometheus 指标名应使用应用前缀、单一量/单位和基础单位；每个唯一 label 组合都会产生时间序列，因此不应使用 work ID、用户 ID、hash 或其它无界高基数标签。来源：https://prometheus.io/docs/practices/naming/
2. Grafana Prometheus alert 文档建议为短暂查询错误采用 Keep Last State，使用合理 pending period，优先用 recording rules 预聚合昂贵/高基数查询，并使用 absent/absent_over_time 检测失联指标。来源：https://grafana.com/docs/grafana/latest/datasources/prometheus/alerting/
3. PyTorch AMP 文档说明：AMP 使用 float16 时通常需要 GradScaler 以避免梯度 underflow；autocast 与 GradScaler 模块化；CPU bf16 示例只使用 autocast。文档还提示 GradScaler scale 不保证大于 1。来源：https://docs.pytorch.org/docs/2.13/amp.html
4. NVIDIA Transformer Engine FAQ 说明 FP8 metadata（scaling factors 和 amax histories）保存在 checkpoint 的 `._extra_state`；不同版本的 key 位置可能变化，因此恢复时必须绑定 Transformer Engine 版本并验证 state key。来源：https://docs.nvidia.com/deeplearning/transformer-engine/user-guide/faq.html
5. “绝对数值对齐”必须限于同一 checkpoint、相同 precision backend/版本、相同 optimizer 实现、相同 world-size/确定性运行条件下的状态恢复比较。world-size 改变或通信/算子归约树改变时，正确合同是从同一 committed checkpoint 无丢失加载状态，而不是声称中断前未提交 update 的逐 bit 连续性。
