# JEPA 世界模型训练的最大数据与算力瓶颈

## 结论先行

对 AMI Labs 而言，公开资料证实其目标是以真实世界传感器数据学习抽象表征，并开发动作条件世界模型；但其官网**没有披露**训练集规模、模型参数、机器人轨迹量、训练集群或单位训练成本。[1] 因此，不能严谨地声称“AMI 已经拥有某规模的数据/算力”或给出其具体 GPU 预算。

不过，以 V-JEPA 2、JEPA-WMs 等公开工作为工程参照，最大的难题并不是“再找更多视频”或“再买更多 GPU”中的单独一项，而是以下组合：

> **第一瓶颈是高质量、同步、动作条件且覆盖长尾的交互数据；第二瓶颈是把大规模视频表征预训练、动作条件后训练与在线滚动规划同时放进可承受的计算与时延预算。**

JEPA 可以避开像素级生成的一部分冗余，因而有机会降低规划阶段的表示成本；但它没有消除互联网规模视频预训练、真实交互数据质量、模型式规划候选搜索和安全验证带来的系统成本。[2] [3]

## 1. 先区分：AMI 的公开事实与工程推断

| 事项 | 公开可核验的信息 | 不能从公开资料推出的内容 |
|---|---|---|
| **AMI Labs 的目标** | AMI 表示将从连续、高维、噪声化的真实世界传感器数据中学习抽象表征，并开发动作条件世界模型以预测行动后果、规划任务序列和遵守安全护栏。[1] | 具体采用哪类 JEPA 变体、是否使用特定机器人数据集、是否已部署机器人产品。 |
| **AMI 的数据与算力** | 官方网站未公布数据规模、训练硬件、模型规模、训练时长、数据许可或推理吞吐。 | “AMI 使用多少小时视频、多少张 GPU、成本多少”均不能作为事实陈述。 |
| **开源参照：V-JEPA 2** | 论文报告超过 100 万小时互联网视频和 100 万图像的动作无关预训练；随后以少于 62 小时的 DROID 未标注机器人视频/交互数据训练动作条件 V-JEPA 2-AC；其预训练编码器规模最高达约 10 亿参数，动作条件模型约 3 亿参数。[2] | 这些配置不能等同于 AMI 当前或未来配置。 |
| **开源参照：JEPA-WMs** | 研究系统检验了多步 rollout、预测器、上下文长度、本体感觉、编码器、模型大小、数据增强与规划优化器对物理规划的作用。[3] | 没有证明任意 JEPA 配置都能在任意真实任务中高效规划。 |

## 2. 数据瓶颈：真正稀缺的是“可用于行动反事实”的数据

### 2.1 被动视频很丰富，动作条件数据并不丰富

互联网视频可提供大量外观、运动、遮挡与常见物理规律的观察样本。V-JEPA 2 的两阶段策略正是先用超过百万小时的视频获得广泛视觉表征，再以少量机器人数据引入行动条件。[2] 这说明视频能显著降低对任务特定示范的依赖，但它不能直接回答最关键的问题：

> **在当前状态下，如果“我”施加这个动作，环境会如何变化？**

公开视频通常没有可用的机器人动作、动作坐标系、夹爪状态、施力、延迟、相机外参或失败标记。即使画面里有人拿起杯子，系统也不知道采用了什么抓取姿态、摩擦条件、力控策略或关节路径。因此，动作条件世界模型仍需要与实体、传感器和执行器严格对齐的交互轨迹；对家用双臂机器人而言，还要覆盖不同材质、重量、湿滑程度、家具布局、人体干扰和多物体接触。

### 2.2 最大的数据缺口不是平均样本，而是干预覆盖与长尾失败

一个可部署 Agent 不只应在“桌面干净、光照稳定、物体摆放标准”的平均案例中预测未来。它还必须识别抓取滑移、遮挡、相机抖动、门回弹、对象脆弱、儿童或宠物进入、安全边界附近操作、工具失败和网络中断。问题在于，这些样本在自然采集数据中罕见，而在真实机器人上主动制造又昂贵甚至不安全。

| 数据类型 | 对 JEPA 预训练的价值 | 对动作条件规划的缺口 | 主要工程代价 |
|---|---|---|---|
| 大规模公开视频 | 外观不变性、运动模式、常见物体与背景知识 | 缺少 Agent 的行动变量和可控干预 | 版权/许可、去重、质量过滤、视频解码和分布偏差。 |
| 人类第一视角视频 | 提供操作过程和物体互动线索 | 动作语义、人体与机器人本体差异、力/触觉缺失 | 多视角对齐、隐私、动作反推不确定。 |
| 机器人示范轨迹 | 提供 \((观察, 动作, 结果)\) 对齐 | 很难覆盖未见场景、异常与多本体 | 设备时间、维护、校准、人工监督与安全保障。 |
| 失败/恢复轨迹 | 学习不确定性、回退与何时请求帮助 | 数据常被训练集清理掉，稀缺且敏感 | 必须安全地设计采集，严禁为数据而制造危险。 |
| 触觉、力、深度与本体感觉 | 识别接触、滑移与可达性，是操作因果性的关键 | 传感器异构、缺失、漂移和时钟不同步 | 硬件成本、标定、时间同步和格式标准化。 |

**因此，最大的数据难题是“可用的干预密度”，不是原始视频时长。** 少量动作数据若覆盖精确且同步，可能非常有价值；但一旦要跨家庭、跨机器人、跨物体和跨任务泛化，其规模和多样性仍会迅速成为主约束。

### 2.3 数据质量管线本身是一项大工程

“自监督”并不意味着无需数据工程。V-JEPA 2 的公开数据处理已显示这一点：研究者对 YT1B 做镜头切分，保留约 **3.16 亿**场景片段；对中间帧运行 DINOv2 ViT-L 获取嵌入，聚成约 **150 万**簇，并为贴近目标分布最终保留包含约 **1.15 亿**场景的簇。[2] 这意味着至少有五类持续成本：大规模媒体存储与带宽、视频解码、嵌入提取、聚类/去重、数据分布重新加权与数据集版本治理。

对 AMI 或任何商业团队，还会额外叠加数据许可、隐私、人体/家庭场景敏感信息脱敏，以及与行业伙伴合作时的数据使用边界。对开源团队而言，即使模型代码公开，也未必能低成本复制同等规模、同等质量和同等合法性的训练语料。

## 3. 训练算力瓶颈：视频时空 token、预处理与分布式训练

### 3.1 JEPA 不生成像素，不等于视频训练廉价

JEPA 在潜在表示空间预测，避免为每个未来像素建模。这通常降低了“为规划而反复生成高保真视频”的负担；V-JEPA 2 论文也指出，基于视频生成的规划会因计算成本而难以在机器人执行中进行广泛评估。[2] 但 JEPA 的前端仍必须读取和编码大量时序视觉输入。

V-JEPA 2 的公开配方在主训练阶段使用 **16 帧、4 FPS、全局批量 3072**；冷却阶段增至 **64 帧**，并将裁剪尺寸升至最高 512。[2] 对视频 Transformer 而言，帧数、空间分辨率、patch/token 数、批量规模和模型尺寸会共同放大激活内存、通信和数据加载成本。动作无关编码器规模最高约 10 亿参数，也意味着优化器状态、检查点、并行通信和容错不再是小团队可忽略的开销。[2]

### 3.2 数据准备与训练同样竞争算力

视频 JEPA 的 GPU 预算不只花在反向传播。预训练前的数据嵌入、筛选、聚类和质量控制也需要大量离线计算；训练中则需要高吞吐、稳定的视频读取与解码，否则昂贵的加速器会因 I/O 饥饿而空转。实践中，数据管线、缓存策略、跨节点文件系统、检查点写入和训练可恢复性往往比单一模型算子更早成为吞吐瓶颈。

### 3.3 模型式规划把训练成本转成在线推理预算

动作条件模型约 3 亿参数看似比预训练编码器小，但在线规划不是“推理一次”。每个控制周期都可能要：生成多组候选双臂动作、对每组做长度为 \(H\) 的潜在 rollout、迭代优化、计算目标进展/约束风险/不确定性，再从第一步开始执行并重复。这类成本近似随：

> **候选数 × 优化迭代数 × 规划时域 × 模型前向成本 × 控制频率**

增长。

JEPA 使用潜在预测相比像素级视频生成更有优势，但当任务变成双臂协同、接触丰富、长时程且需要多个不确定性样本时，候选空间仍会爆炸。对家用机器人而言，训练集群再强也不能替代现场的低延迟：模型必须在安全控制窗口内给出足够可靠的排序，否则规划器就只能缩短时域、降低并行度，或退回到规则/反应式控制。

### 3.4 长时域既是数据问题，也是算力问题

预测窗口加长会带来两类后果：一是模型必须从数据中学习更长的因果链，二是 rollout 误差会递积。JEPA-WMs 将多步 rollout、训练上下文长度和规划优化器列为关键设计变量，正说明长程物理规划不是简单增加模型参数即可解决。[3] 长时域训练也增加显存、反向传播长度、数据解码量和迭代不稳定性；在线长时域规划则增加动作候选搜索成本。因而工程上更可行的做法通常是**长程任务图 + 短时域 MPC + 每窗口重新观测**，而不是要求世界模型一口气准确模拟整项家务任务。

## 4. 开源界与 AMI 的瓶颈排序不同

| 主体 | 最可能的首要瓶颈 | 次要瓶颈 | 原因 |
|---|---|---|---|
| **AMI Labs（基于公开定位的推断）** | 跨行业获取合法、连续、多模态且动作条件的数据，以及将其对齐为通用状态/动作表示 | 互联网级视频预训练集群、模型式规划吞吐、真实场景安全验证 | AMI 可依托资金与合作伙伴，但其差异化不应只是公开视频规模，而是高价值交互数据与可靠性闭环。具体资源未公开。[1] |
| **大型研究机构 / 平台公司** | 机器人交互数据的跨本体与长尾覆盖 | 预训练与数据工程规模、实时规划 | 已可训练大模型，但真实世界数据采集、设备和评估仍慢于互联网数据增长。 |
| **开源研究团队** | 获得/清洗/合法使用大规模视频及真实机器人轨迹；复现标定与环境 | GPU 集群、存储带宽、分布式训练稳定性 | 很多团队可复用公开权重，却难复制百万小时视频处理、设备时间和完整数据谱系。 |
| **产品/机器人团队** | 安全关键长尾、家庭/工厂现场的分布漂移和数据治理 | 现场推理时延、硬件可靠性、持续评估 | 业务价值取决于罕见事件不出错，而不是平均 benchmark 分数。 |

## 5. 哪些缓解策略真实可行

### 5.1 数据：分层采集，而非直接堆真实机器人小时数

推荐的工程数据策略是四层递进。第一层用合法的大规模视频预训练通用视觉时空表征；第二层用领域视频缩小环境分布差异；第三层采集少量但高度同步的机器人交互轨迹，确保动作、视觉、触觉和结果对齐；第四层依据模型不确定性与失败日志进行主动采集。这样能把昂贵的机器人时间投入在“模型最不知道什么”的状态—动作区域。

高价值数据不应只记录成功示范。应有选择地记录安全失败、抓取滑移、传感器遮挡、暂停、恢复与人工接管；但必须在仿真、受控夹具或严格安全协议下采集，绝不能在家庭/工厂中主动制造危险。

### 5.2 计算：把重训练、轻后训练与限预算规划解耦

| 环节 | 推荐策略 | 目的 |
|---|---|---|
| 视频预训练 | 复用公开预训练编码器；先在较小、领域相关数据上评估，再决定是否进行大规模继续训练 | 避免每个团队重复承担百万小时预训练成本。 |
| 动作条件后训练 | 冻结或部分冻结视觉编码器，只训练动作条件预测器、风险头和必要适配层 | 将稀缺机器人数据和训练预算集中于行动后果。 |
| 数据管线 | 先做去重、镜头切分、质量过滤、时钟对齐和版本化；使用缓存与顺序读取减少 I/O 饥饿 | 将昂贵加速器用于模型，而不是等待解码/网络。 |
| 在线规划 | 用策略先验 warm-start 候选；自适应减少候选数/时域；只在高不确定性或高价值决策时扩大搜索 | 控制“候选 × 时域 × 迭代”的计算爆炸。 |
| 长程任务 | LLM/任务图做低频分解，JEPA-MPC 只做局部滚动前瞻 | 防止长 rollout 的显存、时延和误差累积。 |
| 安全 | 不依赖世界模型独自判定安全；用几何、权限、力控和急停做确定性外壳 | 即使为了时延缩减模型预算，也不削弱硬约束。 |

## 6. 最关键的工程判断

如果只能选一个优先投资方向，我会选择：

> **构建版本化、动作同步、多模态、包含失败与恢复、可合法使用的交互数据飞轮。**

原因是计算可以通过复用预训练模型、冻结编码器、蒸馏、分布式训练和规划预算管理逐步优化；但一个没有真实行动变量、时间对齐、失败长尾和安全语义的数据集，无法仅靠增加算力变成可靠的动作条件世界模型。AMI 的公开愿景恰好把焦点放在真实世界传感器、持久记忆、规划、可控与安全上；但是否能建立这种高价值数据闭环，仍是其尚未公开、也最值得观察的执行问题。[1]

## 参考资料

[1]: [AMI Labs, *Real World. Real Intelligence.*](https://amilabs.xyz/)

[2]: [Assran et al., *V-JEPA 2: Self-Supervised Video Models Enable Understanding, Prediction and Planning*, arXiv:2506.09985](https://arxiv.org/html/2506.09985v1)

[3]: [Terver et al., *What Drives Success in Physical Planning with Joint-Embedding Predictive World Models?*, arXiv:2512.24497](https://arxiv.org/html/2512.24497v1)

[4]: [Meta AI, *Introducing V-JEPA 2*](https://ai.meta.com/research/vjepa/)
