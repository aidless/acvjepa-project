# M3 闭环评测设计（P3 MPC 规划对比）

> 对应 `PROJECT_PLAN.md` M3 里程碑与 `轻量级 V-JEPA` 方案 §5.2 P3：在 RoboCasa 做图像/状态目标条件 MPC，
> 比较「无世界模型 / 冻结特征 / 轻量 JEPA」三种排序方式。本文件是评测的**预先登记**（避免事后选择指标），
> M3 执行前应冻结此设计（版本号 v1，改动需决策记录）。

## 1. 评测问题与假设对应

| 假设 | 评测问题 |
|---|---|
| H1（研究假设） | 轻量 JEPA 世界模型在相同候选预算下，能否比「冻结视觉特征」和「无世界模型（几何/随机）」基线显著提升规划成功率、降低约束违规或减少重规划次数？ |
| H0（零假设） | 无显著差异或负收益。 |

判定规则：**成功率 / 约束违规率 / 重规划次数**三项中至少一项有显著改进（配对检验 + 效应量），其余不得显著退化，才算支持 H1；否则报告 H0 方向证据。

## 2. 基线（三组，同一候选动作集）

| 组 | 候选排序器 | 说明 |
|---|---|---|
| A. 无世界模型 | 随机 / 几何可达性启发（如末端距离 + 障碍惩罚） | 不调用任何学习模型；下界参照 |
| B. 冻结视觉特征 | 冻结 V-JEPA 编码器输出的 z_t，用线性/余弦距离排序候选的期望未来状态 | 无预测，仅表征 |
| C. 轻量 JEPA（待测） | `z_t, a→z_{t+1}` 动作条件预测器 rollout H 步，按预测目标达成 + 约束风险 + 不确定性排序候选 | 本项目的世界模型 |

关键控制：三组使用**完全相同的候选动作生成器与 MPC 预算**（候选数 K、规划时域 H、每步执行首个动作再重规划），唯一差异是排序信号来源。避免把「候选生成更好」误归因为「世界模型更好」。

## 3. 任务与数据

- 环境：RoboCasa（钉版任务 id）；任务子集：与「早餐台整理」相关的拾取放置、门/抽屉、桌面整理，10–30 任务模板起步。
- 隔离协议（沿用 DATA_MANIFEST 纪律）：**按任务模板 + 布局 + 对象实例划分**训练/验证/测试，禁止帧级随机切分。评测任务（未见布局/对象）独立于模型训练数据。
- 每任务重复：≥20 次 rollout（不同随机种子、初始布局扰动），记录逐 roll 结果。

## 4. 指标（预先登记）

| 指标 | 定义 | 方向 |
|---|---|---|
| 任务成功率 | 任务定义的成功判据（如入盘、门到位）达成比例 | 越高越好 |
| 约束违规率 | rollout 中出现硬约束违反（保护区、力上限）的比例 | 越低越好 |
| 重规划次数 | 每 rollout 中触发重新规划的平均次数（相同预算下更低=预测更准） | 越低越好 |
| 平均动作数 | 完成任务消耗的动作步数 | 越低越好 |
| 分布外不确定性 | 模型对未见布局的预测不确定性（校准：不确定性与实际误差相关性） | 单调正相关 |
| sim-to-real 差距 | 同任务仿真与真实回放成功率之差（M5 才可测） | 越小越好 |

## 5. 统计设计

- 主检验：按**任务模板分层**的配对比较（同一任务模板内 A/B/C 三组两两配对），用 Wilcoxon signed-rank（rollout 成功率）；多重比较用 Holm 校正。
- 效应量：成功率差异的 Cohen's h / 风险差 + 95% CI；重规划次数用 Cliff's delta。
- 功效：每任务 ≥20 rollout 至少能检出 ≥15 个百分点成功率差（α=0.05，双侧）——若实际差异小于此，判定「无实际意义差异」，不把 p 值当背书。
- 预注册本文件 v1 后**不允许**：临时增删指标、换检验、合并失败模式重定义成功。

## 6. 判定阈值（对应 PROJECT_PLAN H1/H0）

| 结果模式 | 判定 |
|---|---|
| C 组在 ≥1 项主指标显著优于 A 与 B，其余无显著退化 | 支持 H1（记录效应量与局限） |
| C 组与 B 组无显著差异，但显著优于 A | 部分支持（表征已够，世界模型边际收益有限——同样是有价值的结论） |
| C 组不显著优于 B，且不劣于 B | H0 方向：JEPA 在受限场景收益有限；基础设施复用于 VLA/策略路线 |
| C 组显著劣于 B | 记录负结果并诊断（预测误差积累、校准失败、候选分布） |

## 7. 执行依赖与顺序

1. M2 真实训练完成（P1 域适配 + P2 动作条件后训练）→ 得到「轻量 JEPA」C 组模型。
2. RoboCasa 环境 + ≥24GB GPU（评测需要在线 rollout + MPC 候选推理）。
3. 本文件 v1 冻结 → 实施评测代码（`m3_mpc_eval.py`，待 M2 后实现）→ 结果表 → M4 判定报告。
