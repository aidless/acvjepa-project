# 主计划：「从 LLM 路线之争到 AC-VJEPA 可运行工程体系」

> 版本：v1 ｜ 确立日期：2026-08-15 ｜ 批准：用户（plan mode 批准）
> 前置文档：`决策记录.md`、`STATUS.md`、`BACKLOG.md`、`VERIFY_RESULTS.md`、`CLUSTER_VALIDATION_RUNBOOK.md`、`轻量级 V-JEPA：突破数据与算力限制的技术路线与实验室实施方案.md`

## 一、命题与本计划目的

**路线之争**（本项目起点）：LeCun 主张「纯语言缩放不足以形成可靠自主智能体，LLM 路线是死胡同」；本项目阶段一已核验并给出立场——问题诊断成立，但「LLM 注定无用/被替代」超出证据；更可能的终局是 **LLM × JEPA 混合智能体**（见 `杨立昆、JEPA 与 AMI Labs：技术路线与产业叙事的审慎评估.md`）。

**本计划的转化**：不满足于停留在叙事判断，而是把「JEPA 路线是否真有收益」变成**可证伪的实验命题**，并围绕它把已交付的 AC-VJEPA 工程资产组装成可运行、可复现、可判定的实验体系。

**计划产物**：本文件即主计划；里程碑按 M0–M5 推进，每阶段门禁记录在案。

## 二、可证伪命题（实验层核心假设）

| 假设 | 表述 | 判定方式 |
|---|---|---|
| **H1（研究假设）** | 在受限家居操控分布内，动作条件潜在预测（轻量 JEPA）比「冻结视觉编码器 + 反应式策略」基线更优：短期状态/事件预测更准、动作候选排序更好、重规划/失败率更低。 | P3 闭环评测：相同候选预算下成功率/违规率/重规划次数至少一项显著改进（含 OOD 与 sim-to-real 差距报告）。 |
| **H0（零假设）** | 无显著差异或负收益。 | 若 H0 成立，负结果即有效结论（JEPA 在受限场景收益有限）——同样写入判定报告，基础设施仍可复用于 VLA/策略学习。 |

成功标准（项目级）：产出**有证据的路线判定报告** + **可复现的工程基座**（当前 42/42 验证全绿）+ 决策记录闭环。

## 三、分层实施计划

### L1 研究层（已完成 90%，持续维护）
- 已交付：主评估报告、11 页 PPT、findings 六件套、深度报告五份、C9 动态核验（2026-08-15）。
- 持续：按季度更新 C9（AMI/开源 JEPA-Agent 动态）；追踪 V-JEPA 2-AC / JEPA-WMs / VLA-JEPA 上游进展。

### L2 工程基座层（已完成，验证 42/42 全绿）
- 资产：训练核心、DDP/torchrun 训练、NCCL 弹性恢复、2:1 异构微批、UpdatePlan、混沌注入、三层 KV 缓存、HITL 账本、监控告警、GitOps 仲裁、CI/CD、本地/容器演练、`verify_all.ps1` 基线。
- 本层已闭环；后续仅随 L3 需求做接口扩展。

### L3 实验实现层（新工作，本计划核心）
**接口缺口（已确认）**：`ac_vjepa_core.ActionConditionedVJEPA` 为自研模型；`--init-from` 仅接受自研 checkpoint；官方 V-JEPA 2.1（80M ViT-B/16）权重适配层不存在。轻量方案的起点恰是复用官方权重。

1. **新增 `vjepa_backbone.py`**：官方 V-JEPA 2.1 checkpoint 加载适配器——映射官方 state_dict → 冻结/轻适配骨干，输出潜在表征 z_t；显式支持冻结、末层解冻、LoRA（按 `轻量级 V-JEPA` 文档 5.1 表）。
2. **扩展 `train_ac_vjepa_ddp.py`**：`--init-from` 增加 `vjepa2:<path>` 模式（官方权重）；动作头/预测器从自研模块接出；保持 `--init-from` 既有自研语义不变。
3. **新增 `vjepa_backbone_smoke.py`**：合成数据验证（官方权重缺失时以随机初始化同构 ViT-B/16 走通接口，权重就绪后复跑真实加载）。
4. **装配演示数据**：`make_demo_ddp_data.py` 已可用（窗口格式与官方 V-JEPA 视频格式的转换函数写入 backbone 适配器内）。

### L4 数据层（按四层金字塔装配）
- A 层：官方 V-JEPA 2.1 权重（需下载，约数百 MB；网络受限时先以同构随机权重走通管线，权重获取列为前置项与风险）。
- B 层：本地无标签视频（20–50h，需合法采集/许可）。
- C 层：RoboCasa 仿真子集（10–30 任务模板，含反事实与长尾）。
- D 层：真实双臂轨迹（5–10h，时间同步；P4 阶段才需要）。
- 数据纪律：按布局/对象实例/任务组合隔离训练测试集；时间戳/标定/动作坐标系一致 → 落成 `DATA_MANIFEST.md` 清单文件。

### L5 集群验证层（需硬件，执行清单已产出）
- B1–B9 按 `CLUSTER_VALIDATION_RUNBOOK.md` 逐项执行：NCCL 压测、RDMA 演练、阈值校准、FP8/AMP 扩展、K8s 隔离 Job、故障恢复、分波校准、三层缓存硬件验证。
- 无集群时保持 blocked，不阻塞 L3/L4 本机进度。

### L6 治理与安全层
- 决策记录、BACKLOG、安全边界、HITL 双重审核、混沌单变量纪律持续维护；每里程碑结束追加决策条目。

## 四、里程碑与验收门禁

| 里程碑 | 内容 | 通过标准 | 依赖 |
|---|---|---|---|
| **M0（已完成）** | 接管、结构还原、Windows 兼容修复、42/42 验证基线、文档体系 | git 3 提交、工作树干净 | — |
| **M1 实验基座** | `vjepa_backbone.py` + 训练器扩展 + 冒烟；`DATA_MANIFEST.md` | 同构权重冒烟通过；官方权重（若有）加载路径验证；数据集清单落盘 | 本机可做（权重下载为可选前置） |
| **M2 域适配+后训练** | P1 无动作 JEPA 域适配；P2 动作条件后训练 | 短时潜在预测优于最后帧基线；`z_t,a_t→z_{t+1}` 事件预测改进 | ≥24GB GPU 或小配置（本机 6GB 仅能做微型验证）；B 层/C 层数据 |
| **M3 闭环评测** | P3 MPC 规划对比（随机/几何/冻结视觉/轻量 JEPA） | 相同候选预算下成功率/违规率/重规划至少一项明确改进 | RoboCasa 环境 + GPU |
| **M4 判定报告** | 汇总 H1/H0 判定、工程复现说明、对路线之争的更新评估 | 有证据的结论（正负均可）；决策记录追加 | M2+M3 结果 |
| **M5（可选）实机** | P4 影子模式→低速受控真实回放 | 预测残差/抓取放置校验/急停满足 `AC-V-JEPA 双臂部署：实机时延与控制频率验收协议.md` | 双臂硬件 + 受控场地 |

## 五、边界与明确不做

- 不做互联网级 JEPA 预训练复现（明确不建议，见轻量方案文档）；不做端到端 LLM 控制机器人（LLM 仅高层分解 + 受限安全托管）；不改原 zip/E 盘原件。
- B 级集群项在本机不执行（无硬件）；C 级（C1–C8）保持 blocked 待外部系统/审批。

## 六、风险与失败模式

| 风险 | 缓解 |
|---|---|
| 官方 V-JEPA 权重下载/许可受限 | 同构随机初始化走通接口；权重获取独立跟踪；B 层自采数据不依赖官方权重 |
| 本机 6GB 显存不足以做 P2/P3 | M2/M3 需 ≥24GB 或集群；本机先微型冒烟（降分辨率/短窗口），结果只作方向性信号 |
| 数据获取（B/C 层）周期长 | 先行装配管线与清单；RoboCasa 数据许可与子集选择列入前置任务 |
| JEPA 收益有限（H0 成立） | 按设计即有效结论；工程基座可复用于 VLA/策略路线，不白费 |
| 上游开源（V-JEPA 2-AC 等）演进 | C9 季度跟踪；适配器按 state_dict 映射隔离版本差异 |

## 七、执行方式与验收

1. 本计划已批准；M1 起按「代码 → 冒烟 → 回归 → 文档/决策 → git 提交」循环推进。
2. 每里程碑结束跑 `verify_all.ps1` 回归（保持全绿），更新 STATUS/BACKLOG，追加决策条目。
3. 验收：M1 代码+冒烟+数据清单落地；每里程碑有门禁记录；M4 判定报告对 H1/H0 给出有证据的结论。
4. 明确假设：B/C 层数据与 ≥24GB GPU 属外部依赖，无资源时 M2/M3 顺延、M1 不阻塞；权重下载属可选前置。
