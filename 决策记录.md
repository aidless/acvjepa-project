# 决策记录

| 日期 | 决策 | 类别 | 依据/理由 | 来源 | 未决影响 |
|---|---|---|---|---|---|
| 2026-08-14 | 将用户文本视为待核查的叙事，而非既定事实；分别核验技术主张、人物职业动态、公司成立与融资、合作计划。 | 方法 | 文本含有具体日期、金额、引述与人员信息，部分可能为推断、转述或尚无法独立证实的信息。 | 用户提供材料；后续以一手资料和可靠报道交叉验证。 | 某些私下谈话与传闻可能无法获得公开证据，只能标为未证实。 |
| 2026-08-14 | 结论将区分事实、技术解释与个人评估，并避免把“LLM的局限”误写为“LLM无价值”或把JEPA写成已被完全验证的AGI路线。 | 分析框架 | 该领域仍处于快速发展与经验验证阶段，路线优劣不能仅由单一模型范式断言。 | 技术论文、研究机构信息、公开访谈与权威媒体。 | 是否存在2026年后续融资、产品与合作，须以最新来源为准。 |

| 2026-08-15 | 审核账本采用“受控对象存储中的原始多模态证据 + SQLite 事件投影”的分层方式。账本只保存内容哈希、Merkle 证据根、事件、签名和锚定；不保存大体积原始视频与点云。 | 安全与隐私 | 可验证引用减少隐私暴露与不可删除日志的冲突；内容绑定仍能支持事后重哈希验证。 | 用户需求、工程推断 | 生产对象存储仍需版本化、保留策略、访问日志与备份。 |
| 2026-08-15 | 每条审核事件使用 Ed25519 签名并绑定前一事件哈希与证据根；链头必须周期性复制到独立的 WORM/透明日志锚点。 | 防篡改 | 哈希链与签名让内容修改和重排可检测，但不能单独防止数据库整体删除；独立锚点补齐该威胁。 | NIST SP 800-12 第 18 章、工程推断 | 原型仅验证本地锚定签名；生产系统需接入独立锚定服务及密钥轮换。 |
| 2026-08-15 | 高风险数据补丁需经过多模态验证与两名不同审核者对同一 patch 哈希的批准，才获得“可进入数据候选池”资格。 | 人类在环与安全 | 防止单人误标或相互冲突的补丁进入训练；资格不等价于自动训练或部署。 | 用户此前的 HITL 双重审核约束 | 仍需数据治理、训练评测、影子模式和灰度发布门控。 |
| 2026-08-15 | 异构训练在每一次 optimizer update 前广播不可变 UpdatePlan；所有 rank 在最后一个本地微批共同执行一次同步 backward。 | 分布式正确性 | DDP 默认按 rank 平均梯度；使用 `world_size / global_samples` 缩放 local mean loss 可恢复全局样本均值。 | PyTorch DDP/NCCL collective 语义、推导 | 对可变 token/时间掩码必须采用有效 token 的 loss-sum，而不只是 batch-size 加权。 |
| 2026-08-15 | rank 失效、计划摘要不一致或成员变化均停止当前 update，并只在持久 checkpoint 后重建过程组。 | 故障安全 | NCCL collective 要求所有 rank 以一致顺序参与；中途弹性变更会破坏该契约。 | NCCL/PyTorch 文档、工程推断 | 生产须配置 torchrun elastic、NCCL 健康监控及操作员处置 SOP。 |
| 2026-08-15 | 真实 NCCL 集群压测采用“拓扑基线→微基准→应用级动态计划→受控故障恢复”的阶段化设计，性能以同环境健康基线区间评估，不预设跨硬件通用的 GB/s 阈值。 | 性能与验证 | 集体带宽受 GPU、互联、消息、rank 数、网络 rail 和负载影响；尾延迟与恢复正确性比单一平均带宽更能反映实际动态训练风险。 | NCCL/PyTorch 官方文档、工程推断 | 需由实际集群运行后建立具体门槛。 |
| 2026-08-15 | Gloo 双进程测试仅作为动态计划、2:1 本地微批、全局样本缩放和局部参数副本一致性的语义回归，不作为 NCCL 带宽、GPU 拓扑或 RDMA 故障恢复的验收。 | 测试边界 | CPU/Gloo 不覆盖 CUDA stream、NCCL watchdog、NVLink、IB/RoCE、GPU-NIC 亲和或硬件尾延迟。 | 本地测试代码、NCCL/PyTorch 文档 | 需要在隔离 GPU 集群复跑并扩展到全量 state/EMA/optimizer 对比。 |
| 2026-08-15 | 任一 worker/节点/成员变化都使当前 UpdatePlan 失效；整个 worker group 需从最近确认 checkpoint 重建，再收集 telemetry 并广播新计划。 | 故障恢复正确性 | torchrun 的故障/成员变化语义会停止现存 workers 并用新的 rank/world-size 建立 group；旧 rank 不稳定。 | PyTorch torchrun Elastic 文档 | checkpoint 提交协议和数据游标恢复需在真实作业中演练。 |
| 2026-08-15 | 2:1 异构微批回归测试从单参数比较升级为完整模型、EMA、buffer 与 AdamW optimizer state 的递归逐项比较，并增加同六样本单进程参考更新。 | 训练正确性 | 仅比较一个参数无法发现目标编码器、优化器矩或参数组分叉；单进程参考避免两个分布式副本“同错但一致”。 | 本地 Gloo 集成测试 | GPU/AMP/通信压缩下需重新校准误差预算。 |
| 2026-08-15 | 单进程参考路径必须与分布式路径对齐 loss 缩放、梯度裁剪、optimizer 和 EMA；发现参考遗漏梯度裁剪后修复，再以 2e-5 比较完整状态。 | 测试预言机 | 缺失 clip 会造成 AdamW moment 差异，属于参考实现缺陷而非 DDP 分叉。修复后 118 个跨 rank 及 118 个参考张量条目通过。 | `test_dynamic_nccl_full_state_equivalence.py` 运行结果 | 多 update、AMP/TF32 和 lossy comm hook 仍需独立阈值。 |
| 2026-08-15 | 跨节点 UpdatePlan 绑定 topology epoch/digest 与完整 work manifest digest；拓扑仅影响下一 update 的预算与数据分桶，绝不在 active collective 内改变成员或网络配置。 | 跨节点安全与正确性 | torchrun 失败/成员变化会重新形成 group 并可能改变 rank/world-size；绑定 elastic run/restart 及 inventory 记录可阻止旧计划复用。 | PyTorch/NCCL 文档、`topology_aware_update_plan.py` | 真实 topology probe 的认证、inventory 服务和 GPU 集群压测尚待部署。 |
| 2026-08-15 | 对当前 Python DDP/torchrun 训练器，NCCL async/watchdog/成员变化统一按 worker-group 失败处理：放弃 in-flight update，从最近确认 checkpoint 重建 group、拓扑、work manifest 和 UpdatePlan。 | 弹性恢复正确性 | 原生 NCCL communicator shrink/grow 不会自动重建 DDP bucket、optimizer、EMA、dataloader 分片或自定义计划状态；在未建立专用运行时前不应在进程内续跑。 | NCCL communicator/Fault Tolerance 与 torchrun 官方文档 | 若未来采用原生通信运行时，需单独证明所有训练状态重构的正确性。 |
| 2026-08-15 | 混沌演练按 L0—L5 分层：先逻辑 failpoint，再 worker 退出，最后才在专用 VLAN/节点池演练 RDMA/rail/rendezvous 分区；每轮只注入一个故障。 | 实验安全与可解释性 | 单变量演练能归因并验证停止/恢复合同；叠加故障会使根因、影响范围和恢复状态不可审计。 | 用户需求、NCCL/PyTorch 文档、工程推断 | 真实网络分区需基础设施授权和目标集群变更窗口。 |
| 2026-08-15 | checkpoint ledger 仅在 optimizer、EMA 与可验证 checkpoint 原子发布后写入 COMMITTED；`after_step_before_commit` 故障必须被视为未提交并从前一 checkpoint 重放。 | 状态原子性 | 防止内存中已执行但未持久化的 optimizer state 与数据 cursor 被错误当作事实。 | 现有训练/账本设计、工程推断 | 严格一次性样本语义仍需更高层事务性数据 cursor。 |
| 2026-08-15 | 将弹性训练数据恢复定义为 rank 无关的全局 work-window cursor：仅已验证 checkpoint 的 COMMITTED 记录推进 offset；任何 in-flight 2:1 attempt 在宕机后 ABORTED 并从同一连续 range at-least-once 重读。 | 数据一致性 | torchrun 重建后 RANK/WORLD_SIZE/拓扑均可能变化，拼接 rank-local sampler/梯度/optimizer 状态会破坏全局 update 语义。 | `elastic_data_cursor_ledger.py` 烟雾测试 | 严格 exactly-once 样本消费需要外部事务性数据系统。 |
| 2026-08-15 | RDMA/rail 自动化采用请求守卫 + 基础设施自有受限 executor 的双层模型；交付代码只做请求校验和本地 mock 回滚，不含任何真实网络扰动命令。 | 网络演练安全 | 训练作业不应持有共享网络控制权限。守卫验证干运行、环境隔离、静态范围、双人审批、TTL、checkpoint/健康、topology/work digest 与 finally rollback。 | `rdma_rail_chaos_guard.py` 烟雾测试 | 真实 executor 必须在服务器端独立实施 mTLS、标签、TTL 和审计，且仅在批准的测试窗口执行。 |
| 2026-08-15 | 监控设计采用 Prometheus 低基数指标 + ledger/对象存储高维证据分离；告警只允许冻结新计划、标记 SUSPECT、通知和保留工件，不可直接触发网络、cursor 或发布操作。 | 可观测性安全 | Prometheus 高基数 label 会产生不可控时间序列；监控不应成为基础设施执行权限通道。 | Prometheus/Grafana 官方文档、`distributed_training_observability.py` | 部署时需依据实际 cluster/job 规模设置 series budget 和 scrape 方案。 |
| 2026-08-15 | 混合精度恢复的“绝对对齐”限定为同一 COMMITTED checkpoint load 后、下一 forward 前 model/EMA/AdamW/scaler 或 FP8 metadata/RNG 的精确 fingerprint 相等；world-size 变化后不承诺下一 update 逐 bit 连续。 | 数值恢复正确性 | 失败的 in-flight update 不可重构；FP8 metadata 和 FP16 GradScaler 都是数值状态。 | PyTorch AMP/Transformer Engine 文档、`mixed_precision_elastic_recovery.py` smoke test | CUDA RNG、scheduler、FSDP/ZeRO shard 和具体 FP8 backend 仍须在目标集群扩展测试。 |
| 2026-08-15 | FP16 overflow 建议采用不推进 cursor 的 PRECISION_STATE_COMMIT：持久化 scaler 变化但重读同一 work range；optimizer/EMA/scheduler 仅在实际 step 后推进。 | 数据与 precision 对齐 | 防止 GradScaler backoff 丢失或无 optimizer update 的样本被错误记为已训练。 | AMP 语义与工程推断 | 需将事件类型接入实际 checkpoint/ledger schema。 |
| 2026-08-15 | 将大规模抢占的恢复优化目标定义为 RTO_trusted（新 worker group 通过 checkpoint、precision、cursor、topology、plan 与 shadow 验证），而非仅进程重启时间；采用分波 admission、指数退避、确定性抖动与 checkpoint 读并发上限防止 rendezvous/对象存储风暴。 | 弹性恢复性能 | torchrun 在成员变化后重建 worker group 且 rank 不稳定；旧 in-flight 状态不可复用。 | `rapid_recovery_alert_drill.py` smoke test、PyTorch torchrun 文档 | wave/concurrency 参数必须由实际 rendezvous、对象存储和网络压测校准。 |
| 2026-08-15 | Alertmanager 联动限制为允许列表的非破坏性动作：freeze new plans、mark SUSPECT、capture evidence intent、notify/escalate；禁止 alert webhook 直接控制网络、节点、cursor 或发布。 | 告警自动化安全 | webhook 是通知/集成接口而非基础设施权限通道；重复投递必须按 fingerprint 幂等。 | `rapid_recovery_alert_drill.py` 的 Alertmanager-style payload parser 与 smoke test | 生产 adapter 仍需 mTLS、身份、速率限制、持久去重和服务端 allowlist。 |
| 2026-08-15 | 2:1 自动化混沌测试采用逻辑 failpoint（final AllReduce 节点失效、prepare 后分区、stale plan、topology mismatch、cache corruption），一次只注入一个故障，验证 cursor、plan fencing、重放范围与安全告警合同；不包含任何真实网络/节点扰动命令。 | 混沌测试安全与正确性 | 异构微批的局部 no_sync 进度无法当作提交事实，必须验证未提交范围的完整重读。 | `heterogeneous_microbatch_chaos_framework.py` smoke：5 scenarios、17 assertions | 真实 L3/L4 事件须由独立受控 executor 在隔离资源池执行。 |
| 2026-08-15 | checkpoint 加速采用“强一致小 pointer + durable content-addressed shard + 可丢失 verified cache”三层；RDMA/节点缓存只读且每次命中验证 hash，失效/损坏/TTL/契约不符回退 durable。 | 缓存与恢复一致性 | etcd 适合 linearizable CAS metadata，不适合大 tensor；GPUDirect RDMA 受拓扑/BAR/pinning 等硬件约束。 | `verified_checkpoint_cache.py` smoke：hit、corrupt fallback、CAS fence | 自定义 DCP StorageReader 与真实 RDMA/KV 集成仍需目标硬件与访问隔离验证。 |
| 2026-08-15 | 将五类 2:1 离线 failpoint 拆为独立 unittest：final AllReduce 节点失效、prepare 后分区、Rendezvous 后 stale plan、topology mismatch、optimizer cache corruption；另测试拒绝 production/non-dry-run。 | 测试覆盖 | 每个测试使用新 framework/experiment ID 并断言 cursor、fencing、global range replay、cache durable fallback 与安全告警行为。 | `test_heterogeneous_microbatch_failpoints.py`：6/6 通过 | 尚未替代真实 NCCL/RDMA/调度器隔离演练。 |
| 2026-08-15 | 高并发 checkpoint 缓存采用 single-flight、分层 token budget、TTL jitter、negative cache、自适应并发、完整性熔断和 pointer CAS fence；实测前使用离线模拟器验证调度不变量。 | 防雪崩与容量保护 | 强一致 KV 仅存小 pointer，冷 key storm 不应令 durable read 随 worker 数线性放大。 | `checkpoint_cache_load_shedding_simulator.py`：32 请求→1 durable read、31 coalesce；16 writer→1 CAS winner | 真实阈值和容量需根据目标 KV、对象存储、RDMA cache/rail 在专用测试池完成分阶段压测。 |
| 2026-08-15 | 为五类 failpoint 增加 bounded fault/phase 的恢复时延 histogram、触发 counter、active gauge；为 verified cache 增加 tier/component/outcome 的 fetch/bytes/duration 指标。细粒度 attempt/hash 只进入 JSON/audit artifact，不进入 Prometheus label。 | 可观测性与基数控制 | 需要在 fault class 之间聚合 p95，同时避免 work/checkpoint/GPU 等高基数标签。 | `distributed_training_observability.py` smoke、Prometheus instrumentation/histogram 文档 | 真实训练器需在协调器恢复边界调用 phase span API；离线计时不能作为生产 RTO 基线。 |
| 2026-08-15 | CI workflow 仅运行离线 compile/unit/cache/chaos/metric/config contract，采用 contents:read、固定 action SHA、无 secrets/infra mutation、受控 artifact；真实集群演练是独立审批任务。 | CI/CD 安全 | 不可信 PR 与可变 workflow 不得继承生产集群权限。 | `.github/workflows/failpoint-chaos-ci.yml`、`validate_failpoint_ci_config.py` smoke | 需由仓库维护者安装 workflow 并按组织策略审核 action SHA/runner 隔离。 |
| 2026-08-15 | Failpoint 5 corruption recovery 采用内容寻址验证→cache entry eviction/隔离→single-flight leader→有界 durable admission→验证后回填→waiter verified hit；KV committed pointer 与 cursor 在该路径中只读。 | 缓存完整性与防雪崩 | 损坏缓存不得变成无界 durable fallback，更不得借恢复路径推进 cursor 或覆盖 checkpoint pointer。 | `checkpoint_integrity_corruption_demo.py`：1 corrupt、1 recovery durable fallback、31 coalesced waiter/verified hit、pointer revision=1、cursor=0 | 真实 durable/KV/RDMA 拓扑、I/O 与带宽仍需隔离环境压测。 |
| 2026-08-15 | 本地 Compose 仅包含合成 metrics demo、Prometheus、Grafana 和显式 profile 的 offline CI；所有端口绑定 loopback，服务 internal network/read-only/cap_drop ALL/no-new-privileges，禁止 privileged/device/host network。 | 本地演练安全 | Compose 用于可视化与合同回归，而非生产监控或真实故障注入。 | `validate_local_compose.py` 通过；Docker CLI 不在当前 sandbox，未执行容器 build/run | 目标本机/隔离 runner 应先执行 `docker compose config`，再通过已批准镜像 digest 与组织安全策略运行。 |
| 2026-08-15 | 用 `Condition` 保护 per-key immutable shard flight、verified cache 和 stats；用独立 admission `Condition` 保护跨 key durable budget；leader 在临界区外 I/O，`finally` 归还 token；flight 含 deadline/generation，过期 leader 被 fence，不能晚到回填。 | 多线程 cache 防雪崩 | 同 key 需要 single-flight，跨 key 需要共享 budget，且 stale leader 必须不能覆盖后继 generation。 | 32 同 key：1 leader/31 follower/1 durable load；2 异 key：limit=1 下 max durable inflight=1 | 仅单进程参考；跨 pod/node 须使用线性一致 lease/CAS/fencing token。 |
| 2026-08-15 | Compose、PR CI、Kubernetes isolated Job 共用 `scripts/run_offline_chaos_contract.sh`；Kubernetes CI 只验证/渲染 immutable digest manifest 并上传 artifact，不含 kubeconfig/kubectl/helm/secret；实际 apply 委托受保护 GitOps/部署身份。 | CI/CD 与 Kubernetes 演练隔离 | 防止本地离线合同被悄然改写，防止 PR runner 取得 cluster 权限。 | shared script、K8s manifest、renderer 和 artifact-only workflow 静态/本地联合验证通过 | 真实隔离 Job 和真实故障演练仍需独立集群、admission policy、GitOps 和人工审批验收。 |
| 2026-08-15 | 生产监控采用三层信号：正确性阻断、容量尾延迟、流程/人工响应；新增 RecoveryDeploymentEpoch、Git revision match、inputs valid、fence rejection、GitOps sync/pending 指标，并以低基数标签导出。 | 生产可观测性 | 防止 cache/NCCL/恢复性能退化与 checkpoint/cursor/Git 并发正确性问题被混为一谈；告警只 freeze/取证/升级。 | exporter smoke 通过；Prometheus 18 alerts/10 recording rules；Grafana 19 panels 配置验证通过 | 0.85/5s/900s 是初始保护栏，必须按 cluster/storage/checkpoint baseline 和 SLO 重新校准。 |
| 2026-08-15 | Rendezvous 与 GitOps 共享 `RecoveryDeploymentEpoch`：每个 transition 需要 expected generation、Lease liveness 和 checkpoint/cursor/precision/topology/plan/Git canonical input digest；revision drift freeze 当前 epoch，必须 new generation。 | 控制面并发一致性 | Lease 只选 leader，不能单独原子绑定训练账本和 Git desired state；generation fencing 阻止晚到 worker/GitOps writer 覆盖新恢复。 | `recovery_deployment_arbiter.py`：Git drift 后 generation=2；旧 recovery/GitOps 各被 stale_generation fence 一次 | 生产须用线性一致 CAS/事务与 admission/pre-sync gate 持久化该记录；reference 模块不连接 Kubernetes/Git/存储。 |
| 2026-08-15 | 接管决策：交付物从 `E:\杨立昆为何批评大语言模型为"死胡同"？.zip` 物化为工作副本 `F:\deepseek\acvjepa-project\`；修复 `:`/`/` 非法文件名；还原被 zip 压平的原始目录结构（monitoring/、docker/、k8s/chaos-lab/、.github/workflows/、scripts/）；新增 README/STATUS/BACKLOG/VERIFY_RESULTS 与 verify_all.ps1 验证基线。 | 项目接管 | 原交付仅在 E: zip（唯一副本），工作区无活目录；validate_* 与 compose 按原始布局断言路径。 | 148 文件物化、import 全通、38/40 验证 PASS | 真实集群/NCCL/RDMA 与生产部署仍按 BACKLOG 分级待办。 |
| 2026-08-15 | 接管修复（跨平台兼容，逻辑未变）：`file://` URI 解析在 Windows 下用 urlparse（3 文件）；chaos framework 场景补 ledger.close() 且 TemporaryDirectory 加 ignore_cleanup_errors；npz 句柄 close；hitl 演示改用临时文件幂等；prometheus v3.5.0 flag 改 `--no-web.enable-lifecycle`。 | 兼容性修复 | 原交付在 Linux 验证；Windows 下 `file:///F:/...` 剥前缀得 `/F:/...` 非法路径、SQLite/npz 文件锁、prometheus 参数解析差异导致验证失败。 | 修复后单元测试 21/21、冒烟 21/22、配置校验 6/6、compose 3 容器 Healthy | 修复以最小侵入为限，未改业务逻辑；Linux 行为不变。 |
| 2026-08-15 | 本机验证环境约束：pytest 全局 deepeval 插件损坏 → 统一用 `python -m unittest`（与 run_offline_chaos_contract.sh 一致）；torch 2.5.1 Windows 无 libuv → torchrun 需 `USE_LIBUV=0`；CUDA 冒烟不稳 → `CUDA_VISIBLE_DEVICES=-1` 强制 CPU（空串无效）；torchrun 双进程因内存不足（WinError 1455）不可行。 | 验证方法 | 本机 15.4GB 物理内存仅余 2.2GB、页文件 49GB 用 22.5GB、多并行任务占用；决策记录原验证在 Linux/CPU 环境完成。 | mixed_precision 偶发 0xC0000005 单独复跑 PASS；ddp 4 项 blocked(env) 入 BACKLOG A10 | Gloo 双进程语义回归需内存充裕环境或 Linux 复跑，不改变已交付结论。 |
| 2026-08-15 | **Gloo 双进程回归改用手工启动器**：新增 `scripts/manual_gloo_runner.py` 直接 spawn 两个 python 进程（RANK/WORLD_SIZE env + gloo + USE_LIBUV=0），替代 torchrun——torchrun 的 elastic agent 自身完整 import torch 触发 WinError 1455（页文件不足），手工双进程在可用 commit 预算内可跑。 | 验证方法（改进） | 同一双进程语义回归（train/topology/full_state/integration），只是绕开 elastic agent 的额外内存开销；与 Linux 交付验证语义一致。 | **ddp 4/4 PASS**：train 2 rank 1 epoch；topology 2:1 global_samples=6；full_state 118+118 条目；integration passed → 验证基线升至 **42/42 全绿** | 该 runner 仅用于本机回归；真实集群仍用 torchrun/NCCL（见 RUNBOOK B1/B7）。 |
| 2026-08-15 | 容器内契约脚本适配：chaos-ci 镜像按 .dockerignore 排除 `.github/workflows/`（CI 文件不入演示镜像），`run_offline_chaos_contract.sh` 对 `validate_failpoint_ci_config`/`validate_kubernetes_chaos_ci` 在文件缺失时显式 SKIP（容器语义），仓库 checkout 场景保持严格校验。 | 容器一致性 | 原脚本在容器内必 FileNotFoundError；`.github` 排除是有意的安全设计（防止演示镜像携带 CI/密钥材料）。 | chaos-ci profile 容器内离线契约全流程通过（SKIP 2 项 CI 校验，其余全 PASS） | 容器内不验证 CI workflow 语法；该职责保留给 GitHub Actions 本身。 |
| 2026-08-15 | 追加审计完成：A4 Docker 全流程（build→up→chaos-ci→down）、A5 PPTX 一致性（12 页↔12 区块、关键事实全过）、A7 SKILL.md 保留归档、C9 AMI/JEPA 动态核验（ICML 2026 冯雁特邀演讲、AMI 拒绝 AGI 标签聚焦机器人世界模型、SBVA €30M 后续投资、中国 JEPA 具身热度；原结论维持）；B1–B9 产出 `CLUSTER_VALIDATION_RUNBOOK.md` 执行清单。 | 交付审计与推进 | 「全做」指令覆盖 BACKLOG 全部本机可推进项；B 级项无硬件无法执行，故产出可执行清单作为接力物。 | 42/42 全绿；PPTX 审计报告、C9 核验文档、RUNBOOK 均落盘 | B1–B9 需真实集群时按 RUNBOOK 执行；C1–C8 需外部系统/审批，保持 blocked。 |
| 2026-08-15 | **确立主计划 `PROJECT_PLAN.md`**：把「LLM 是否死胡同」的路线之争转化为可证伪实验命题（H1：轻量 JEPA 动作条件潜在预测在受限家居操控分布优于冻结视觉/反应式基线；H0 负结果同样有效），按 M0–M5 里程碑推进：M0 接管基线（已完成）→ M1 实验基座（官方 V-JEPA 权重适配器+训练器扩展+冒烟+数据清单）→ M2 域适配与动作条件后训练 → M3 MPC 闭环评测 → M4 判定报告 → M5 可选实机。 | 项目方向 | 路线之争的裁决需要实验证据而非叙事；工程基座 42/42 已具备承载实验的条件。 | 计划经用户批准落盘 | M1 本机可做；M2/M3 依赖 ≥24GB GPU 与 B/C 层数据；权重下载为可选前置。 |
| 2026-08-15 | **M1 实验基座完成**：新增 `vjepa_backbone.py`（官方 V-JEPA 2 ViT-B/16 权重适配器：键重映射支持 `encoder.`/`backbone.` 前缀与 qkv 合并/拆分布局；frozen/last_k/lora/finetune 四种微调模式；LoRA 低秩适配；安装时同步替换 student 与 EMA target 的 frame_encoder 保持参数契约）+ `vjepa_backbone_smoke.py`（forward 契约/模式/键映射/训练步四类检查）+ `train_ac_vjepa_ddp.py --init-from vjepa2:<path>[:mode]` 扩展 + `DATA_MANIFEST.md` 四层数据清单。 | M1 门禁 | 轻量方案起点是复用官方 V-JEPA 2.1 80M 权重，而原工程只有自研 ActionConditionedVJEPA；接口缺口需补齐才能进入 P1 域适配。 | `vjepa_backbone_smoke.py` 全 PASS（150 键 strict 重映射、LoRA 50 参数、安装后训练步+EMA 交换）；Gloo 双进程回归不破坏 | 官方权重下载（数百 MB/许可）与 B/C 层数据是 M2 前置；冒烟路径为同构随机初始化。 |
| 2026-08-15 | **A 层权重获取与真实加载验证**：下载 HuggingFace `davevanveen/vjepa2.1-vitb-fpc64-384`（Meta V-JEPA 2.1 ViT-B/16 80M 的 HF 转换版，438.9 MB，SHA `77D2D116...C19D6035`）至 `weights/`（不入库）。实际结构为**视频编码器**（时空 patch embed、分离 q/k/v、mod embeds、蒸馏键），与原生 ViT-B/16 同构假设不同 → 新增 `HFVJEPA2Backbone`（transformers VJEPA2Model 包装，逐帧 384px 输入保持 `[N,C,H,W]→[N,latent]` 契约；frozen/finetune 模式；EMA target 共享冻结 HF 权重 + 独立 head 副本）。 | M1 权重前置 | 官方原生 `.pt` 未公开于该 HF 仓库；HF 转换版是当前可获取的 80M 官方权重；predictor 蒸馏头(1664)与 transformers 4.57 维度不同但 encoder 391/391 键全覆盖。 | `vjepa_backbone_smoke.py --safetensors` PASS：391/395 键加载、`[1,3,384,384]→[1,64]` 契约、真实安装+EMA 训练步 | 训练入口接入见下一条（已在本轮完成）。 |
| 2026-08-15 | **真实 HF 权重接入训练入口并验证**：`train_ac_vjepa_ddp.py --init-from vjepa2hf:<path>[:frozen|finetune]` 走 `install_hf_vjepa2_encoder`；spec 解析改为「前缀剥离 + 尾部已知 mode 后缀」以兼容 Windows 盘符冒号；`make_demo_ddp_data.py` 增加 `--img-size`/`--root`。**修复**：`HFVJEPA2Backbone.forward` 曾带 `@torch.no_grad()` 导致 trainable 投影 head 梯度无法回流（DDP 报「参数未参与 loss」）→ 移除（冻结 HF 参数本身 `requires_grad=False`）。 | M1 训练入口 | 单进程可跑但双进程 DDP 报 unused params；DETAIL 定位为 head 无梯度，根因是 no_grad 装饰。 | 单进程 4 步 loss 正常（0.72→0.80 收敛方向）；**Gloo 双进程 rank0/1 均 exit=0，`last.pt`（434 MB）落盘**；`make_demo_ddp_data.py --img-size 384` 生成 384px 演示数据 | 384px 冻结 ViT-B CPU 前向慢（≈1 步/分钟级）；真实 M2 训练仍需 ≥24GB GPU；双进程冒烟可复跑（命令见 DATA_MANIFEST）。 |
| 2026-08-15 | **HF 训练冒烟固化进验证基线（verify_all.ps1 H 组）**：权重存在时执行——`hf.make_demo_data_384`（384px 演示数据）→ `hf.train_vjepa2hf_frozen_1proc`（单进程 1 epoch，`--init-from vjepa2hf:<ckpt>:frozen`）→ `hf.checkpoint_saved`（校验 last.pt 落盘后清理）；权重缺失时 SKIP 不阻塞。 | 验证基线扩展 | HF 真实权重路径是 M1 核心交付，应纳入可重跑回归，防止后续改动破坏训练入口。 | **44/44 全绿**：H 组 3 项 PASS（demo 数据、训练 exit=0、last.pt 414 MB 落盘） | 单进程 CPU 384px 训练约 2–3 分钟；仍为接口级冒烟，非性能基线。 |
| 2026-08-15 | **M2 数据装配工具链完成**：① `robocasa_adapter.py`——RoboCasa 采集适配器（实现 `SimulatorAdapter` 契约，`--simulator robocasa` 接口；robocasa 延迟导入，未装时给出可执行指引；portable synthetic 契约冒烟）；② `video_to_windows.py`——B 层无标签视频切域适配窗口（零动作/事件 + `domain_adaptation_only` provenance，可被 `WindowEpisodeDataset` 直读）；③ `assemble_m2_dataset.py`——端到端装配（C 层 episode→commit→windows、B 层窗口、split 隔离校验按 clip/job、DATA_MANIFEST 登记）；④ `scripts/make_synthetic_clips.py` 合成冒烟帧；`sim2real_pointcloud_video_pipeline.py` 增 `--simulator` 选择。 | M2 数据前置 | M2 域适配需要 B/C 层数据装配；本机无 RoboCasa/实拍视频，故以 portable synthetic 契约验证全链，真实采集接口就绪。 | **48/48 全绿**：I 组 4 项 PASS；C 层窗口被训练器直读、B 层窗口零动作可加载；split 隔离校验逻辑验证 | 真实 RoboCasa 装配（需 sim 环境 + 任务 id 钉版）与 B 层实拍视频采集仍是 M2 训练前置；synthetic 产物禁止训练。 |
| 2026-08-15 | **P1 域适配训练脚本完成**：新增 `train_p1_domain_adapt.py`——复用 `train_ac_vjepa_ddp.py` 的 DDP 初始化/EMA/checkpoint/loader 工具与 `init_from_vjepa2`（vjepa2hf 路径），损失显式排除动作与事件（`event_targets=None`、`event_weight=0.0`，日志 `event_term: 0.0`），仅 latent_nll + cosine + calibration；argparse 补 `--max-horizon`。 | M2 P1 门禁 | P1 无动作域适配（轻量方案 §5.2）需要在 B 层无标签窗口上微调预测器，与动作条件训练分离。 | **52/52 全绿**：J 组 4 项 PASS（合成帧→384px 切窗→单进程 1 epoch 训练→p1-last.pt 434 MB 落盘）；26 步 loss 正常演变 | 真实域适配仍需 ≥24GB GPU + 真实 B 层数据；synthetic 冒烟仅验证训练链路。 |
| 2026-08-15 | **二轮复审修正 6GB 可行性判断**：实测冻结 V-JEPA 骨干 CUDA 训练模式峰值仅 473MB（forward+backward+优化器步；冻结层无梯度存储、EMA 共享冻结权重），6GB 显存可跑冻结骨干微型训练——原「仅接口级冒烟」判断过保守。据此：文档一致性修复（STATUS 旧数字、BACKLOG 补 A11–A17、PROJECT_PLAN 里程碑表更新）、推进本机 CUDA 微型 P1/P2 训练、M3 评测设计文档、C9 跟踪机制。 | 可行性修正 | 本机 RTX 3060 Laptop 6GB + 官方 80M 权重已就绪；冻结训练不存储骨干梯度，显存开销接近前向峰值。 | HF 模型 CUDA forward/train 实测 473MB；`ac_vjepa_core` 自研小模型曾报 CUDA unknown error（未诊断） | 合成数据训练仅验证链路（无域适配价值）；真实 M2/M3 仍需真实 B/C 数据 + RoboCasa；CUDA 训练若遇同源错误按诊断预案处理。 |
| 2026-08-15 | **本机 CUDA 训练路径打通并修复两个真实 bug**：① `setup_distributed` 在 Windows wheel 无 NCCL（USE_NCCL=OFF）时选 nccl 报错 → 改为 `dist.is_nccl_available()` 检查 + Gloo 回退；② backbone 安装（`init_from_vjepa2`）发生在 `module.to(device)` 之后，新装的 HF 编码器仍在 CPU → CUDA 输入撞 CPU 权重 → 两个训练脚本在 init 后补 `module.to(device)`。新增 `scripts/p1_cuda_smoke.py`、`scripts/p2_cuda_smoke.py` 并固化 verify K/L 组（CUDA/权重缺失时 SKIP）。 | CUDA 路径修复 | CPU 冒烟从未暴露 device 不一致；Windows torch wheel 无 NCCL 是环境事实，单卡 Gloo/无 dist 均可运行。 | **59/59 全绿**：K 组 P1 冻结 GPU 训练 26 步（峰值 510.8MB）+ L 组 P2 动作条件 GPU 训练 4 步（峰值 567.2MB，loss 与 CPU 路径一致 0.7165/0.8027/0.8883/0.8015）；checkpoint 落盘 | 本机 CUDA 微型训练为链路验证；真实 P1/P2 训练仍需真实数据；AMP 路径（amp=True）尚未在 CUDA 冒烟启用，留作后续。 |
| 2026-08-15 | **M3 评测预注册 + C9 跟踪机制**：`M3_MPC_EVALUATION_DESIGN.md` v1 落盘（三基线「无世界模型/冻结特征/轻量 JEPA」同候选预算、六指标、分层配对检验 + 效应量 + 判定阈值表，执行前冻结设计）；C9 文档追加季度跟踪机制（触发条件 + 检查清单，下一轮 2026-11-15）。 | 评测纪律 | 预先登记避免事后挑指标；H1/H0 判定与统计设计解耦。 | 文档落盘并纳入 README 索引 | M3 执行需 RoboCasa + ≥24GB GPU；实施代码 `m3_mpc_eval.py` 待 M2 真实训练后。 |
| 2026-08-15 | **开源化（MIT → GitHub aidless/acvjepa-project）**：新增 LICENSE（MIT + 第三方声明：权重不随仓库分发、Manus 评估经授权）、CONTRIBUTING.md、SECURITY.md；README 重写为开源版（安装/权重下载/安全边界/致谢）；路径清洗（`verify_all.ps1` 用 `$PSScriptRoot`、`make_demo_ddp_data.py` 默认 root 改项目内相对、`assemble_m2_dataset.py` `--repo` 默认 `__file__` 定位）；移除无关归档 `_extras/SKILL.md`；内部管理记录全部保留（用户确认）。 | 开源 | 用户确认有权开源全部内容、选 MIT、直接推 GitHub、保留接管记录；本机 SSH 已认证 aidless。 | 推送前全量回归通过；远程仓库由用户建空仓后推送 | 权重 438.9MB 不入库（.gitignore），用户需按 README 下载并遵守 Meta 条款。 |
| 2026-08-15 | **猜想假设集落盘 `HYPOTHESES.md`**：从路线之争与工程体系中提炼五层 11 条细分假设（H-R1/R2 路线、H-T1–T5 技术机制、H-D1 数据、H-P1/P2 生态、H-M1 元层），每条含表述/依据/可证伪预测/验证路径/状态；明确与主判定 H1/H0 的关系（细分而非重复）；规定预注册流程（决策记录登记→执行→正负结果都写回）。 | 研究选题 | 开源后项目需明确的接力研究方向；避免与 V-JEPA 2-AC/JEPA-WMs 已做工作重复（依据 jepa-agent-review 生态清单）。 | 11 条 proposed；其中 H-T2/H-T4 本机 6GB 可消融，H-T1/T3/D1 挂 M3，H-R2/P1/P2/M1 由 C9 与仓库观察承载 | 验证任何一条前须按预注册流程登记指标口径，不事后修改。 |
| 2026-08-15 | **宏大蓝图落盘 `BLUEPRINT.md`**：把对话中已推演的分支树固化为十年推演总纲——§1 判定网络（H1/H0 × 11 条假说的归因组合）、§2 分支树（M3 探针 A1/A2/A3、B1–B3、C1–C3；终局 X/Y/Z；交叉矩阵；全支线证据契约）、§3 时间信号（2026Q4–2036，随 C9 季度修订）、§4 决策门 G1–G5（前置信号 + 登记地点）、§5 风险与反推演、§6 生态位（分布式接力 H-M1）。 | 研究推演 | 用户要求「给出一份宏大的蓝图」且此前明确「不跑任何实验，只给出猜想」；蓝图是 HYPOTHESES.md 的情景化上层，不改动任何指标口径。 | BLUEPRINT.md 落盘并纳入 README 索引；HYPOTHESES.md 头部加交叉引用 | 蓝图每季度随 C9 核验修订（下一轮 2026-11-15）；任何分支判断落地实验前必须走预注册流程。 |
| 2026-08-15 | **蓝图审阅修订（BLUEPRINT.md v2）**：按审阅报告修复 A1–A6/B1–B7 并纳入 C 级建议——H-T4 归位为「简化收益」（C2 只由 H-T2 定义）；B×C 互斥修正（B3×H-R1→Y，注明 C3 下 H-R1 不可测）；H-M1 观察窗改 2027-02（开源 2026-08-15 + 6 个月）；H-T2/H-T4 标注「本机 6GB 可消融」（非 GPU 接力项）；M3 §6 四种判定模式显式映射（新增 B4「表征已够」）；交叉矩阵拆「终局倾向/项目行动」两栏；Z 补第二条可达路径；命名冲突说明（蓝图 A/B/C vs M3 基线 A/B/C）；H-D1/H-P2 显式声明不并入分支树；D 层时间线张力（H-T1 需 D 层 vs PROJECT_PLAN D 层 P4）留待 G1 前裁定；C9 清单固化「三梯队项目终止率」检查项。 | 研究推演（修订） | 用户批准「修 A 级 + B 级（含 C 级建议）」；纯文档变更，不跑任何实验、不改动指标口径。 | BLUEPRINT.md v2 落盘并推送；C9 检查清单扩展 | 下一轮修订随 C9 2026-11-15；D 层采集时点待 G1 前登记。 |
| 2026-08-15 | **M3 评测设计补全（v1.1）+ HYPOTHESES 注记**：按蓝图 v2 接口审阅（M-1/M-2/M-3/M-4/M-5、HYP-1）——新增 §4.1 校准分层分析（冻结 ECE 口径 + 三分位成功率 + H-T3 可证伪预测，裁决 A2 分支）、§4.2 失败归因三类（接口/单模型/执行，H-R1 可证伪预测，裁决 B3 分支）、§7.1 C 组模型卡与结果表模板（可复现性）、§2 候选生成器种子冻结；§6 判定表加蓝图分支映射指针（①→A、②→B4、③→C、④→C3）；HYPOTHESES H-T1 加 D 层张力注记（指向 BLUEPRINT §2.1）。 | 评测预注册（执行前补全） | 蓝图 v2 把 M3 定义为第一根探针，但 v1 无 ECE 分层协议与失败归因类别，无法裁决 A2/B3 分支；用户批准「修 M-1/M-2/M-3 + M-4/M-5 + HYP-1」。补全属执行前设计完善，符合 v1 §5 预注册纪律（非事后增删指标）。 | M3 v1.1 落盘；HYPOTHESES H-T1 注记；README 索引更新 | ECE 口径与归因类别在执行前冻结；D 层时点裁定仍待 G1 前登记（BACKLOG A18）。 |
| 2026-08-15 | **H-T2 预注册登记（A19）**：投影头容量消融——指标口径=验证集一步潜在预测误差（latent NLL，与训练 latent_nll 同口径，辅助 cosine）；数据分档=head 宽度 32/64（基线）/128/256，固定 frozen 官方骨干、horizon、EMA、数据分档版本、等训练预算；统计检验=相邻宽度边际收益比 Δ(128→256)/Δ(32→64)<0.2 且 Cohen's d<0.2 判饱和（饱和点 <768 即支持），预期 32→64 d>0.8、64→128 d 0.3–0.8、128→256 d<0.2；样本量=每宽度 ≥3 seeds，报告均值±95%CI，以边际收益比为主判据（小样本不以 p 值背书）。 | 预注册（H-T2） | 按 HYPOTHESES 预注册流程登记指标口径，执行前不得修改；本机 6GB 可消融（`train_p1_domain_adapt.py --latent-dim` 分档，成本极低）。 | HYPOTHESES H-T2 状态 → preregistered；登记落盘 | 执行待用户解除「不跑实验」约束；结果无论正负写回 H-T2（tested/supported/refuted）。 |
| 2026-08-15 | **H-T4 预注册登记（A19）**：EMA vs 同步目标 A/B——指标口径=一步潜在预测误差（同 H-T2 口径）；A/B 配置=A=EMA 目标（τ=0.999，现默认）、B=同步目标（head 参数直接复制、无平滑），固定 head 64、frozen 骨干、horizon、数据分档、等预算；统计检验=差异百分比 |Δ|=|mean_A−mean_B|/mean_B，判据 |Δ|<5% 且 95%CI 不含实际意义差异 → 支持 H-T4；样本量=每支 ≥3 seeds。 | 预注册（H-T4） | 按 HYPOTHESES 预注册流程登记；本机 6GB 可消融（`train_p1_domain_adapt.py` 加 `--ema-target` 开关）。 | HYPOTHESES H-T4 状态 → preregistered；登记落盘 | 执行待解除约束；结果写回 H-T4。 |
| 2026-08-15 | **A18 裁定：D 层采集时点**——D 层拆两档：**D-mini**（≤2h 真实轨迹）随 P2 动作条件后训练（与轻量方案 §5.2 P2「小量真实轨迹」一致；硬件不可用时先以 C 层仿真运行，不阻塞 M3）；**D-full**（5–10h，H-T1 等值线分档）启动条件 = G1 裁决 ∈ {A, B1, B2}，G1 ∈ {C, B4} 时降级为可选（负结果路径不投入稀缺真实采集）；H-T1 分档口径不变、执行时点后移；M3 在 RoboCasa sim 内评测，不依赖 D 层。 | 决策（数据时间线） | 化解三方张力（PROJECT_PLAN「D 层=P4」/ H-T1 M2 分档需 D / 轻量方案 P2 小量真实轨迹）；D 层是昂贵稀缺资源，按 H-D1 数据价值判断应在 A/B 分支确认价值后投入。 | 决策记录 + PROJECT_PLAN L4/M2 行 + HYPOTHESES H-T1 注记更新；BACKLOG A18 done | D-full 具体预算（2h/5h/10h）与采集协议（时间同步/动作坐标系/许可）于 G1 前登记。 |
| 2026-08-16 | **H-T2/H-T4 消融结果（双 refuted，合成数据链路级）**：AutoDL 3090 48GB 全 18 runs 完成（batch 2、30 epochs × 800 窗口、每 run ≈41 分钟；runner 两次修复后无失败）。**H-T2**：均值 latent_nll（越低越好）w32=**-3.505** / w64=-3.300 / w128=-2.719 / w256=-1.732；预注册饱和判据不成立（边际收益比 **4.80** ≫ 0.2，d(128→256)=**9.9**）；**方向反转发现**——宽度越大误差越高（32 最优），冻结配置在 32–64 非容量瓶颈（「投影头瓶颈」表述在合成数据上不成立）。**H-T4**：ema=**-3.300**（CI[-3.392,-3.207]）vs sync=**0.204**（CI[0.132,0.277]），|Δ|=**1715%** ≫ 5%——同步目标显著劣化，**EMA 收益巨大**（「去 EMA」被排除，G2 选项剔除）。可复现性：同 seed 同配置跨 run 逐位一致（ht4_ema 与 ht2_w64 相同）。**局限**：合成 demo 数据（链路级证据）；NLL 跨维度可比性注意（宽头维度更多，观测到的反向趋势更强）；真实 B 层数据按同口径复验待办。**影响**：G2 输入——保留 EMA、latent_dim 128→64 候选（待真实数据确认）。证据：`experiments/ht2_ht4_2026-08-16/`（summary.tsv + verdict.json/md + 18 logs）。 | 实验执行（消融结果） | 预注册口径（2026-08-15）冻结执行；负结果同样有效（HYPOTHESES 纪律）。 | HYPOTHESES H-T2/H-T4 → tested（refuted）；BACKLOG A19 done；证据入库 | 真实 B 层数据复验；默认 latent_dim 调整待复验后决策。 |
| 2026-08-15 | **C9 第二轮核验（提前轮，用户指示，BACKLOG A21）**：检索窗口=2026-08-15 白天核验之后——AMI：谢赛宁加入（待一手确认）、$1B 种子轮含 Nvidia/Temasek（与已记录 10.3 亿美元一致）；JEPA-WMs 权重发布至 HF；VLA-JEPA 进入 LeRobot v0.6.0（可复现策略入口）；新开源项目 INTACT-JEPA（arXiv:2607.26056，免搜索世界模型）/ LeWorldModel / worldmodels_ros2 / W2-VLA；具脑磐石 2026-05-25/26 亿元融资（与首轮一致，无新事件）；LeCun 多篇访谈重申立场（无新实证）。首读数：H-R2/H-P1 方向支持（定性）；原交付四条结论全部维持。 | C9 季度核验（提前轮） | 用户「全做」指令覆盖 A21；正式下一轮仍为 2026-11-15（含三梯队终止率计数）。 | C9 文档追加「第二轮核验」小节；BACKLOG A21 done | INTACT 免搜索规划方向与蓝图「候选生成成本」担忧相关，纳入 C9 后续跟踪。 |
| 2026-08-15 | **PROJECT_PLAN/轻量方案接口审阅修复**：成功标准「42/42」→「59/59」（陈旧数字）；前置文档补 `HYPOTHESES.md`/`BLUEPRINT.md`/`M3_MPC_EVALUATION_DESIGN.md`；L4 D 层行与 M2 真实训练行按 A18 裁定更新（D-mini/D-full）；M3 行标注 v1.1；轻量方案 §5.2 P2 行加 D-mini 指针、P3 行加 M3 v1.1 指针。 | 文档一致性 | 与蓝图 v2/M3 v1.1 接口审阅；轻量方案 P3 通过标准与 M3 §1 判定规则一致（M3 增「其余不得显著退化」为细化，非冲突）。 | PROJECT_PLAN/轻量方案更新 | 无。 |
| 2026-08-15 | **第六轮猜想落盘（BLUEPRINT.md v2.1 §7 附录）**：信号概率更新表（VLA-JEPA 进 LeRobot→H-P2 实证、JEPA-WMs 权重→2027-06 前 ≥2 复现猜想、AMI 谢赛宁→2027 前 >30% 可复现实验猜想、单 GPU→稀缺性转移）、新支线 F（免搜索世界模型，X1/X2 细分，接口错误=过渡期瓶颈）、反向猜想 B4×H-P2「表征优先」汇合、Y' 变体（分域收敛）、时间线猜想（H-P1 窗口提前至 2026Q4）。可检验声明仅两条（JEPA-WMs 复现计数 / H-T2 消融汇合），其余纯 C 级不进入决策门。 | 研究推演（C 级附录） | 用户「全部开源吧」= 将对话中唯一未入库内容固化并推送，确保全量内容公开；纯猜想、无实验执行。 | BLUEPRINT.md v2.1 落盘；仓库全量内容公开（除 Meta 许可权重与可再生的 verify_artifacts） | F 支线与 Y' 变体纳入 C9 后续跟踪（不进入决策门）；2026-11-15 正式轮首测终止率。 |
| 2026-08-15 | **项目与 AGI 关系自审并入（BLUEPRINT.md v2.2 §7.5）**：反推演前置（若通往 AGI，证据形态=能力贡献/路线裁决/认知贡献；对照现状=能力≈0）；分层结论（直接能力❌、路线裁决✅、方法论✅、个人认知✅、社会学⚠️ H-M1 先例）；自证预言消毒三条（研究 AGI 路线≠推进 AGI、定位=裁决节点非引擎、窄分布切片=路标检定员）；结论=「引擎 vs 地图/罗盘/航标」二分中属后者，不进入任何决策门。 | 研究推演（C 级附录） | 用户「并入吧」指令；纯猜想、无实验。 | BLUEPRINT.md v2.2 推送 | 无（纯 C 级）。 |
| 2026-08-15 | **AGI 总题猜想并入（BLUEPRINT.md v2.3 §7.6）**：定义三分（AGI-1 软件域/AGI-2 物理域/AGI-3 涌现，两个 AGI 不同构不先合并）；三条路径（A 工具化 AGI 2028–2031 / B AGI-2 三瓶颈=反事实数据 H-T1/D1×校准 H-T3×接口 H-R1 / C 免搜索规划=映射而非搜索）；时间线 2026–2040（2035–2040 表征层会合）；反推演六条否定形态；结论=两条曲线经表征层会合合并，三瓶颈=本项目三个预注册切片。 | 研究推演（C 级附录） | 用户「并入吧，继续猜想」指令；纯猜想、无实验。 | BLUEPRINT.md v2.3 推送 | 检验锚点=C9 2026-11-15、H-T2/H-T4 消融、M3 裁决。 |
| 2026-08-15 | **AGI 机制层深推并入（BLUEPRINT.md v2.4 §7.7）**：会合机制=「同构涌现」（对象=流形/动作=转移算子/语言=命名检索层，token 即对象）；AGI-2 临界形态=结构化场景 + 无人监管长任务链；反事实密度量化 10⁴–10⁵ 决策点×分支（轨迹≠反事实，采集差距>算力差距）；校准阈值量化 ECE<0.05+AUROC>0.9（哲学极限→受监督自主）；W 路线三候选（能量效率/环境即模型/连接组先验，权重 10–20%）；终极含义=接口错误占比曲线=AGI-2 距离度量。每条配否定信号。 | 研究推演（C 级附录） | 用户「并入吧，继续猜想」指令；纯猜想、无实验。 | BLUEPRINT.md v2.4 推送 | 反事实型数据集、长任务链演示、能量效率曲线纳入 C9 后续跟踪。 |
| 2026-08-15 | **AGI-2 可达性地图与概率账本并入（BLUEPRINT.md v2.5 §7.8）**：双轴地图（反事实密度×校准水平；四象限=演示区/陷阱区/可信但窄/AGI-2 区；路径=先校准再扩密度的阶梯上升；大厂堆数据不校准 2030 前爆雷猜想）；终局概率账本（Y 25%/X 20%/Z 20%/Y' 15%/W 10%/未建模 10%，更新纪律=C9 轮 ±5–10pp、消融与 M3 为大更新）；人的角色=裁决与校准不可替代；元层猜想=AGI-1 接管裁决前 M3 是「人类手写裁决末班车」。 | 研究推演（C 级附录） | 用户「并入吧，继续猜想实现路径」指令；纯猜想、无实验。 | BLUEPRINT.md v2.5 推送 | 账本 2026-11-15 C9 首调；陷阱区爆雷信号纳入 C9 跟踪。 |
| 2026-08-15 | **实现路径猜想并入（BLUEPRINT.md v2.6 §7.9）**：本项目三档时间线（乐观 2027H1 / 现实 2027H2–2028 / 悲观阻塞；瓶颈=约束解除/数据算力/接力三外部变量；交接面最薄原则）；卡点破局（H-T2/H-T4 消融=敲门砖、C 层优先、预注册包=可审计资产外联）；AGI-2 产业三公共品化节点（反事实数据集 2027–2029 / 校准工具链 2028–2030 / 长任务链基准 2029–2031）；最优个人策略四步；每段通过标准（失败即转）。 | 研究推演（C 级附录） | 用户「并入并且开源吧」指令；纯猜想、无实验。 | BLUEPRINT.md v2.6 推送 | 公共品化节点纳入 C9 跟踪。 |
| 2026-08-15 | **C9 第三轮核验（提前轮，用户指示「看看现在别人对于这个研究是什么情况」）**：领域成熟化——WAM 术语收敛（多篇综述 2606.20781/2605.12090 等）、统一 VLA 评测框架公共品化（allenai/vla-evaluation-harness）、JEPA-WAM v2（2608.10780v2）、VLA-JEPA 结果传播（MoTo 74.0/60.4/43.1）、单 GPU VLA 微调研究（2606.13856）；AMI 首次公开研究信号（世界模型脆弱性，2026-06，待一手核验）；阿里 WorldRoamBench 长时程基准。影响：H-P1 开源侧强化、AMI 半句待细分；H-R2 强化；§7.9 猜想③雏形出现；概率账本 Y+X 方向支持。 | C9 季度核验（提前轮） | 用户指令；正式下一轮仍 2026-11-15。 | C9 文档追加「第三轮核验」小节；BACKLOG A21 备注更新 | AMI 脆弱性研究、vla-evaluation-harness 对接 M3 的可能性待核验。 |
| 2026-08-15 | **WAM 生态学习批次落地（学习报告 + A22–A25）**：3 组子代理并行分析 12 项外部研究（VLA-JEPA/INTACT/LeWorldModel/W2-VLA/JEPA-WAM 10780 双论文区分/vla-evaluation-harness/LeRobot v0.6.0/WorldRoamBench/worldmodels_ros2/WAM 综述双引擎/2605.00080/Seed Lottery），产出 15 条精华模式；落地=学习报告入 README 索引、M3 §7.2 生态参照储备（**不改冻结口径**）、HYPOTHESES 术语注记（latent-only WAM）+ H-P1 注记（AMI 公开研究信号待核验）、BACKLOG A22–A25（vla-eval 对接评估/设计借鉴储备/生态清单核对/多种子稳定性核验已固化）、C9 检查清单加多种子稳定性项；ESSENCE_LEDGER E108–E120 landed。 | 外部学习落地 | 用户「学习一下他们的」+ corpus-learning 五步管线；只读分析（web/README/arXiv），未克隆、零实验。 | 学习报告落盘并推送；M3/HYPOTHESES/BACKLOG/C9 更新 | vla-eval 对接结论、核对清单闭环待后续轮；账本条目待月度审计。 |
| 2026-08-16 | **解除「不跑实验」约束 + H-T2/H-T4 消融云端启动**：用户「同意开始做吧」。云端 AutoDL 3090 48GB（`connect.westd.seetacloud.com:33909`）已部署 AC-VJEPA 全栈并通过短训验证（800 窗口×4 epoch 全 PASS，`last.pt` 产出；证据=云端 `out_800/out_short`，已 SSH 实测）。启动预注册消融：H-T2=宽度 32/64/128/256×3 seeds、H-T4=ema/sync×3 seeds（固定 head 64）；数据=`demo_ddp_data_800`（合成链路级，真实 B 层数据后补——结果将标注合成证据）。**口径校正**：EMA τ 按代码默认 **0.996** 执行（预注册文本「0.999」为笔误；A/B 对比本质不变）。实施：`train_p1_domain_adapt.py` 新增 `--ema-target {ema|sync}`（sync=EMA momentum 0.0 硬拷贝，公式见 `ac_vjepa_core.EMAStateEncoder`）；新增 `scripts/run_ht2_ht4_ablation.sh`（H-T2 12 runs + H-T4 6 runs，汇总 summary.tsv）。 | 实验执行（预注册消融） | 用户批准；H-T2/H-T4 口径 2026-08-15 已登记；云端 48GB 满足 ≥24GB 门槛。 | 开关+脚本落盘并推送；云端启动（nohup）；结果回写 HYPOTHESES | 合成数据证据局限；真实 B 层数据就绪后按同口径补跑。 |
| 2026-08-16 | **A22 结论：vla-evaluation-harness 对接评估**——对接可行：M3 评测包可注册为其「模型服务器（三基线排序器）+ RoboCasa 基准客户端」集成（其 RoboCasa 已集成、待首复现，21.4GB 镜像）；统计层边界：harness 保证「跑得起来、可复现、逐 roll artifact」，但 **ECE 分层 / Wilcoxon+Holm / 失败归因 / M3 六指标统计仍须自建**（「分层在 harness 之上」）；决策：M3 执行时若 vla-eval 可用即以之为评测底座（评测公共品化 + H-M1 主动外联），否则自建；入榜须自守口径（harness 汇总他人自报分数、无防挑指标机制）。 | 评测对接（A22） | WAM 学习批次；vla-eval 是「评测公共品」现实范本。 | M3 §7.2/§7.3 已含；BACKLOG A22 done | 待 M3 执行时点实测 RoboCasa 镜像。 |
| 2026-08-16 | **A23 采纳：M3 v1.2 预注册扩展**——§7.2 储备中 4 项转正为协议：①ECE 三分位前/中/后段时间轴分解（§4.1 扩展，WorldRoamBench 度量，强化 H-T3 中途信心崩塌检验）；②结果表加「外部复现判定」列（v1.2 四档：Reproduced/Above/Validated-no-ref/Upstream defect）；③奖励模型成功判定器门槛（默认不启用，过 judge 校准门 FNR<0.15/FPR<0.10 方可启用）；④搜索预算诊断性对照（INTACT 四接口说明，不进 §6 主判定表）。 | 评测预注册（A23 采纳） | 「不改冻结口径」原则：四项均为执行前新增协议，不动主三基线判定与 §6 阈值。 | M3 v1.2 落盘；BACKLOG A23 done | ECE 时间轴分解口径在 M3 执行前细化冻结。 |
| 2026-08-16 | **A24 生态清单补录（部分）**：`jepa_agent_project_findings.md` 补 INTACT-JEPA（A 级免搜索）/ LeWorldModel（B 级）/ W2-VLA（A 级研究原型）/ worldmodels_ros2（B 级集成层）+ **JEPA-WAM 双论文区分注记**（2608.09381 vs 2608.10780v2）；**待核验项因 web 检索预算耗尽（Insufficient Balance）标记待补**：VLA-JEPA ECCV/LICENSE、W2-VLA LICENSE、JEPA-WAM 零样本声称全文、vla-eval 榜单口径版本。 | 生态清单（A24） | WAM 学习批次数据（已落盘）足以补录；实时核验预算受限。 | 生态清单更新；BACKLOG A24 部分完成（核验项待预算恢复） | 预算恢复后闭环 4 项待核验。 |
| 2026-08-16 | **G2 决策：`latent_dim` 默认值**——**保持 128 暂不更改**；64 候选登记（H-T2 合成证据：w32/w64 优于 w128/w256，但方向反转仅见于合成数据、且 NLL 跨维度可比性有保留）。判据：改默认影响真实 M2/M3 全部后续训练，合成数据不足以定案；**真实 B 层数据同口径复验后按结果定案**（若复验仍支持窄头 → 默认降至 64 并相应更新模型卡/M3 配置）。 | 决策（G2） | H-T2 refuted 的方向反转发现；预注册纪律（口径冻结，配置可调整但需登记）。 | 决策记录；BACKLOG A19 备注 | 真实数据复验触发重新评估。 |
| 2026-08-16 | **概率账本提前首调（记录，不调权重）**：累计信号——C9 R2/R3（VLA-JEPA 进 LeRobot、vla-eval 公共品化、JEPA-WAM v2、WAM 术语化）、消融双 refuted（H-T4 简化叙事排除、H-T2 头非瓶颈利好轻量）、Seed Lottery（多种子稳定性）。**结论：维持初始权重**（Y 25%/X 20%/Z 20%/Y' 15%/W 10%/未建模 10%）——合成数据与定性信号不足以调权；2026-11-15 正式轮按账本纪律首次实质调权（届时含三梯队终止率计数）。 | 概率账本（提前首调） | §7.8 账本纪律（C9 轮 ±5–10pp、大更新=消融/M3）；本轮为记录性首调。 | 决策记录 + BLUEPRINT §7.8 注记 | 正式轮实质调权。 |
| 2026-08-16 | **真实数据复验立项（BACKLOG A26，blocked）**：H-T2/H-T4 的合成数据证据需在真实 B 层数据（无标签视频窗口）上按**同口径**复验（同 `scripts/run_ht2_ht4_ablation.sh` + `analyze_ht2_ht4.py`）。前置=真实 B 层数据采集/许可（外部依赖，暂无）。复验结果将决定 G2 定案（latent_dim 默认）与 H-T2/H-T4 状态的合成限定是否解除。 | 实验立项 | 合成数据局限显式化；预注册纪律要求同口径复验。 | BACKLOG A26（blocked） | B 层数据就绪后执行。 |
| 2026-08-16 | **M3 评测骨架落地 `m3_mpc_eval.py`（v1.2 实现，冒烟通过）+ RoboCasa 部署前置评估（A27）**：骨架=三基线排序器接口（A 随机/几何、B 冻结特征余弦、C 轻量 JEPA rollout，vla-eval 模型服务器 predict 等价物）+ Mock 环境 + MPC 控制器（候选种子冻结）+ 六指标采集 + ECE 分层（15 bins）与前/中/后时间轴分解 + 失败归因接口 + 配对 Wilcoxon+Holm + Cohen's h / Cliff's delta（bootstrap 95%CI）+ §7.1 结果表模板（含复现判定列）。**冒烟全绿**（Mock 链路）：成功率阶梯 A 0.000 < B 0.033 < C 1.000；C 校准最佳 ECE 0.174；C vs B p=0.000、Cliff's δ=0.967（95%CI [0.90,1.00]）；§6 判定 H1-support 字符串正确。**RoboCasa 前置评估**：云端 pip 可用（另一会话已成功 pip install 重型包）、磁盘与 Isaac 依赖可装，但 **C 组模型尚不存在（待 M2 真实训练），现装 RoboCasa 无评测对象，且实例显存被另一会话占用** → 暂不部署；列入 M2 真实训练启动时的前置清单（BACKLOG A27，blocked on M2）。 | 工程实现 + 部署评估 | A22 结论落地为代码；M3 §1/§4/§5/§7 协议全部对应实现；Mock 只验证机制，不产生任何科学结论（§6 判定字符串在 Mock 上仅为链路演示，真实判定待 M3 执行）。 | `m3_mpc_eval.py` 落盘 + 冒烟通过；BACKLOG A27 增加 | 真实 M2 训练就绪后接 RoboCasa 与真实排序器。 |
| 2026-08-17 | **「全部在云端做」轮（BACKLOG A28 done）：P2 动作条件后训练工程验证 + M3 骨架 × 真实权重 chain-live 演示**。云端 AutoDL 3090 48GB：①仓库经 GitHub clone 重新同步（`.old` 保留）；②`scripts/make_structured_p2_data.py` 生成结构化合成窗口（600 × 384px，红块=agent/绿块=goal/灰块=干扰，动作=真实位移，**工程验证数据、非预注册实验**）；③`train_ac_vjepa_ddp.py --init-from vjepa2hf:<权重>:frozen` 训 P2 20 epochs（loss_total −0.03→−1.27 收敛、calibration→0.001，`last.pt` 436MB）；④`scripts/m3_live_demo.py` 把**真实 AC-JEPA 模型**接入 M3 骨架（A 随机 / B 冻结特征·一步余弦 / C 动作条件 GRU 潜变量 rollout·逐维方差加权目标距离），ImageReach 环境与训练同渲染语义。**结果（工程验证，非假设检验）**：成功率 A 0.000 / B 0.200 / C 0.067（C vs B p=0.317 不显著，δ=−0.13）；**C 校准最优 ECE 0.027（B 0.098）且时间轴 ECE 随 rollout 上升（0.017→0.030→0.093）**——呼应 H-T3 §7.3 时间轴分解设计动机；管道全链路验证通过（真实编码器 391 键 + P2 模型 + M3 统计/结果表）。**诚实读数**：演示级（15 rollout×32 候选、2D 合成环境、demo 规模 P2、评分函数粗放），C 未优于 B **不进入任何假设判定**。另完成 **A24 云端实抓核验闭环**（W2-VLA=MIT ✅、JEPA-WAM 10780v2 摘要无零样本声称 ✅、VLA-JEPA 无 LICENSE 文件 ⚠️ 待一手、vla-eval README leaderboard 存在 ✅）。证据：`experiments/p2_chain_live_2026-08-17/`（results.json + live_demo.log）。 | 工程验证（云端执行） | 用户「全部在云端上做」；预注册纪律=链路验证不进入 HYPOTHESES；A24 用云端网络绕开本地检索预算。 | BACKLOG A28 done；A24 done；证据入库；决策记录 | 真实 M2（B/C 层真实数据）+ RoboCasa 仍为 M3 前置；P2 模型接入真实评测的接口已就绪（m3_live_demo.py）。 |
| 2026-08-17 | **H-D1 预注册登记 + 云端执行立项（BACKLOG A29）**：失败轨迹单位价值假设——指标口径=ImageReach 闭环成功率（成功=dist<24px 且 ≤12 步；每模型 20 个同 env seed rollout 的**逐 rollout 成功向量**，供按 seed 配对）；数据分档=全成功集（600 结构化窗口直线趋向）vs 失败注入集（600 窗口，30% 含「安全失败+恢复」：先偏离 goal 再回拉）；训练=P2 冻结骨干 20 epochs（batch 4×4、latent 128、同 seed）；统计=按 env seed 配对的 **Wilcoxon（单侧：失败注入更优）** + Cohen's h / Cliff's delta + 95%CI；预期效应=失败注入集闭环成功率 **+15–30pp**（h≥0.5）；判定=配对 p<0.05 且 h>0.3 → 支持；样本=20 rollouts/模型（检出 ≥20pp、α=0.05）；局限=合成 2D 环境 + 合成「失败+恢复」语义，链路级证据；真实失败轨迹复验待办。实施：`make_structured_p2_data.py --failure-ratio` 扩展 + `m3_live_demo.py` 逐 rollout 成功向量输出 + 云端双训练双评测。 | 预注册（H-D1） | 按 HYPOTHESES 预注册流程登记指标口径，执行前冻结；H-D1 验证路径「M3 前用合成 C 层对比」在合成 ImageReach 上的落地（链路级）。 | HYPOTHESES H-D1 → preregistered；BACKLOG A29 立项 | 真实失败轨迹数据就绪后按同口径复验。 |
| 2026-08-17 | **运维教训（云端，H-D1 执行中的误判）**：①`num_workers>0` 时 DataLoader 会 fork 同名子进程（worker）——`ps | grep train_ac_vjepa` 会看到「1 主 + N worker」，**判定重复进程必须用 ppid 区分**，不得凭 cmdline 相同就杀；②pwsh→ssh 内联 `\$1` 的 awk 常被 PowerShell 转义破坏，导致 kill/计数静默失败（「CLEANED」/「0」可能是假象）——**远端操作一律走脚本文件**（scp 后 bash）；③本次误判导致一次不必要清理（rm -rf 输出目录），后续须对产物做完整性校验（loss 日志单调 + last.pt 可加载）再采信。 | 运维教训 | 前两次 H-T2/H-T4 与 P2 都出现过重复进程疑云，本次定位根因=worker fork；记入 OPS 纪律。 | 决策记录 | H-D1 success 模型产物需校验后采信。 |
| 2026-08-17 | **H-D1 消融结果（refuted，合成链路级，BACKLOG A29 done）**：全成功集 vs 30% 失败注入集（各 600 窗口 → P2 20epoch → ImageReach 20 rollout 同 seed 配对评测）。success 模型闭环成功率 **0.05** vs fail30 **0.05**（差 0.0pp）；配对 Wilcoxon（单侧）p=**1.0**、Cohen's h=**0.0**、Cliff's δ=**0.0** → 预注册判据（p<0.05 且 h>0.3）未满足 → **H-D1 refuted（合成链路级）**。**诚实读数**：地板效应（两模型均 5% 成功率，演示级 P2/硬环境/粗评分掩盖信号）——「弱条件下未检出效应」≠ 强否定；真实失败轨迹复验（A26 类）为更强证据路径。**执行中运维事故**：DataLoader worker 被误判为重复进程，一次不必要清理（success 模型产物经校验完整：epoch 20/740 步/823 键可加载）。证据：`experiments/hd1_2026-08-17/`（双 results + verdict）。 | 实验执行（H-D1 结果） | 预注册口径（2026-08-17 登记）冻结执行；负结果同样有效（HYPOTHESES 纪律）。 | HYPOTHESES H-D1 → tested（refuted）；BACKLOG A29 done；证据入库 | 真实失败轨迹数据就绪后按同口径复验；A26 类真实数据路径。 |
| 2026-08-17 | **A27 RoboCasa 云端部署硬阻塞确认 + vla-eval 对接骨架落地（A22→代码）**：云端探测确认两个硬约束——①acvjepa 环境为 **Python 3.12**，`pip` 无 `robocasa`/`isaacgym` 匹配发行版（IsaacGym/RoboCasa 需 ≤3.10）；②**磁盘已满 200G/200G（仅 331M 空闲，另一会话占满）**。→ RoboCasa 部署需「专用 AutoDL Isaac 镜像 + 独立 ≤3.10 环境 + 释放磁盘」，属外部环境约束（A27 保持 blocked，附硬阻塞原因）。同时把 A22 结论落成代码：新增 `scripts/m3_vla_eval_dock.py`——`M3ModelServer`（vla-eval 模型服务器 predict 契约，包装 A/B/C 三基线）+ `RoboCasaClientStub`（基准客户端四方法契约 + RoboCasa 任务/隔离映射说明）+ 冒烟通过（合成环境 A 0.2/B 0.1/C 1.0、C 校准最优 ECE 0.19）；统计层仍由 `m3_mpc_eval.py` 在 harness 之上运行（「分层在 harness 之上」）。 | 部署评估 + 工程实现 | A27 硬阻塞=环境约束（非本项目可解）；对接骨架不依赖 RoboCasa 环境，任何机器可用。 | `scripts/m3_vla_eval_dock.py` 落盘 + 冒烟；BACKLOG A27 附阻塞原因 | RoboCasa 部署待专用环境（独立镜像/磁盘）；M3 对接面已就绪。 |
| 2026-08-17 | **三负结果技术报告落盘 + 账本叙事注记**：`实验记录与负结果报告_2026-08-17.md` 汇总 H-T2/H-T4/H-D1 三预注册实验（判据/结果/局限/复现）+ 工程链路验证 + M4 前置清单 + 运维教训，入 README 索引。**账本叙事注记**（不调权重，纪律保留给 C9 正式轮）：三 refuted 的叙事含义——H-T4 剔除「去 EMA」简化选项、H-T2 给「latent_dim 64 候选」、H-D1 暴露「评测强度不足而非数据价值不存在」；三条均为合成/链路级，**不改变 §7.8 初始权重**（Y 25%/X 20%/Z 20%/Y' 15%/W 10%/未建模 10%）。 | 报告 + 账本注记 | 正负同录 + 局限显式化的方法论收获；账本纪律=权重只在 C9 正式轮调。 | 报告落盘 + README/STATUS 索引 | C9 2026-11-15 正式轮按证据实质调权。 |
| 2026-08-17 | **现状评审落盘 + P0/P2 行动执行（用户「评审一下现状」→ 批准计划）**：①`现状评审_2026-08-17.md`（完成度盘点：工程✅/裁决⚠️未启动/纪律✅/外部依赖🔒；六类问题；P0 外联+最小数据集、P1 环境、P2 文档治理）；②`H-M1_主动外联包_2026-08-17.md`（协作资产/目标对象/邀约文案/执行步骤——外联实际发送待用户确认）；③`最小真实数据集方案_2026-08-17.md`（B 层候选数据集 + 许可核对 + 最小执行路径）；④README 文档分区（核心裁决科学 vs 继承工程基建）+ 索引补全；⑤PROJECT_PLAN L2「42/42」表述修正为「当时 42/42、现 59/59」。 | 评审 + 治理 | 评审结论=工程与纪律高水准，但核心裁决目标零科学进展，差真实数据与一次主动外联。 | 三文档落盘 + README 分区 + L2 修正 + 索引 | RoboCasa 环境切换（P1-C）待方向确认；H-M1 外联实际发送待用户确认对象/措辞。 |
| 2026-08-17 | **数据获取路径打通 + 首次获取真实数据（用户「全做」）**：云端实测确认——①`hf-mirror.com` 可用（http=200），设 `HF_ENDPOINT` 即解锁 HF；②`lerobot/pusht`（MIT）/`aloha_sim`（MIT）可下载，`bridge`/`epic-kitchens` 门控（需 HF token），SSv2 官方需注册；③云端磁盘已释放至 25G（此前 331M 满）；④**实际下载 `lerobot/pusht` 成功**（206 episodes/25650 帧/96×96 av1，落盘 `/root/autodl-tmp/realdata/pusht`，7.4MB）——**项目首次获取真实机器人数据**。后续依赖：视频解码器（需 `pip install av`）、格式映射（96→384、proprio 2→8、action→20）、pusht 为 2D 推块域非厨房域（适合管线验证、不适合 H-T5 判定）。外联侧：无 gh/token，无法自主向第三方 repo 发 issue，发送待用户 GitHub 动作。 | 数据获取（里程碑） | 「全做」执行：把「等真实数据」推进为「数据获取已解锁 + 首个真实数据集在手」。 | `最小真实数据集方案_2026-08-17.md` §六/§九（已验证可得性 + 里程碑）；云端 pusht 落盘 | pusht→窗口格式转换为下一聚焦工程步骤；厨房域数据需 HF token/官方注册（用户）；外联发送待用户。 |
| 2026-08-17 | **真实数据管线冒烟通过（用户「同意」）**：装 PyAV（av1 解码通过）；新增 **`scripts/lerobot_to_windows.py`**（LeRobot v3 parquet+mp4 → AC-VJEPA 窗口：96→384 上采样、ImageNet 归一化、proprio/action pad）；pusht 前 15 episodes（2056 帧）→ **206 窗口**（2.4G）；**P1 域适配冒烟**（冻结 V-JEPA 2.1 骨干、latent_dim 64、5 epochs）loss_total 单调下降 **−0.21→−0.38**、loss_calibration 0.20→0.11、event_term=0、`p1-last.pt`（434MB）落盘、exit=0。**意义**：真实（非合成）数据首次流过 AC-VJEPA 训练管线。边界：pusht 为 2D 推块域，仅验证管线，不作 H-T5 厨房域判定。 | 工程验证（真实数据链路） | 把「真实数据能流过管线」坐实；此前消融用随机合成张量，本次首次真实机器人帧。 | `scripts/lerobot_to_windows.py` 入库；`最小真实数据集方案_2026-08-17.md` §十；云端窗口+checkpoint 落盘 | 厨房域数据待用户；后续可用 pusht 做 LeRobot 系评测替代的非合成链路验证。 |
