# 家用双臂机器人：LLM × JEPA 混合 Agent 的落地场景与任务执行示例

## 场景概览：早餐台安全整理

用户对家用双臂机器人说：

> “请把早餐桌收拾好：把牛奶放回冰箱，把盘子和勺子放进水槽，玻璃杯留在桌上，并擦干净没有食物的桌面区域。”

这不是一个简单的“识别物体—执行抓取”任务。机器人需要理解“收拾好”的隐含边界，区分牛奶、盘子、勺子、玻璃杯和食物残渣，避免把玻璃杯错放进水槽；同时处理冰箱门、桌面遮挡、人类临时靠近、杯子滑动和抓取失败。它适合展示 LLM、JEPA 世界模型和安全控制如何各司其职。

本示例假设机器人只处理**常温、完好、预定义类别**的物品；不处理热源、液体泄漏、刀具、破碎玻璃、药物或未知物。硬件配置为双 7 自由度机械臂、带触觉与力控制的夹爪、头部 RGB-D、腕部相机、近场人/宠物检测和独立紧急停止。相关边界见配套场景范围说明。

## 1. 为什么这是一个双臂混合 Agent 任务

| 任务难点 | 单体 LLM / 反应式策略的典型不足 | 混合 Agent 的解决方式 |
|---|---|---|
| 自然语言歧义 | 容易把“收拾好”泛化为清空桌面或错误处置玻璃杯。 | LLM 将口语目标编译为对象、目标位置、禁止项和成功判据。 |
| 视觉部分可观测性 | 杯子被盘子遮挡、牛奶盒标签朝向变化时，单帧识别不可靠。 | JEPA 将连续观察编码为潜在状态，并预测遮挡后或动作后的状态变化。 |
| 双臂协同 | 一只手开门、另一只手搬牛奶时可能发生路径干涉。 | 世界模型对双臂联合动作进行短程前瞻；规划器选择无碰撞、低风险时序。 |
| 真实世界不确定性 | 夹爪抓不稳、冰箱门回弹、对象滑动会使固定脚本失效。 | 执行后校验预测残差；高残差自动触发局部重规划或安全停止。 |
| 家庭安全 | 人或宠物可能进入工作区，未知物不应被擅自处理。 | 独立安全层拥有最终拒绝权；LLM 无权覆盖人身安全和不可逆动作规则。 |

## 2. 模块映射：谁负责什么

| 混合 Agent 模块 | 本场景中的具体职责 | 输入 | 输出 | 不能做什么 |
|---|---|---|---|---|
| **LLM 任务编排器** | 解析用户意图；把“收拾好”拆为归还牛奶、移盘勺、保留玻璃杯、清洁空白区域；管理异常对话。 | 用户语音、家庭规则、状态摘要、失败事件。 | `PlanSpec`、子目标图、对象策略、澄清问题。 | 不直接输出关节角、力矩或绕过安全规则。 |
| **JEPA 状态编码器** | 融合连续 RGB-D、腕部视角、夹爪触觉和关节状态，维护厨房工作区潜在状态 \(z_t\)。 | 多帧观测、最近动作、对象追踪。 | 潜在状态、对象运动一致性、遮挡/状态置信度。 | 不解释“用户想要什么”。 |
| **动作条件 JEPA 世界模型** | 预测候选双臂动作后牛奶、盘子、玻璃杯、冰箱门及人类工作区的未来潜在状态。 | \(z_t\)、候选技能参数、目标表征、约束。 | `PredictionReport`：目标进展、碰撞/接触风险、不确定性、预期观测。 | 不自行授权高风险动作。 |
| **MPC / 技能规划器** | 组合 `open_fridge`、`grasp`、`carry`、`place`、`wipe` 等受限技能；每个短窗口后滚动重规划。 | `PlanSpec`、世界模型预测、技能库。 | `ActionProposal`，例如“左臂维持门开启，右臂以 0.15m/s 搬运牛奶”。 | 不改变用户任务的语义边界。 |
| **安全验证与执行层** | 检查人/宠物距离、夹爪力、工作区、对象温度/完整性、门的关闭风险；批准或拒绝动作。 | 动作提案、传感器、权限与家庭规则。 | 执行命令、减速/保持/急停、`OutcomeEvent`。 | 不因 LLM 的文字解释而忽视硬约束。 |

## 3. LLM 输出的任务规范

在本场景中，LLM 应把自然语言翻译为可校验的 `PlanSpec`，而不是直接说“我先拿牛奶，再拿盘子”。以下是一个可执行的简化版本。

```json
{
  "mission": "breakfast_table_tidy",
  "subgoals": [
    {
      "id": "g1",
      "intent": "将可识别且密封的牛奶盒放入冰箱指定冷藏区",
      "success": ["milk_in_fridge_zone", "fridge_door_closed_or_handed_back"]
    },
    {
      "id": "g2",
      "intent": "将盘子和勺子置于水槽内的待清洗区域",
      "success": ["plate_in_sink", "spoon_in_sink"]
    },
    {
      "id": "g3",
      "intent": "保持玻璃杯在桌面安全区域，不触碰、不移动",
      "success": ["glass_pose_change_lt_5mm"]
    },
    {
      "id": "g4",
      "intent": "擦拭无食物与无玻璃杯的桌面空白区域",
      "success": ["cleanable_region_coverage_gte_90pct", "no_object_contact"]
    }
  ],
  "constraints": {
    "forbidden_objects": ["glass_cup", "knife", "hot_container", "unknown_item"],
    "person_min_distance_m": 0.8,
    "pet_min_distance_m": 0.8,
    "max_gripper_force_n": 12,
    "max_cartesian_speed_mps": 0.25,
    "need_confirmation_for": ["unknown_object", "spill_detected", "fridge_door_blocked"]
  },
  "action_grammar": [
    "observe", "move_view", "open_fridge", "hold_door", "grasp", "carry", "place",
    "release", "wipe_region", "retreat", "ask_user", "safe_stop"
  ]
}
```

这里有一个重要设计细节：`g3` 不是“玻璃杯在桌上”这样的静态描述，而是明确规定**玻璃杯姿态变化不超过 5 mm**。这使安全层和事后验证器能够把“不要碰玻璃杯”转换为可量化、可审计的约束。

## 4. 感知状态与目标表示

机器人不应让 LLM 直接阅读全部视频流。状态层每个周期维护两份相互补充的状态。

| 状态类型 | 示例内容 | 消费者 |
|---|---|---|
| **JEPA 潜在状态 `z_t`** | 桌面空间关系、对象遮挡、双臂构型、冰箱门开合动力学、近期物体运动。 | 世界模型、MPC、分布外/不确定性检测。 |
| **可解释状态摘要** | `milk: {pose, sealed, confidence}`；`glass: {pose, fragile, protected_zone}`；`human_distance`；`fridge: {door_angle, shelf_free}`。 | LLM、规则引擎、审计日志、用户解释。 |
| **目标嵌入 `g`** | “牛奶在冷藏区”“盘和勺在水槽”“玻璃杯不动”“桌面区域干净”的联合目标。 | 世界模型和规划器的目标进展计算。 |

JEPA 的作用不是取代对象清单，而是让系统从连续观察中保留对决策有用的、可预测的状态结构。例如，当左臂握住冰箱门把手、右臂携带牛奶盒时，世界模型需要预测门的回弹趋势、两臂间隙、牛奶相对夹爪是否滑动，以及该动作序列是否会让玻璃杯受到干扰。

## 5. 任务图与双臂分工

任务图并不是严格串行。机器人应在安全约束允许的范围内并行化“非接触、低风险”的工作，但不能为了效率把双臂同时伸向同一狭窄区域。

| 阶段 | 左臂职责 | 右臂职责 | 并行许可条件 |
|---|---|---|---|
| S0：环境扫描 | 移动腕相机/维持安全姿态 | 移动腕相机/维持安全姿态 | 仅感知动作，且两臂工作空间不重叠。 |
| S1：归还牛奶 | 打开并稳定冰箱门 | 抓取、携带并放置牛奶盒 | 世界模型确认门保持力、双臂间隙、人体距离与牛奶抓取置信度均满足阈值。 |
| S2：整理餐具 | 将盘子移动到水槽 | 把勺子放入水槽或暂时保持桌面通道畅通 | 两条移动路径不交叉，水槽区域可达且无玻璃/热物。 |
| S3：擦拭桌面 | 持折叠布并擦拭经分割的安全区域 | 处于观察/防碰撞姿态，必要时更新视角 | 擦拭轨迹距玻璃杯保护区、食物残渣和人体区域均保持安全边界。 |
| S4：终态复核 | 观察冰箱门、盘子与桌面 | 观察玻璃杯和勺子位置 | 只进行感知，不再触碰对象。 |

## 6. 一次端到端执行轨迹

下面的数值是**示例性配置阈值**，用于说明信息如何流动，而不是对真实机器人性能的承诺。核心要求是：每次动作都可追溯到当前状态、世界模型预测、安全批准与真实结果；机器人永远只提交短动作窗口，而不是一次性执行完整长任务。

### T0：环境建立与任务编译

头部 RGB-D 与双腕相机完成两秒环境扫描。状态层识别到：密封牛奶盒在桌面右后方；一个盘子与一把勺子位于桌面中央；玻璃杯位于左前方；桌上有面包屑，但没有液体、热物或锐器；冰箱冷藏区可用；距机器人 1.4 m 内无人和宠物。LLM 将用户目标编译为前述 `PlanSpec`，并把玻璃杯周围半径 20 cm 的区域标记为不可擦拭、不可触碰保护区。

| 项目 | `StateEstimate` 示例 | 允许的后续操作 |
|---|---|---|
| 牛奶盒 | `sealed=true, pose_confidence=0.96, grasp_candidates=2` | 可规划抓取和归还。 |
| 玻璃杯 | `fragile=true, pose_confidence=0.98, protected_zone=true` | 只允许观察；禁止移动、清洁接触和近距离跨越。 |
| 桌面残渣 | `dry_food=true, spill_probability=0.03` | 可列为后续擦拭/回收区域。 |
| 冰箱 | `door_closed=true, handle_visible=true, shelf_free=true` | 可列为双臂开门与放置目标。 |
| 人/宠物 | `nearest_distance=1.4m` | 速度可保持在本任务的低速上限内。 |

### T1：世界模型选择“先归还牛奶”

LLM 的子目标图允许先移盘子或先收牛奶，但规划器偏向先处理牛奶，因为它需要开冰箱且其目标位置会减少桌面遮挡。左臂候选技能是 `open_fridge_and_hold`，右臂候选技能是 `grasp_milk → carry_to_fridge → place_on_shelf`。

世界模型对三组双臂轨迹做短程预测：一组为右臂先拿牛奶再开门，一组为左臂先开门再由右臂运送，一组为双臂同时从桌面中央穿越。第二组的预测目标进展最高，且预计两臂最小间隙和玻璃杯距离均满足约束；第三组虽然更快，但在靠近桌面中央时不确定性高，因而被淘汰。

| 候选 | 世界模型前瞻 | 安全门结论 | 决定 |
|---|---|---|---|
| A：右臂先抓奶、再开门 | 牛奶盒可能遮挡右腕相机；开门时双臂切换次数多。 | 允许但成本高。 | 不选。 |
| B：左臂开门并保持、右臂运奶 | 预测门角稳定、右臂到冷藏区可达、距玻璃杯安全。 | 满足硬约束。 | **选中。** |
| C：两臂同步穿越桌面中央 | 预测路径接近玻璃杯保护区，模型分歧较大。 | 风险/不确定性超过阈值。 | 拒绝。 |

### T2：左臂开门、右臂抓取并放置牛奶

左臂只执行一个短动作窗口：接近把手、闭合夹爪、将门转至 70°、维持门把手力。执行后，校验器确认门角为 68° 至 72°、门速度接近零且左臂力矩未超阈值。右臂随后抓取牛奶盒；触觉与视觉共同验证夹持，而不是只根据“夹爪已闭合”推断抓住了对象。

接下来，右臂在世界模型支持下执行“抬升—横向避开玻璃杯保护区—进入冷藏区—轻放—释放”的滚动轨迹。每次仅推进一个小窗口；若潜在预测表明牛奶盒会倾斜或碰到门边，规划器立即重新采样局部路径。

| 检查点 | 预期可观察事实 | 实际校验 | 继续条件 |
|---|---|---|---|
| 左臂开门后 | `door_angle≈70°` 且门角速度低 | 关节位置与腕相机估计一致 | 允许右臂进入。 |
| 右臂抓取后 | `milk_relative_pose_stable`、夹持力位于安全范围 | 触觉压力分布与牛奶盒视觉掩膜一致 | 允许搬运。 |
| 放置后 | `milk_in_fridge_zone`、`gripper_empty` | 冷藏区视觉区域与夹爪触觉均确认 | 允许撤出右臂。 |
| 合门前 | 无门缝障碍、人手、宠物或衣物 | 门边 RGB-D 与力反馈无异常 | 允许左臂缓慢释放并退回。 |

### T3：餐具收纳与玻璃杯保护

冰箱门关闭并验证后，LLM 激活 `g2`，同时保持 `g3` 的保护约束。规划器不会把“最快的移动路径”当成唯一目标：如果盘子和玻璃杯之间的通道过窄，它先让右臂把勺子移至水槽，再让左臂绕开玻璃杯抓盘子。若盘子上仍有大块食物，视觉/触觉分割会将其标记为“需要用户确认”，不会自动倾倒或冲洗。

盘子放入水槽时，低层控制器使用接触感知的缓慢放置，而非直接松爪。校验器要求盘子在水槽底部稳定、双臂已从水槽区域撤出、勺子在水槽可见区域；只有三项同时满足，`g2` 才标记完成。

### T4：受限擦拭与终态验证

LLM 为 `g4` 生成的不是一条自由手臂轨迹，而是一组已分割的 `wipe_region` 目标区域。安全层从这些区域扣除了玻璃杯保护区、食物残渣附近的确认区、桌沿和人与宠物动态区域。左臂持清洁布以低力值擦拭；右臂主要提供腕部补充视角，并保持远离工作区域的姿态。

世界模型预测擦拭轨迹对桌面、玻璃杯和布的短期影响；如果预测到布边缘会扫入玻璃杯保护区，规划器宁可缩小覆盖范围，也不追求“100% 清洁”。任务最终由只读观察完成：牛奶在冰箱、冰箱门闭合、盘勺在水槽、玻璃杯姿态变化在阈值内、允许擦拭区域覆盖达到目标且无约束违规。

## 7. 异常分支：混合架构何时停、何时重规划、何时询问

### 分支 A：右臂抓取后检测到牛奶盒滑移

触觉传感器显示夹持接触面积下降，腕相机也发现牛奶盒相对夹爪有旋转。世界模型本轮预测仍可能认为能到达冰箱，但不确定性显著上升。安全层不允许“再夹紧一点然后继续”这种未经验证的补救，而是进入 `CAUTIOUS`：右臂低速撤回至桌面上方安全高度，左臂继续稳定门或在安全时缓慢合门。规划器随后提出“重新观察标签朝向—选择更大接触面抓取—缩短搬运窗口”的恢复策略。

```text
触发：grasp_slip_probability > 阈值
动作：暂停前进 → 提升至安全高度 → 原地保持 → 重新估计物体姿态
恢复：生成替代抓取候选 → 世界模型重预测 → 安全门复核
禁止：带着不稳定抓取穿越玻璃杯保护区或进入冰箱深处
```

### 分支 B：人或宠物进入工作区

若近场检测显示人或宠物距任一机械臂小于 0.8 m，系统不让 LLM 判断“对方看起来是否注意到机器人”。执行器立即进入 `HOLD`：维持或缓慢退至已验证的安全姿态，取消当前动作窗口，且不再发起新的肢体运动。待连续观测确认安全距离恢复后，状态层重建 \(z_t\)，世界模型重新规划；在此期间 LLM 仅负责通过语音说明“已暂停，等待工作区清空”。

### 分支 C：冰箱门无法安全关闭

门角变化异常、门边检测到障碍或合门力超阈值时，左臂停止施力并将门维持在安全开度。因为“关闭冰箱门”可能夹到人手、衣物或塑料袋，机器人不自行推开未知障碍。它通过 `ask_user` 报告可解释事实，例如“检测到门缝内有障碍，无法安全关闭冰箱。请确认是否需要我保持开门状态或由您处理。”

### 分支 D：桌面检测到疑似液体或无法识别的物品

如果残渣分类从干面包屑变为液体泄漏，或视觉系统无法高置信识别一个物体，`g4` 不再继续。JEPA 的不确定性输出只能告诉系统“预测不可靠”，不能把未知物自动归类为可清洁物。安全层锁定擦拭技能，LLM 请求用户确认；若用户不在场，机器人保持远离未知区域并完成其他不依赖该区域的低风险子任务。

## 8. 运行时事件日志示例

| 时间点 | 组件 | 结构化事件 | 系统反应 |
|---|---|---|---|
| 00:00 | LLM | `PlanSpec.valid=true` | 启动任务与观测。 |
| 00:03 | 状态层 | `glass.protected_zone=true` | 将玻璃杯区域写入硬约束。 |
| 00:05 | 世界模型 | `candidate_B.success=high, uncertainty=low` | 规划器选取“左开门、右运奶”。 |
| 00:08 | 安全层 | `door_workspace_clear=true` | 批准左臂开门短窗口。 |
| 00:15 | 验证器 | `milk_grasp_verified=true` | 批准右臂搬运窗口。 |
| 00:27 | 验证器 | `milk_in_fridge_zone=true` | 切换至餐具子任务。 |
| 00:42 | 人体检测 | `human_distance=0.65m` | 进入 `HOLD`，取消未执行擦拭动作。 |
| 00:56 | 状态层 | `human_distance=1.3m, state_resynced=true` | 重新进行短程规划。 |
| 01:18 | 验证器 | `all_success_criteria=true` | 结束任务，向用户汇报结果与未处理异常。 |

## 9. 落地验收：何时可以让机器人从演示进入真实家庭

该任务不应以“演示视频看起来流畅”作为验收标准。最低限度应在仿真、受控厨房、受监督家庭三类环境中，分别测试对象位置变化、照明变化、遮挡、抓取滑移、人/宠物侵入、门缝障碍、未知物和网络/传感器中断。每次测试都需同时记录任务成功、约束违规、预测校准、误停/漏停、恢复成功和人工接管次数。

| 验收项 | 必须证明的能力 | 不通过时的处置 |
|---|---|---|
| 语义正确性 | 不移动玻璃杯、不处理未知/危险物，能就歧义提问。 | 回到 `PlanSpec`、对象策略或规则层修正。 |
| 状态与预测 | 抓取、门角、对象位姿等关键事件能被及时观测与校验。 | 增加传感器、数据或降低行动时域。 |
| 双臂协同 | 无自碰撞、无交叉抢占、能在狭窄空间安全串行化。 | 收紧工作区、减少并行度、增加 MPC 约束。 |
| 人身安全 | 人/宠物接近时稳定保持或撤退；急停有效。 | 禁止扩大自主权限，先修复检测与控制链。 |
| 恢复能力 | 抓取滑移、门障碍、视觉遮挡后不会盲目继续。 | 增加失败轨迹、可观察断言与恢复技能。 |
| 用户信任 | 可说明做了什么、为何暂停、哪些项未完成。 | 改善结构化事件到自然语言解释的映射。 |

## 10. 本示例的核心启示

这个家用场景说明，LLM 与 JEPA 的协同不应理解为“LLM 想一想、JEPA 看一看”。更准确的分工是：**LLM 维护用户意图与规范；JEPA 维护可预测环境状态；规划器把潜在预测转为短程动作；安全层把动作变成受约束、可停止、可校验的现实操作。**

双臂机器人真正的长程能力，来自每一个短动作窗口后的重观测与重规划，而不是一次生成一段很长的“机器人思维链”。当环境偏离预测时，系统应以真实观测覆盖语言假设；当风险超出可验证范围时，系统应暂停并询问，而不是用更流畅的文本掩盖不确定性。
