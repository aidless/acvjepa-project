# 猜想假设集（HYPOTHESES）

> 建立日期：2026-08-15 ｜ 来源：从「LLM 是否死胡同」路线之争与 AC-VJEPA 工程体系中提炼。
> 与 `PROJECT_PLAN.md` 的关系：**H1/H0 是主判定**（轻量 JEPA 是否优于冻结特征基线）；本文件是围绕主判定的细分假设，
> 每条独立编号、可证伪、可预注册。验证纪律：**验证前在 `决策记录.md` 登记，不得事后修改指标**（同 `M3_MPC_EVALUATION_DESIGN.md` v1 原则）。
> 状态：`proposed`（待验证）/ `preregistered`（已登记）/ `tested`（有结果）/ `refuted` / `supported`。

## 一、路线层（宏观，思想实验为主，C9 季度跟踪承载）

### H-R1：混合架构的失败主战场在「接口」，不在任一模型
- **表述**：LLM×JEPA 混合智能体中，瓶颈既不是 LLM 的高层分解、也不是 JEPA 的潜在预测，而是两者之间的**状态摘要协议**（自然语言目标 ↔ 潜在状态的语义对齐）。
- **依据**：`LLM × JEPA 世界模型混合 Agent：可实施架构蓝图.md` 的 PlanSpec 与可解释状态摘要双通道设计；LeCun 一方从未否认 LLM 作为接口层的价值。
- **可证伪预测**：混合系统故障日志中，「目标指代歧义 / 状态摘要失真」导致的失败 > 任一单模型预测错误导致的失败。
- **验证路径**：M3 评测失败归因中增加「接口错误」类别（扩展 `M3_MPC_EVALUATION_DESIGN.md` 指标表）。
- **状态**：proposed

### H-R2：JEPA 在软件 Agent 领域缺席，且这不妨碍它在具身领域胜利
- **表述**：世界模型对浏览器/办公流/纯文本工具 Agent 无成熟技术栈；软件 Agent 的可靠性来自显式状态机与工具后验证。具身智能方向 latent prediction 论文占比将持续上升。
- **依据**：JEPA 生态三梯队项目全部集中在操控/导航/驾驶/物理规划（jepa-agent-review 技能清单）。
- **可证伪预测**：按季度统计，具身方向 latent prediction 论文占比上升，而软件 Agent 方向「状态机/工具验证」类方法论文占比上升。
- **验证路径**：C9 季度核验（机制已建立，2026-11-15 起）。
- **状态**：proposed

## 二、技术机制层（M2/M3 管线可直接验证；本机 6GB 可做部分消融）

### H-T1：干预密度剪刀差——动作条件数据的边际价值远大于无标签视频
- **表述**：冻结官方编码器 + 轻预测器架构下，每新增 1 小时**同步动作轨迹**的闭环收益 > 每新增 10 小时**无标签视频**；且该比值在 5–10h 真实数据区间内最大。
- **依据**：V-JEPA 2 仅 62h DROID 即达动作条件后训练（arXiv:2506.09985）；「真正稀缺的是可用于行动反事实的数据」（`JEPA 世界模型训练的最大数据与算力瓶颈.md`）。
- **可证伪预测**：收益等值线在 D 层小时数轴上的斜率 > B 层轴斜率的 10 倍（同成本归一化后）。
- **验证路径**：M2 分档对比（B 层 1h/5h/20h/50h × D 层 2h/5h/10h），画闭环成功率等值线。**这是主判定 H1 的最有价值细化**。
- **状态**：proposed

### H-T2：投影头容量是冻结骨干配置的隐性瓶颈
- **表述**：骨干冻结后，`latent_dim=64/128` 的投影头成为表征压缩瓶颈——潜在预测误差下界由投影头容量决定，与骨干质量无关。
- **依据**：本项目真实架构（`vjepa_backbone.HFVJEPA2Backbone`：768→64 投影）；下游（VLA-JEPA 等）普遍小头。
- **可证伪预测**：head 宽度 32/64/128/256 下潜在预测误差呈饱和曲线；饱和点显著低于骨干维度 768。
- **验证路径**：本机 CUDA 微型消融（6GB 可跑，成本极低）——`train_p1_domain_adapt.py --latent-dim` 分档。
- **状态**：proposed

### H-T3：校准决定可用性，而非预测精度
- **表述**：闭环世界模型的可用性由不确定性校准（ECE）决定：预测精度相同但校准差（高 ECE）的模型，在 MPC 中比「无世界模型」基线更危险——因为它会自信地选错动作。
- **依据**：项目 loss 含 calibration 项；`M3_MPC_EVALUATION_DESIGN.md` 已把 OOD 不确定性列为指标；uncertainty-quantification 方向（ECE/Brier 是「世界模型校准」研究入口）。
- **可证伪预测**：按 ECE 分层报告成功率，预期顺序：低 ECE 组 > 无模型基线 > 高 ECE 组。
- **验证路径**：M3 评测按 ECE 分层报告（需先冻结 ECE 计算口径）。
- **状态**：proposed

### H-T4：EMA 目标编码器在「冻结骨干」配置下几乎无收益
- **表述**：JEPA 的 EMA 目标机制只在骨干可训时防止表征漂移；本项目「共享冻结 HF 权重 + 独立 EMA head」配置下，EMA 对 head 的慢更新收益微弱——换成固定同步目标结果几乎不变。
- **依据**：`install_hf_vjepa2_encoder` 正是共享冻结权重设计；EMA 收益的文献证据全部来自骨干端到端训练。
- **可证伪预测**：P1 训练 A/B（EMA vs 同步目标）潜在预测误差差异 < 5%。
- **验证路径**：本机 P1 训练 A/B 对比（`train_p1_domain_adapt.py` 加 `--ema-target` 开关）。
- **状态**：proposed

### H-T5：域适配恢复曲线是对数型，前 5h 吃掉大部分收益
- **表述**：官方权重在目标域（厨房/台面）的 OOD 表征衰减，无标签视频的恢复边际递减；最优性价比点在 5h 附近，之后数据量每翻倍收益 < 20%。
- **依据**：轻量方案文档 20–50h 起始预算为经验值，曲线形状未经验证。
- **可证伪预测**：潜在预测误差 vs B 层时长拟合为对数曲线（R²>0.9），5h 后斜率显著变平。
- **验证路径**：M2 P1 分档测量（1h/5h/20h/50h）。
- **状态**：proposed

## 三、数据层

### H-D1：失败轨迹的单位价值远超成功示范
- **表述**：等量数据下，「安全失败 + 恢复」轨迹对闭环性能的提升 > 成功示范（数据价值由难度而非数量决定）。
- **依据**：HITL 难例编译（`sim2real_hard_example_compiler.py`）、影子退化 RCA、四层金字塔「长尾失败」强调。
- **可证伪预测**：30% 失败轨迹的合成训练集 vs 全成功集，闭环成功率显著更高。
- **验证路径**：M3 前用合成 C 层对比两版训练集（失败轨迹须安全采集，见安全边界）。
- **状态**：proposed

## 四、生态/产业层（C9 季度跟踪承载）

### H-P1：开源生态会先于 AMI 达到「可复现闭环世界模型」
- **表述**：V-JEPA 2-AC/JEPA-WMs 系公开代码的社区迭代速度 > AMI 内部研发；AMI 的真正壁垒在工业数据与系统集成，而非模型能力。
- **可证伪预测**：2027 年前出现可复现的「开源 JEPA 闭环操控结果」，而 AMI 仍无公开复现。
- **验证路径**：C9 季度核验（跟踪已建立）。
- **状态**：proposed

### H-P2：JEPA 系采纳模式是「借编码器、自建预测器/规划器」
- **表述**：官方仓库被当作骨干提供方而非端到端方案（本项目 `vjepa_backbone.py` 即该模式样本）；下游引用 vjepa2 的项目中 >70% 只复用编码器。
- **验证路径**：统计 github.com/facebookresearch/vjepa2 的 dependent repos 用法分布。
- **状态**：proposed

## 五、元层（本项目自身是实验样本）

### H-M1：分布式接力假设
- **表述**：预注册 + 接口完备 + 开源交接，能让「无算力研究者」以显著非零概率影响大算力路线之争的裁决。
- **可证伪预测**：repo 开源后 6 个月内被 ≥1 个持有 GPU 的外部研究者 fork/PR 并执行 M3 评测（或同类评测）。
- **验证路径**：观察 repo 的 fork/issue/PR 动态。
- **状态**：proposed

## 预注册流程（本文件所有假设通用）

1. 决定验证某条假设 → 在 `决策记录.md` 登记（假设 ID、指标口径、数据分档、统计检验、预期效应量与样本量）；
2. 按登记执行，结果无论正负都写入该假设条目（`tested`/`supported`/`refuted`）并追加证据；
3. 指标口径一经登记不得修改；确需修改走新假设 ID，不覆盖原假设。

## 登记状态总表

| ID | 层 | 一句话 | 状态 | 验证成本 |
|---|---|---|---|---|
| H-R1 | 路线 | 混合架构失败主战场在接口 | proposed | M3 扩展 |
| H-R2 | 路线 | JEPA 缺席软件 Agent 但不妨碍具身 | proposed | C9 季度 |
| H-T1 | 技术 | 动作轨迹边际价值 > 10× 无标签视频 | proposed | M2 分档 |
| H-T2 | 技术 | 投影头容量是冻结配置瓶颈 | proposed | 本机消融（低） |
| H-T3 | 技术 | 校准决定可用性而非精度 | proposed | M3 分层 |
| H-T4 | 技术 | 冻结骨干下 EMA 几乎无收益 | proposed | 本机 A/B（低） |
| H-T5 | 技术 | 域适配恢复呈对数曲线，5h 为性价比点 | proposed | M2 P1 分档 |
| H-D1 | 数据 | 失败轨迹价值远超成功示范 | proposed | 合成对比 |
| H-P1 | 生态 | 开源生态先于 AMI 达到可复现闭环 | proposed | C9 季度 |
| H-P2 | 生态 | 采纳模式=借编码器自建预测器 | proposed | 仓库统计 |
| H-M1 | 元层 | 无算力研究者经开源接力影响裁决 | proposed | 观察 6 个月 |
