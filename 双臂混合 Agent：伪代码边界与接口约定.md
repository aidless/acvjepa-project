# 双臂混合 Agent：伪代码边界与接口约定

本设计采用三层接口：LLM 只产生类型化任务规范；JEPA 世界模型只评估状态转移与不确定性；底层控制器只接受受限、已批准的运动/夹爪命令。自然语言、自由文本和未经验证的模型输出均不得进入伺服控制回路。

| 层级 | 典型频率 | 输入 | 输出 | 安全边界 |
|---|---:|---|---|---|
| LLM 任务编排 | 任务开始、子目标切换或异常时 | 用户请求、家庭规则、可解释状态摘要 | `PlanSpec`、子目标、约束、澄清问题 | 不能发送关节、速度、力或原始工具命令。 |
| JEPA 状态/预测 | 观测窗口到达或规划窗口开始时 | 多帧观测、历史动作、目标嵌入 | `LatentState`、`PredictionReport`、不确定性 | 不能改变权限、跳过规则或直接执行。 |
| MPC 规划 | 每个短时域窗口 | 已验证状态、技能候选、预测报告、硬约束 | `ControlWindow` | 所有候选必须来自动作 DSL。 |
| 安全网关与伺服 | 高频、每个控制周期 | 已批准控制窗口、传感器、急停状态 | 受限低层命令或保持/急停 | 独立于 LLM 和世界模型，可拒绝/打断任意动作。 |

核心数据对象为 `PlanSpec`、`StateEstimate`、`SkillCandidate`、`PredictionReport`、`ControlWindow` 和 `OutcomeEvent`。每一个对象都带有 `mission_id`、时间戳、状态版本和可追溯的模型版本。
