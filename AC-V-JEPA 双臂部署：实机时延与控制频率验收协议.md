# AC-V-JEPA 双臂部署：实机时延与控制频率验收协议

## 验收前置条件

所有阈值必须由目标机器人厂家控制规范、任务最大允许速度、有效停止距离、负载、工作空间和风险评估确定。本协议不预设通用的毫秒或 Hz 数字；它要求团队在上线前写入项目特定的 `LIMITS`，并以端到端 p99 和最坏抖动而不是平均值验收。

## 必填运行时指标

| 指标 | 起点 | 终点 | 统计方式 | 失败处理 |
|---|---|---|---|---|
| `camera_to_state_age` | 相机曝光/本体采样时间戳 | 状态进入 AC-V-JEPA 前 | p50/p95/p99、最大值 | 超过新鲜度阈值，禁止新轨迹。 |
| `model_rollout_latency` | GPU 接收固定形状候选 batch | 得到 `PredictionReport` | p50/p95/p99、超时率 | 丢弃整批结果，不入轨迹队列。 |
| `plan_to_approved_window` | 状态窗口完整 | 安全内核批准短轨迹 | p50/p95/p99、队列深度 | 超过 deadline，进入保持/重新观测。 |
| `servo_period_jitter` | 相邻伺服命令 | 相邻伺服命令 | p99/最大周期偏差 | 交由厂商控制域降级/保持。 |
| `hazard_to_hold_latency` | 力异常、人近场、急停、看门狗事件 | 机器人确认保持/撤退 | 最坏值 | 未达项目安全要求则禁止受限自主。 |
| `stale_window_rejection` | 发现轨迹过期 | 控制器拒绝执行 | 计数与审计记录 | 任何漏拒绝均为阻塞缺陷。 |

## 分阶段压测矩阵

| 阶段 | 负载条件 | 必须观察的结果 | 放行条件 |
|---|---|---|---|
| 离线回放 | 固定录制轨迹；无硬件运动 | 输入—预测—安全决定可重复 | 状态/动作时间戳无错位。 |
| GPU 压测 | 最大候选数、最长时域、相机最高帧率、日志写入并发 | p99 仍小于规划 deadline；无无界显存增长 | 超时只导致丢弃/保持，不产生过期命令。 |
| 故障注入 | 暂停模型进程、丢帧、延迟传感器、队列堵塞 | 看门狗检测、轨迹 TTL 生效、本地控制不阻塞 | 机器人以预期模式保持/撤退。 |
| 仿真闭环 | 延迟、噪声、遮挡、滑移、人体进入、门受阻 | 风险事件被相应门控拦截 | 没有依赖 LLM/GPU 的安全放行。 |
| 影子实机 | 真实机器人执行确定性控制，模型仅记录 | 时延分布与仿真估计一致或更保守 | 预测不影响运动，仍可稳定记录。 |
| 受限自主 | 低速、低风险、单原子技能、人工在场 | 预测改善与安全行为均可审计 | 达标后才增加任务时域、速度或双臂并发。 |

## 强制故障用例

1. **GPU 推理超时。** 模型结果在 `PLAN_DEADLINE` 后返回时，安全内核必须拒绝，轨迹队列不可更新。
2. **状态过期。** 相机或本体消息暂停时，系统必须停止发新动作；不得把上一帧状态重复作为新状态。
3. **轨迹队列过期。** 当前短轨迹的 TTL 到期时，伺服环必须保持或切到预验证的安全段。
4. **人/宠物近场事件。** 该事件必须由低层/独立安全链处理，不等待 V-JEPA 或 LLM。
5. **模型输出 NaN、版本不匹配或置信度异常。** 结果必须判为不可用，并写入可审计失败事件。
6. **世界模型假阳性。** 即模型预测安全而几何/力/保护区规则不安全时，确定性安全规则必须赢得最终裁决。

## 记录格式

每个规划窗口应记录：`state_id`、传感器时间范围、模型/动作 schema/标定版本、候选数、时域、推理时延、p99 分桶、选中动作、风险/不确定性、是否被安全门裁剪、窗口 TTL、最终结果和人工接管事件。没有这些字段的成功率统计，不足以证明世界模型的真实部署价值。
