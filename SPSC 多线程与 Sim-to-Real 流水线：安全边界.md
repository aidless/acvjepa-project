# SPSC 多线程与 Sim-to-Real 流水线：安全边界

1. Python SPSC 示例仅展示数据流、状态版本、超时和回退信号；它不连接厂商 SDK、不发送关节命令，也不满足硬实时或安全认证要求。真实机器人必须将伺服、限幅、急停和近场安全放在独立的厂商控制/安全域。
2. `latest-only` 语义用于世界模型和规划数据：过期观测只能被丢弃，不能在队列中补处理后再驱动机器人。
3. 难例采集不是开放式探索授权。持续高不确定性首先触发保持、观察或人工示教；仅在预注册的低速、低力、安全包络技能内允许受监督的信息采集。
4. 仿真、生成或域随机化数据必须保留来源、种子、参数、物理引擎版本与质量标记；它们是扩充和消融资料，不能自动替代真实接触/触觉/安全数据。
5. 增量微调在离线或隔离训练环境进行；部署前必须经过历史 replay、难例、故障注入、仿真、影子模式和可回滚发布门控。
