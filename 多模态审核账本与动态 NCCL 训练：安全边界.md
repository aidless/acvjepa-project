# 多模态审核账本与动态 NCCL 训练：安全边界

1. 审核账本的目标是可验证的证据完整性、可追溯审核和抗事后静默篡改，不是替代真实硬件安全控制、访问控制、备份或法律合规流程。
2. 原始大体积 RGB-D、点云和视频应保存于访问受控的对象存储；账本只保存内容寻址哈希、Merkle 根、时间戳、来源、审核决策与签名。不能把隐私敏感原始数据写入不可删除的账本。
3. 每条审核事件绑定上一条事件哈希和当前证据 Merkle 根；签名密钥应位于受保护的密钥系统，撤销和角色权限由独立身份系统管理。仅哈希而无可信签名、访问控制和外部锚定不能抵抗有数据库写权限的攻击者。
4. 账本验证失败、证据缺失、签名无效或校准/传感器合同异常时，case 必须保持隔离；不得生成训练数据 patch 或自动重试实机任务。
5. 动态 UpdatePlan 在每一次 optimizer update 前由 rank 0 生成、广播并由所有 rank 校验。各 rank 可有不同局部微批数，但必须以相同参数版本共同执行一次最终同步 backward/AllReduce；过程内不能更改成员集合。
6. NCCL 通信错误、rank 健康失效、计划不一致、全局有效样本数为零或 checkpoint 血缘不匹配时，应停止当前 update，在安全 checkpoint 后重建 process group 或人工处置，绝不让任一 rank 独自继续 optimizer step。
