# JEPA 世界模型与主流大模型：技术架构的本质区别

**比较对象。** 本文比较两类公开范式：其一是以 GPT-4 技术报告为代表的**生成式、自回归 Transformer 基础模型**；其二是以 I-JEPA 与 V-JEPA 为代表的**联合嵌入预测架构（JEPA）**。这不是对 OpenAI 当前闭源模型完整内部实现的反向工程：GPT-4 的公开资料足以支持对训练目标和外部能力作比较，但不足以确认所有后续模型的具体层数、专家混合设计、数据配方或推理系统。

## 结论先行

两者最本质的分歧**不在于是否使用深度学习或 Transformer**。I-JEPA 的编码器和预测器也可建立在 Vision Transformer 上；GPT-4 也被公开描述为 Transformer-based model。[1] [2] 真正的分歧在于：模型被迫学习**什么可预测对象**、在**哪个表示空间**计算损失、以何种数据连接现实，以及是否将**行动后果**纳入模型的核心学习对象。

> **主流生成式大模型**把“预测下一个可观察符号”作为核心训练压力，因而天然擅长生成语言、代码和其他可离散化输出。**JEPA**把“从上下文预测另一部分的抽象表征”作为核心训练压力，试图舍弃不可预测的细节，保留与状态、语义和未来演化有关的结构。

这并不推出“LLM 只是词频表”或“JEPA 已具备因果智能”。前者把训练目标误写成内部表征的全部，后者把表征学习成果误写成完整智能体能力。更严谨的判断是：**JEPA 更像构造可行动世界模型所需的预测表征模块；LLM 更像构造语言接口、知识先验与高层任务分解模块。完整自主智能体很可能需要二者及控制、记忆、安全验证器共同组成的系统。**

## 一张表看清核心差异

| 比较维度 | 主流大模型：以 GPT-4 公开范式为例 | JEPA：以 I-JEPA / V-JEPA 为例 | 架构含义 |
|---|---|---|---|
| **核心训练目标** | 最大化下一个 token 的条件概率；GPT-4 公开表述为“预训练以预测文档的下一个 token”。[1] | 预测被遮挡目标区域的**特征/嵌入表示**；I-JEPA 预测图像目标块表示，V-JEPA 预测视频特征。[2] [3] | 前者学习可生成符号；后者学习可预测状态。 |
| **损失所在空间** | token 或其他可观察输出空间；错误常由词元级交叉熵衡量。 | 学习到的 latent / feature space；不要求像素级或文本级重构。 | JEPA 有意忽略不可预测、任务无关的细节。 |
| **典型骨架** | 自回归 Transformer：给定历史上下文，顺序地产生下一个输出。GPT-4 的详细内部骨架并未完整公开。 | I-JEPA 的具体实现有上下文编码器、目标编码器和预测器；目标编码器采用上下文编码器的指数滑动平均更新。[4] | JEPA 是一类学习架构，而非一款固定的“通用模型”。 |
| **时间与生成方式** | 推理时通常逐 token 采样，输出序列天然可读、可执行为文本或代码。 | 不以逐 token 生成作为目标；可一次或少量步骤预测目标表征，但若要显示图像/文本，需另接生成解码器。 | JEPA 更适合内部状态建模；LLM 更适合外部沟通与内容生成。 |
| **数据的世界接触方式** | 以语言语料为根基，并可接收图文等多模态输入；GPT-4 公开为图像和文本输入、文本输出。[1] | I-JEPA 从图像学习；V-JEPA 仅用公开视频上的特征预测学习，不使用文本、重建、负样本或预训练图像编码器。[3] | JEPA 的归纳偏置更靠近视觉时空结构；LLM 的归纳偏置更靠近人类符号与知识文本。 |
| **对不确定性的处理** | 直接给出下一个 token 的概率分布，并在采样中选择/搜索序列。 | 只预测抽象表征时，可以不必猜中随机纹理等细节；但多种未来状态的显式建模仍须另行设计。 | JEPA 规避“不必知道的随机性”，却不能自动解决多未来分支。 |
| **行动是否是原生变量** | 语言模型本身可描述计划、调用工具或经外部代理执行，但标准 next-token 目标不等于物理行动模型。 | 已公开 I-JEPA/V-JEPA 的重点是图像/视频表征，不是端到端动作控制；**动作条件预测**是实现完整世界模型仍需补齐的部分。 | “观察到世界”与“能预测我改变世界后的结果”是不同层级。 |
| **后训练与可控性** | GPT-4 报告明确有后训练 alignment，用于改善事实性和遵从期望行为。[1] | 论文重点是自监督表征预训练与下游视觉任务；价值对齐、任务策略和安全控制不是 I/V-JEPA 的主要公开结果。 | LLM 的产品化对齐链条更成熟；JEPA 需要进入更大的智能体安全栈。 |
| **已证据支持的强项** | 语言、代码、文档/图像理解、指令遵从与工具接口。 | 视觉与视频上的语义、运动和外观相关表征；V-JEPA 在图像/视频下游评测表现良好。[3] | 两类优势互补，当前不是同一任务上的公平替代。 |

## 从目标函数看：预测“符号”与预测“状态”

在最简化的抽象下，自回归语言模型学习的是：

> \(\max_\theta \sum_t \log p_\theta(x_t \mid x_{<t})\)

其中 \(x_t\) 是下一个 token。这个目标非常通用：只要文本、代码或其他模态被表达为 token 序列，模型就能把所有历史上下文压缩到内部状态，并继续生成。其代价是，模型被要求为每一个输出位置给出预测；当把同一思路直接扩展到高维传感器信号时，它可能耗费大量容量处理光照、背景纹理和测量噪声等对决策无关、却无法确定的细节。

I-JEPA 的简化目标则是：由可见上下文 \(c\) 经过编码器得到 \(z_c\)，由目标区域 \(y\) 经过目标编码器得到 \(z_y\)，预测器输出 \(\hat{z}_y=p_\theta(z_c, \mathrm{position})\)，最小化 \(D(\hat{z}_y,z_y)\)。目标不是还原 \(y\) 的每个像素，而是预测其在学习到的表示空间中的内容。I-JEPA 的论文明确将其界定为从单一上下文块预测同图像多个目标块的表示。[2]

因此，“JEPA 预测规律，LLM 预测文字”是一个**有用但过度压缩**的说法。LLM 同样可能在内部学到某些规律；JEPA 的预测表征也不必然就是因果规律。严格差别是：两者的训练损失把哪一类信息设为不可回避的预测对象不同。JEPA 选择把原始细节压出目标空间，试图让表示只承载可预测、语义相关的结构。

## 架构层面的差别：单模型生成器与非对称预测器

标准 LLM 推理是一条单向生成链：输入上下文经过 Transformer 后给出下一个 token 分布；已输出 token 再回灌为下一步上下文。它使得模型能够自然地写答案、代码和计划，但也意味着模型内部“预测未来”的过程通常通过外显的 token 序列、提示链、搜索或工具调用来展开。

I-JEPA 的典型设计则是**非对称的双编码器 + 预测器**。上下文编码器只处理可见区域；目标编码器为被遮挡区域提供训练目标，其参数以指数滑动平均方式从上下文编码器更新；预测器根据上下文表示和目标位置，直接预测目标表示。[4] 这使模型无需制造逼真的目标像素，仍能被训练信号迫使去建模“什么高层内容应该在这里”。V-JEPA 延续特征预测思想，用公开视频训练视觉模型，而不是用文本监督或重建损失。[3]

但这并非“Transformer 对抗非 Transformer”。两边都可以使用注意力网络、海量自监督训练和多模态编码器；**JEPA 的新意是预测对象与训练拓扑，而非宣布 Transformer 失效。**

## 为什么 JEPA 更接近世界模型，但仍不是完整世界模型

世界模型至少要回答三件事：当前处于什么状态、某个行动会带来什么状态转移、在不同目标与约束下该选哪个行动。I-JEPA 已经触及第一件事：从部分可观测静态图像中预测未见区域的语义表征。Meta 对其官方表述也很谨慎：将该预测器称作可建模静态空间不确定性的“原始且受限”的世界模型。[4]

V-JEPA 进一步触及时间结构：它从视频中学习特征预测，并在运动和外观相关视觉任务上评估。[3] 但是视频中的“下一帧相关表征”并不等价于“如果我施加动作 A，环境会发生什么”。后者需要把动作作为状态转移的条件、处理多种可能未来、在真实交互中收集干预数据，并结合规划、控制、长期记忆和安全约束。已公开的 I-JEPA / V-JEPA 结果没有证明这些端到端能力。

这正是对杨立昆叙事应作的限定：**JEPA 是通往动作条件世界模型的一种有力候选机制，不是已经完成的通用物理因果引擎。**

## 为什么主流 LLM 并非“只会文本”，但也不天然等于具身智能

GPT-4 已公开为可接收图像与文本输入的多模态模型，[1] 这说明主流大模型不应被概括为只见文字。不过，接收图像的输入接口与拥有经交互验证的物理世界模型并非一回事。模型可以识别图像、回答关于图像的问题、把自然语言任务拆成工具调用步骤，却仍在接触、动力学、传感器漂移、因果干预和风险控制上缺乏足够稳健性。

同样，LLM 的 next-token 训练不等于只能背诵。该目标推动模型压缩广泛的语言、代码、制度和人类常识结构；其内部表示能支持抽象、类比和局部规划。然而，若要把这种能力用于机器人或工业控制，工程上仍必须额外连接感知状态、外部记忆、工具、搜索、约束求解与低层控制器。所谓“主流路线”在实践中也已不是一块孤立的语言模型，而是模型加检索、工具、执行器和评估器构成的系统。

## 真正的竞争点：不是谁取代谁，而是谁负责闭环中最难的部分

一个成熟的自主系统可以作如下分工：

| 系统组件 | 适合的职责 | 为什么单独不足 |
|---|---|---|
| **LLM / 多模态基础模型** | 理解任务、调用知识、与人协作、把目标分解为可执行子任务 | 不天然提供高频、可信的物理状态转移模型。 |
| **JEPA 类感知与预测模块** | 将连续传感器流压缩为预测性状态表征，过滤无关细节 | 不天然输出语言，也不自动产生最优动作或安全证明。 |
| **动作条件世界模型** | 估计“采取行动后会发生什么”，为反事实比较和规划提供模拟空间 | 需要主动交互数据、长期记忆与不确定性建模。 |
| **规划、控制与安全验证器** | 搜索可行方案、执行低层动作、监测约束和故障恢复 | 需要与前三者反复闭环，而不能只在事后审查文本。 |

因此，AMI 若成功，最可能改变的是自主系统的**感知—状态预测—行动后果模拟**层；OpenAI 等主流模型最可能继续主导语言、工具、知识和高层推理接口层。最终的技术分水岭不是哪种模型在论文标题上更接近 AGI，而是谁能在未见过的现实环境中，用有限交互数据稳定地完成长时程任务，并在不确定性下遵守安全边界。

## 最终判断

把分歧表述为“JEPA 预测物理规律，LLM 只是统计幻觉”会掩盖关键事实：两者都从数据中的统计结构学习；任何因果结论都还需来自干预、行动和反事实检验。反过来，把 JEPA 仅仅当作“换了一种视觉预训练”也低估了它的野心：它改变了模型被要求预测的对象，并为连续世界中的高层状态建模提供了更合适的归纳偏置。

**最精确的一句话是：** 主流大模型把智能的入口放在可生成的符号序列上；JEPA 把智能的入口放在可预测的抽象状态上。前者更擅长说、写、检索、协作和组织；后者旨在让系统更少执着于世界的外观、更多建模世界的可行动结构。要走向可靠的具身智能，两条路线大概率需要融合，而不是互相消灭。

## 参考资料

[1]: [OpenAI et al., *GPT-4 Technical Report*, arXiv:2303.08774](https://arxiv.org/abs/2303.08774)

[2]: [Assran et al., *Self-Supervised Learning from Images with a Joint-Embedding Predictive Architecture*, arXiv:2301.08243](https://arxiv.org/abs/2301.08243)

[3]: [Bardes et al., *Revisiting Feature Prediction for Learning Visual Representations from Video*, arXiv:2404.08471](https://arxiv.org/abs/2404.08471)

[4]: [Meta AI, *I-JEPA: The first AI model based on Yann LeCun’s vision for more human-like AI*](https://ai.meta.com/blog/yann-lecun-ai-model-i-jepa/)

[5]: [OpenAI, *GPT-4 research page*](https://openai.com/index/gpt-4-research/)
