# 影子退化诊断与点云—视频合成：安全边界

1. 根因分析输出的是按证据排序的“假设与调优建议”，不是对真实因果的自动判决。它只能触发暂停、回滚、补充评测或训练工单，不能自动放宽安全阈值或推广候选模型。
2. 阴影记录须以相同 `correlation_id` 关联基线、候选、传感器/标定、任务、边缘运行时与事后事件；无法关联的数据只可进入诊断隔离区，不用于发布结论。
3. 合成流水线应生成可重放、带种子、相机内外参、物理/渲染参数、动作 trace、仿真版本和来源标签的点云—视频对。它不应声称伪造的软体动力学就是物理真值。
4. 软体材料、接触和形变参数只能从审核过的物理先验中采样。传感器/相机扰动必须有界；硬件/人员安全事件不得自动编译为探索或训练作业。
5. 合成产物须经过 schema、时间同步、点云有效性、图像/深度一致性、接触/终态和 sim-real 相似性门控。合成数据不能替代真实接触与触觉的独立验证。
