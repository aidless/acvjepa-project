# NCCL 真实集群压测与故障恢复实验：安全边界

1. 压测和故障注入仅在隔离的预生产/专用 GPU 队列、可丢弃的数据集副本和非机器人控制运行中执行；不得在共享生产训练、在线推理、真实机械臂任务或有未保存状态的集群上直接注入故障。
2. 任何 kill、网络限速/隔离、GPU reset、驱动重载或节点重启动作都必须由具有集群权限的值班人员在已批准的变更窗口执行。本文提供实验设计与观察点，不提供绕过权限或破坏他人作业的命令。
3. 故障实验必须在每轮前写入已验证 checkpoint，并确认工作负载可从该 checkpoint 重建 process group；严禁把“进程没有挂死”误作数据或优化状态正确恢复。
4. 压测 telemetry、NCCL 日志、拓扑文件、环境变量与 checkpoint 血缘可能暴露主机/网络信息，应进入受控实验工件库，不写入训练样本或 HITL 账本的公开字段。
5. GPU 温度、功率、ECC/Xid、IB/RoCE 端口错误或节点健康告警达到既定停止阈值时，应中止实验、释放资源并转交基础设施负责人。不得反复重试以掩盖硬件或网络故障。
6. 任何模型 checkpoint 仅可用于离线一致性、恢复和影子评测验证；压测输出不能直接促进模型上线或改变机器人安全策略。
