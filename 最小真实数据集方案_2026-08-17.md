# 最小真实数据集获取方案（2026-08-17）

> 背景：现状评审 P0-B——「哪怕 1h 合法 B 层视频即可解锁 H-T5/H-T2 真实复验」。本方案把「等真实数据」细化为「最小可行数据集获取」。
> 数据金字塔（DATA_MANIFEST）：A 官方权重（已有）/ B 无标签视频 / C RoboCasa 仿真 / D 真实轨迹。
> 关键：**H-T5（对数恢复曲线）与 H-T2 真实复验只需 B 层（无标签视频），不需要动作、不需要 RoboCasa**——这是当前最小解锁点。

## 一、最小解锁点：B 层无标签视频（1–5h 即可起步）

H-T5 预注册：P1 latent NLL vs B 层时长 1h/5h/20h/50h，拟合对数曲线。
**最小可行**：先拿 **1h + 5h** 两档即可画出曲线前段（判断是否对数 + 5h 性价比点），20h/50h 后续补。

## 二、公开数据集候选（B 层=无标签视频，厨房/家居域优先）

| 数据集 | 域 | 规模 | 许可 | 适配性 | 备注 |
|---|---|---|---|---|---|
| **Something-Something v2** | 人手-物体交互（桌面） | ~220k 短视频 | 研究用途（需申请） | ★★★★ 桌面操控、无语言依赖 | 最贴近「台面整理」；需注册下载 |
| **Epic-Kitchens-100** | 厨房第一视角 | 100h | CC BY-NC 4.0（研究） | ★★★★ 厨房域 | 量大，取子集即可；注意 NC 条款 |
| **DROID**（视频部分） | 机器人操控 | 76k 轨迹 | CC-BY | ★★★ 含动作（可只取视频帧做 B 层） | 与 A 层权重同源域 |
| **Open X-Embodiment** | 多机器人 | 1M+ | 各源混合 | ★★★ 体量大 | 取厨房/桌面子集 |
| **Bridge V2** | 桌面操控 | 60k | CC-BY | ★★★ | DROID 同源 |
| 自拍/自录（合法） | 自定义厨房 | 任意 | 自有 | ★★★★★ 无许可障碍 | 1h 手机拍摄即可，最可控 |

> 许可核对要点：研究用途 vs 商用；CC-BY-NC 不可商用（本项目 MIT 开源，若数据仅用于内部研究可接受，但**不得随仓库分发**）；下载渠道与配额。

## 三、最小执行路径（按成本排序）

1. **自录 1h 厨房/台面视频**（零许可风险，最快）→ `video_to_windows.py` 切窗 → P1 域适配 → H-T5 首点；
2. **Something-Something v2 子集**（注册下载，桌面域最贴合）→ 1h/5h 两档 → H-T5 曲线前段；
3. **Epic-Kitchens 子集**（若需更大域覆盖）。

## 四、解锁后的执行链（接 `M2_M3_REAL_RUNBOOK.md`）

```
B 层视频 → video_to_windows.py（切窗，domain_adaptation_only）
        → train_p1_domain_adapt.py（P1 域适配，1h/5h 分档）
        → H-T5 对数曲线拟合 + H-T2 真实复验（同口径 run_ht2_ht4_ablation.sh）
```
- H-T5 判据：对数拟合 R²>0.9 + Δ(5→20)<20%·Δ(1→5)；
- H-T2 复验：解除合成限定（若真实数据仍方向反转 → 更新 G2 定案）。

## 五、风险与边界

- **合成 vs 真实的边界**（BLUEPRINT §7.10）：B 层真实数据才能裁决「方向」；合成只证机制；
- 许可合规：NC/研究用途数据**不入 git、不随仓库分发**，仅本地/云端训练用；
- 最小起步原则：先 1h 验证管线 + H-T5 首点，再决定是否扩到 5h/20h。

## 六、已验证可得性（2026-08-17 云端实测）

> 用云端 curl 实测（本地 web_search 额度已尽），结论如下：

| 渠道/数据集 | 实测结果 | 可用性 |
|---|---|---|
| **hf-mirror.com**（AutoDL 学术镜像） | http=200，API=200 | ✅ **解锁 HF**：设 `HF_ENDPOINT=https://hf-mirror.com` 即可在云端下载 HF 数据集 |
| `lerobot/pusht` | MIT，14814 下载 | ✅ 可下载（LeRobot 规范 2D 推块任务，非厨房域） |
| `lerobot/aloha_sim_transfer_cube_human` | MIT，8788 下载 | ✅ 可下载（仿真双臂，非厨房域） |
| `lerobot/bridge` | 「Invalid username or password」 | ❌ 门控，需 HF token |
| `epic-kitchens/epic-kitchens-100-rgb-frames` | 「Invalid username or password」 | ❌ 门控，需 HF token（或走官方站） |
| Something-Something v2（Qualcomm/TwentyBN 官方） | http=200，需跳转注册 | ⚠️ 门控，需注册（用户动作） |
| epic-kitchens.github.io 官方站 | http=200 | ✅ 可达（官方下载渠道） |

**结论**：
1. **HF 访问已解锁**（hf-mirror），`pusht`/`aloha_sim`（MIT）可立即下载——但它们是**仿真/台面机器人域，非厨房域**，适合做 **LeRobot 管线验证**与 **LeRobot 系 M3 替代评测**，不适合 H-T5 的厨房域适配；
2. **厨房域数据（epic-kitchens/SSv2）均为门控**，需 HF token 或官方注册——**用户动作**；
3. **云端磁盘已满（331MB 空闲）**——任何实际下载前**必须先清磁盘**（另一会话占满 200G）。

## 七、修正后的最小执行路径（按可行性排序）

1. **清磁盘**（前置，必需）：清理云端另一会话冗余 / 换实例；
2. **LeRobot 管线验证**（可立即做，MIT 数据）：经 hf-mirror 下 `pusht`，跑通「LeRobot 数据 → P1/P2 → 评测」链，验证非合成数据管线；
3. **厨房域 B 层**（需用户）：epic-kitchens 官方站下载（需注册/许可核对）或 HF token；
4. **自录**（零许可，最可控）：1h 厨房视频，手机即可。

## 八、待用户确认

- 数据源选择（自录 vs 公开数据集 vs 混合）；
- 下载/存储位置（本地 or 云端——磁盘已释放至 25G 可用）；
- 许可接受范围（是否接受 CC-BY-NC 仅研究用途）。

## 九、里程碑：真实数据已获取（2026-08-17 云端实测）

**已完成**（项目首次拿到真实机器人数据，数据获取路径打通）：
- 经 `hf-mirror.com`（`HF_ENDPOINT=https://hf-mirror.com`）在云端成功下载 **`lerobot/pusht`**（MIT）：
  - 206 episodes / 25650 帧 / 96×96×3 视频（av1，10fps）/ observation.state 2 维；
  - 落盘 `/root/autodl-tmp/realdata/pusht`（7.4MB，LeRobot v3.0 格式 parquet+mp4）。

**已识别的后续依赖**（转换到我方窗口格式前需解决）：
- **视频解码器缺失**：云端无 ffmpeg/PyAV，torchvision 未带 video——需 `pip install av`（PyAV 自带 ffmpeg 库）；
- **格式映射**：pusht 96×96 → 我方 384×384（V-JEPA 2.1 ViT-B 需 384，需上采样）；proprio 2 维 → 8 维（pad）；action → action_dim 20；
- **域不匹配**：pusht 是 2D 推块仿真域，**非厨房/台面域**——适合做 **LeRobot 管线验证**与**非合成数据链路打通**，**不适合** H-T5 厨房域适配判定。

**结论**：数据获取已解锁（MIT 数据可下载）；厨房域真实数据（epic-kitchens/SSv2）仍需 HF token 或官方注册（用户动作）。~~「pusht → 窗口格式转换 + 真实数据链路冒烟」是下一个聚焦工程步骤~~ → **已完成（见 §十）**。

## 十、里程碑升级：真实数据管线冒烟通过（2026-08-17 云端）

**已完成**（真实、非合成数据首次流过 AC-VJEPA 训练管线）：
- 装 `PyAV`（`pip install av`）→ av1 解码通过（96×96 rgb 帧）；
- **`scripts/lerobot_to_windows.py`**（已入库）：LeRobot v3 parquet+mp4 → AC-VJEPA 窗口（96→384 双线性上采样、ImageNet 归一化、proprio 2→8 / action 2→20 pad、future_events 置零）；
- **转换**：pusht 前 15 episodes（2056 帧）→ **206 窗口**（2.4G，384×384）；
- **P1 域适配冒烟**（冻结 V-JEPA 2.1 骨干，latent_dim 64，5 epochs）：`loss_total` 单调下降 **−0.21 → −0.38**、`loss_calibration` 0.20→0.11、`event_term=0`（P1 正确）；`p1-last.pt`（434MB）落盘，`P1_EXIT=0`。

**意义**：证明「真实视频 → 窗口 → 冻结骨干 P1 训练」全链路可用；此前 H-T2/H-T4/H-D1 用的是随机合成张量，本次首次用真实机器人帧。
**边界**：pusht 为 2D 推块域，仅验证管线，**不作 H-T5 厨房域判定**；厨房域数据仍待用户（HF token/官方注册/自录）。
