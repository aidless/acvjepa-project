# H-M1 主动外联包（2026-08-17）

> 背景：H-M1（分布式接力假设）预测 repo 开源后 6 个月内被 ≥1 个持 GPU 的外部研究者 fork/PR 并执行 M3 评测。当前 0 star / 0 fork（2026-08-17 GitHub API）——**等不来，必须主动发**。
> 现状评审结论：这是当前唯一高杠杆、未阻塞、未执行的动作。本包提供「发给谁、说什么、给什么」。
> 纪律：外联不承诺算力/数据，只提供**已就绪的预注册评测资产**；任何对方贡献走 CONTRIBUTING 流程。

## 一、协作资产（我们提供什么）

1. **M3 v1.2 预注册评测包**：三基线（无世界模型/冻结特征/轻量 JEPA）+ 六指标 + ECE 分层/时间轴 + 配对统计 + 判定阈值，口径已冻结（`M3_MPC_EVALUATION_DESIGN.md`）。
2. **真实权重集成**：官方 V-JEPA 2.1 ViT-B 80M（391/395 键）适配 + P2 动作条件训练链（`train_ac_vjepa_ddp.py`）。
3. **评测骨架 + vla-eval 对接面**：`m3_mpc_eval.py` + `m3_vla_eval_dock.py`（模型服务器 predict 契约）。
4. **59/59 工程验证基线** + 完整决策记录 + 可复现脚本/seed。
5. **已跑通的 chain-live 演示**（`experiments/p2_chain_live_2026-08-17/`）。

**一句话卖点**：「我们有一套口径已冻结、可直接执行的 M3 评测协议 + 真实权重训练链，缺的只是你的 GPU 跑一次真实评测——跑完结果正负同录、署名共享。」

## 二、目标对象（按可达性排序）

| 优先级 | 对象 | 为什么 | 接触渠道 |
|---|---|---|---|
| ★★★ | **allenai/vla-evaluation-harness 维护者** | 已有 RoboCasa 集成，正是 M3 评测的天然底座；我们已写好 dock 骨架 | GitHub issue / PR（附 dock 骨架 + 预注册包） |
| ★★★ | **ginwind/VLA-JEPA 作者** | 「借 V-JEPA 编码器自建预测器」路线的直接实践者；可联合验证 H-T1 | GitHub issue / 论文联系方式 |
| ★★ | **facebookresearch/jepa-wms 团队** | JEPA 物理规划基准平台；可对比我们的轻量冻结配置 | GitHub issue |
| ★★ | **LeRobot（huggingface）社区** | 已集成 VLA-JEPA 策略；可在其基准上跑我们的评测 | GitHub discussion / PR |
| ★ | **INTACT-JEPA / LeWorldModel 作者** | 免搜索/端到端路线对照臂 | GitHub issue |
| ★ | 具身智能方向实验室（高校/初创） | 持 GPU + 真实数据 | 邮件（附预注册包摘要） |

## 三、协作邀约文案模板（可直接用）

```
主题：一套口径已冻结的 JEPA 世界模型闭环评测协议，邀请您在真实评测上协作

您好，

我们在做一个可证伪的路线裁决实验（AC-VJEPA）：检验「轻量 JEPA 动作条件
世界模型」在闭环规划上是否真的优于「冻结特征/无世界模型」基线。

已就绪（MIT 开源）：
- M3 评测协议 v1.2：三基线 + 六指标 + ECE 分层/时间轴 + 配对统计，口径冻结
- 官方 V-JEPA 2.1 80M 权重集成 + P2 动作条件训练链
- vla-eval 对接骨架（可与您的 RoboCasa/基准集成）
- 59/59 工程验证 + 完整决策记录 + 可复现脚本

缺的只是一次真实评测的算力。若您愿意执行（或指认合适的人），我们提供：
预注册包、对接代码、署名共享；结果正负同录、不挑指标。

仓库：github.com/aidless/acvjepa-project
协议文档：M3_MPC_EVALUATION_DESIGN.md（v1.2）
外联包：H-M1_主动外联包_2026-08-17.md

期待您的回复。
```

## 四、执行步骤（待批准后由会话代理代操作）

1. 选 1–2 个 ★★★ 对象起草 GitHub issue（附 dock 骨架 diff + 预注册包链接）；
2. 发出后在 BACKLOG A20 记录观察读数；
3. 若对方响应 → 按 CONTRIBUTING 走，结果写回 HYPOTHESES H-M1 + 决策记录。

## 五、风险与边界

- **不承诺**算力/数据/SLA；只提供已就绪资产；
- 外联文案**不含**夸大（不称「首个」「SOTA」）；
- 对方若拒绝/无响应 → 记录为 H-M1 观察数据，不重试骚扰；
- 所有外联动作在决策记录登记（日期/对象/渠道/结果）。
