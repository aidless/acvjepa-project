# LLM × JEPA 混合 Agent：设计范围

## 目标场景

本蓝图面向需要在连续、部分可观测环境中完成多阶段任务的视觉/具身 Agent，例如机器人操控、视觉导航、工业检查或自动化设备控制。它也可迁移到软件 Agent，但软件环境应使用结构化状态转移模型，而不是强行以视频 JEPA 代替浏览器/数据库状态。

## 非目标

本蓝图不假设当前 I-JEPA 或 V-JEPA 已经是完整通用世界模型，不将 LLM 直接授权为高风险执行器，也不以端到端训练取代确定性安全规则、权限控制与人工确认。

## 核心分工

| 模块 | 主要职责 | 不应承担的职责 |
|---|---|---|
| LLM | 理解自然语言目标、检索知识、分层任务分解、解释与异常处置 | 直接估计连续物理状态或无验证地执行高风险动作 |
| JEPA 世界模型 | 从观测中维护预测性潜在状态，预测动作后果及不确定性 | 解释复杂用户意图、生成最终自然语言答复、单独决定业务权限 |
| 规划/控制器 | 在世界模型上搜索短至中程动作序列，施加硬约束并执行滚动优化 | 解释宽泛语义目标或覆盖安全策略 |
| 验证与安全层 | 校验真实观测、动作前置条件、不可逆性、风险与恢复路径 | 充当模糊任务的唯一规划器 |
