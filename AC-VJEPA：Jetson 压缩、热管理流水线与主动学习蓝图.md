# AC-VJEPA：Jetson 压缩、热管理流水线与主动学习蓝图

## 结论先行

如果 Jetson AGX Orin 在复杂动态环境下的端到端推理 p99 仍无法稳定落入 30 ms，首先不应把问题简化为“再量化一点”。必须先定位 p99 来自**视频编码、候选 rollout、CPU–GPU 拷贝、TensorRT 子图回退、队列积压、热降频还是安全门**。对 AC-VJEPA，最稳妥的压缩路径是：以完整教师模型为基准，先固定时空 token 预算并蒸馏更小学生模型；随后进行硬件友好的结构化裁剪；再配合 FP16/选择性 INT8；最后才尝试经验证的 2:4 结构化稀疏。无结构随机剪枝即使减少参数，也未必降低 TensorRT 的真实 p99。

在控制侧，GPU 世界模型绝不应取代本地安全/伺服环。系统应将传感器采集、状态准备、GPU 推理、规划、安全与伺服放在有界、可丢弃旧数据的多速率流水线中，并用热/功耗感知的**降级状态机**而非“跑得慢就继续堆队列”来处理抖动。对于柔软物体等未见物理交互，持续高不确定性首先意味着“停止自主推进并收集可审计难例”，不意味着允许机器人自由探索或让在线模型直接改权重。

## 1. 30 ms p99 的正确定义与预算

这里的 30 ms 应定义为“**完整且新鲜的传感器窗口已到达 → 新的经安全批准的短期控制窗口进入队列**”。它不包含等待下一次相机曝光的自然帧周期，也不包含硬件伺服本身；相机帧年龄仍必须另行受控。

| 阶段 | 初始 p99 预算目标（示例，不是性能承诺） | 关键优化 | 若超时 |
|---|---:|---|---|
| 状态窗口对齐、预处理与拷贝 | ≤4 ms | 固定 shape、预分配/页锁定缓冲、零拷贝路径优先、只取最新完整窗口 | 丢弃窗口，不补算旧帧。 |
| 视觉状态编码 | ≤10 ms | 减少 token/帧数、学生骨干、TensorRT FP16/INT8、特征滚动缓存 | 不生成新规划。 |
| 动作条件 rollout 与候选排序 | ≤7 ms | 共享状态编码、批量候选、缩短时域、蒸馏预测器 | 减少候选或进入保持。 |
| 风险、不确定性与确定性安全门 | ≤2 ms | 常驻 CPU、预计算保护区、无网络调用 | 拒绝新控制窗口。 |
| 轨迹桥接与队列替换 | ≤1 ms | 原子替换、TTL、无锁/单生产者单消费者环形缓冲 | 保持当前有效安全段。 |
| 抖动预留 | ≤6 ms | GPU 流优先级、CPU 亲和性、热裕量与队列上界 | 达到降级门限。 |

这只是一个设计起点。最终是否满足要求，必须在目标 Jetson、目标 JetPack、目标功耗模式、目标散热、并发相机/日志负载和热稳态下测量 p50/p95/p99。[1]

## 2. 面向真实延迟的模型压缩优先级

### 2.1 先压缩时空计算，再压缩参数

对视频世界模型，时空 token 往往比 MLP 参数更直接决定计算量。应固定输入为已测量的静态 shape，例如短历史窗口、固定低风险分辨率和固定 rollout 时域；再根据任务验证逐步提高。不要为了覆盖极端情形在所有时刻输入长视频或高分辨率。

| 层级 | 策略 | 对 p99 的作用 | 对 AC-VJEPA 的特殊约束 |
|---|---|---|---|
| 时空预算 | 减少上下文帧、时间步采样、ROI/对象裁剪、滚动特征缓存 | 直接降低视觉编码 token 与显存流量 | 不能丢失接触前后、形变开始等关键时序；需按事件保留高频窗口。 |
| 学生架构 | 降低视觉宽度/深度、减少 predictor GRU/Transformer 层数/宽度、缩短候选时域 | 降低每次前向与候选 rollout 成本 | 保留动作条件输入、事件头和不确定性头。 |
| 结构化裁剪 | 裁剪 attention head、MLP 通道、卷积输出通道或整个层 | 实际减少矩阵尺寸，TensorRT 更可能获得收益 | 裁剪后必须重新训练/蒸馏；不直接裁掉风险/方差输出路径。 |
| 2:4 结构稀疏 | 对支持层进行硬件感知稀疏化后再微调 | 可能进一步提高可支持层吞吐 | 先确认目标 TensorRT 图和目标硬件实际使用稀疏内核；否则可能无真实收益。 |
| 低精度 | FP16 基线，选择性 INT8，再评估更广泛 INT8 | 降低算力和带宽压力 | `log_variance`、归一化和阈值敏感头应先保留 FP16。 |

NVIDIA 关于 TensorRT 结构化稀疏/INT8 的公开材料强调，稀疏化应配合训练/量化流程维持精度；因此它是“重新训练后的模型结构选择”，不是对任意现有 checkpoint 的无损开关。[2]

### 2.2 世界模型蒸馏：蒸馏行为与“知道自己不知道”

将完整 AC-VJEPA 作为教师，训练一个具有更小视觉编码器和更窄动作条件预测器的学生。蒸馏不应只匹配最终潜在状态；还应匹配事件和不确定性排序，否则学生可能跑得更快却在未知形变交互中变得虚假自信。

> \(\mathcal{L}_{student}=\mathcal{L}_{JEPA}+\lambda_z\mathcal{L}_{latentKD}+\lambda_e T^2\operatorname{KL}(p_e^T/T\Vert p_e^S/T)+\lambda_u\mathcal{L}_{uncertainty\_rank}+\lambda_r\mathcal{L}_{replay}\)

其中 `latentKD` 匹配教师未来潜在轨迹；事件蒸馏匹配任务事件 logits；`uncertainty_rank` 要求学生在教师认为高风险/高不确定的窗口中维持相对排序；`replay` 保留历史正常任务，缓解只学习新长尾场景造成的遗忘。

| 学生组件 | 建议压缩方向 | 教师监督信号 | 首轮验收 |
|---|---|---|---|
| 视觉编码器 | 减少层数/通道，或选更小 V-JEPA 兼容骨干 | 上下文与未来 latent | 未见布局/光照下的 latent 与事件表现。 |
| 动作 tokenizer | 更小 MLP、共享左右臂部分投影 | 条件 latent 变化 | 不同双臂动作候选能否仍被正确区分。 |
| 时序预测器 | 降低隐藏宽度、层数和 horizon | 多步 future latent + 事件 | 目标达成/接触/门角等短期事件。 |
| 方差/风险头 | 尽量保留宽度和 FP16 | 误差—不确定性关系、风险排序 | OOD/柔软物体样本上的 AUROC/校准与回退召回。 |

学生训练数据必须混合：代表性正常轨迹、仿真变化、传感器噪声/失败回放和人工审核后的柔软物体难例。只用教师在正常数据上的伪标签蒸馏，会强化常见场景，无法修复长尾物理交互。

### 2.3 裁剪实验的顺序

1. **建立 FP16 静态 TensorRT 基线。** 测量每个子图、CUDA 回退比例和端到端 p99。
2. **做结构化敏感度扫描。** 分别裁剪视觉通道、预测器宽度、attention head/层和动作 token 宽度，每次只改一个轴。
3. **逐步裁剪—蒸馏—短期微调。** 使用教师、多任务损失和历史 replay；不一次性大比例裁剪。
4. **量化感知或目标域 PTQ。** 先 FP16，再选择性 INT8；INT8 校准必须包含相机、本体和动作三输入。[3]
5. **验证真实 TensorRT 图覆盖。** 不能只看参数/FLOPs；检查关键子图是否仍落到 CUDA EP，及实际 p99 是否改善。
6. **仅对有收益的层启用结构稀疏。** 目标硬件/引擎不产生加速，就不把稀疏化当作性能优化。

## 3. Jetson AGX Orin：功耗、热与多线程流水线

Jetson 的电源、热和电气管理会同时约束时钟、功耗门控、电压、CPU 核可用性等；负载调频和热管理也会互相影响。[1] 因此，“开发时强制最高频率能跑到 25 ms”不等于“密闭机器人底盘热稳态下 p99 仍小于 30 ms”。

### 3.1 三个故障域与六条流水线

```text
独立安全/伺服域（不依赖 GPU、Python、LLM）
  RT_CONTROL ───────────→ 厂商控制器 / 急停 / 力和速度限幅
  FAST_SAFETY ──────────→ 人/宠物、碰撞、TTL、硬件健康

边缘推理域（Jetson）
  SENSOR_CAPTURE ──SPSC ring──→ STATE_PREP ──latest-only──→ GPU_INFERENCE
                                                    ↓              ↓
                                            ACTIVE_LOGGER      PLAN + SAFETY_GATE
                                                                   ↓
                                                          原子替换短期轨迹窗口

任务与学习域（低优先级）
  TASK/LLM/UPLOAD/TRAINING ──仅接收事件和审计数据；不在实时关键路径
```

| 线程/进程 | CPU/GPU 放置原则 | 队列与优先级 | 绝不能做的事 |
|---|---|---|---|
| `RT_CONTROL` | 厂商实时控制域或隔离 CPU；不运行 Python/GPU kernel | 固定周期、预分配、无锁读取有效轨迹 | 等待模型、LLM、磁盘或网络。 |
| `FAST_SAFETY` | 与控制域隔离；可用独立安全硬件/驱动 | 高优先级、事件抢占 | 因等世界模型推理而推迟保持/急停。 |
| `SENSOR_CAPTURE` | 专用 CPU 核组/线程；相机 DMA/时间同步 | 每个传感器单生产者环形缓冲 | 累积无界原始帧或阻塞控制。 |
| `STATE_PREP` | 专用 CPU worker；固定输入 shape、预分配 pinned buffer | 只消费最新完整、时间一致窗口 | 处理已过期窗口以“补历史”。 |
| `GPU_INFERENCE` | 单一高优先级 CUDA stream/专属 worker | 单飞、有 deadline；旧请求可丢弃 | 与训练、生成模型或低优先级视觉任务争抢 GPU。 |
| `PLAN/LOGGER/LLM` | 低/普通优先级 CPU 线程 | 有界异步队列、批量写盘/上传 | 在持锁的安全/伺服路径上执行。 |

SPSC（单生产者单消费者）环形缓冲与“**latest-only**”语义很重要：当推理落后时，系统应丢弃过期状态，不应试图清空历史队列而让控制永远追赶过去。所有消息带有单调时间戳、`state_id`、动作 schema 与传感器同步版本。

### 3.2 功耗与热降级状态机

功耗模式、风扇曲线、散热设计和 CPU/GPU clocks 必须按目标 JetPack/BSP、机器人电源预算和热设计设置并实测。生产策略不应把 `jetson_clocks` 或最高功耗档当作“解决热问题”的替代方案；它们可以用于建立上界 benchmark，但热稳态和电池/供电余量才决定可持续运行。

| 状态 | 触发例子（阈值需现场标定） | 模型/调度动作 | 机器人动作 |
|---|---|---|---|
| `THERMAL_NORMAL` | 温度、功耗和 p99 在预算内 | 正常候选数/时域、正常模型频率 | 允许受限自主。 |
| `THERMAL_GUARD` | 滑动窗口 p99 上升或温度接近警戒线 | 降低候选数/时域、启用特征缓存、暂停低优先级日志/上传 | 降低速度，仅继续低风险已批准窗口。 |
| `THERMAL_DEGRADED` | 持续超时、明确热降频或电源余量不足 | 切换更小学生/FP16 engine 或降低世界模型频率；禁止新进取动作 | 观察、保持、请求用户/人类接管。 |
| `THERMAL_RECOVERY` | 连续健康窗口和温度回落 | 逐步恢复，不一次性打开所有候选/时域 | 重新规划后才恢复低速动作。 |

热策略的优先级应是：先降低低优先级任务和推理预算，再降低机器人自主权限；不能通过放宽轨迹 TTL、跳过安全门或继续使用过期预测来换取表面帧率。

### 3.3 CUDA 与 CPU 调度细节

1. 在部署启动时预热 ORT/TensorRT engine、分配固定张量和 CUDA stream，避免首次任务触发 engine build 或频繁内存分配。
2. 视觉编码和候选预测共享一次状态编码；动作候选只在预测器中 batched rollout。
3. `GPU_INFERENCE` 单飞运行，避免多个低价值 batch 排队。高优先级模型流与日志/可视化/上传隔离。
4. 将传感器、状态准备和日志绑定到不同 CPU 核组时，应根据实际核心数、I/O IRQ、厂商控制线程和 `tegrastats`/系统 profiling 决定亲和性；不要从通用示例硬编码 CPU 编号。
5. 使用时间戳与触发器测量完整流水线而非仅 `session.run()`；记录温度、频率、功耗、GPU 利用率、队列深度和每阶段 p99。

## 4. 柔软物体等长尾物理交互：安全主动学习闭环

### 4.1 持续高不确定性不是探索许可

柔软物体的形变、摩擦、遮挡和接触动力学可能同时导致 JEPA 的潜在预测残差和不确定性升高。应通过多个信号共同判断，而不是一个高方差值就触发再训练：

> \(S_{hard}=w_u\,\overline{U}+w_r\,\overline{R}+w_n\,N+w_c\,C\)

其中 \(\overline{U}\) 是连续窗口的不确定性，\(\overline{R}\) 是预测—观察残差，\(N\) 是与已有回放库的表征新颖度，\(C\) 是安全门拒绝、抓取滑移、接触异常等约束事件。只有 `S_hard` 连续超过阈值、且不存在硬安全告警时，才将该片段标为候选难例；存在接触/人员/硬件风险时，优先 `SAFE_HOLD`，不再继续主动尝试。

具身主动学习的研究表明，可用不确定性（如模型集成熵）指导机器人选择更有信息量的交互或专家查询，从而改善样本效率。[4] 但该结果不应被解释为允许家用机器人在未知柔软物体上进行开放式试错。

### 4.2 四级难例采集策略

| 级别 | 条件 | 允许动作 | 记录内容 |
|---|---|---|---|
| L0：被动记录 | 任务中有一次短暂不确定性升高，但仍安全 | 不改变当前策略 | 传感器、本体、动作、预测、残差、环境版本。 |
| L1：保守复观测 | 持续不确定性，且无危险 | 停止运动、改变视角、低速观察、请求用户确认物体类别 | 多视角视频、物体区域、接触前状态。 |
| L2：人类指导采集 | 用户/操作员在场，任务区域已安全定义 | 遥操作/示教、有限的低力预注册探测技能 | 完整轨迹、力/触觉、专家标签、失败原因。 |
| L3：仿真/离线扩增 | 已获得审核过的核心难例 | 数字孪生/软体仿真中的反事实扰动 | 合成来源、参数、sim-real 适用边界。 |

所有 L2/L3 数据应标明来源和质量等级，不能把世界模型生成的视频或未经审核的逆动力学动作直接当作高质量真实标签。

### 4.3 从难例到增量微调的离线门控

```text
边缘端：持续高不确定性
  → LOCAL_HOLD / CAUTIOUS_OBSERVE
  → 写入加密、可追溯的 CandidateEpisode（不更新在线权重）
  → 去重、聚类、隐私/质量审核、必要时人工示教或标签
  → Hard-example buffer + 历史代表性 replay buffer
  → 离线/中心训练：adapter 或小预测器增量微调 + 教师蒸馏
  → 离线回放与传感器故障注入
  → 仿真柔软物体/接触压力测试
  → 影子模式对比旧/新模型
  → 小范围 canary、可回滚发布
```

增量微调应优先训练小型动作条件预测器、适配器和事件/不确定性头，避免频繁全量改动视觉骨干。训练目标混合：新难例监督、历史 replay、旧模型蒸馏、预测误差—不确定性校准和安全事件分类。每个模型版本都必须通过旧任务回归，以避免学会“布料折叠”后遗忘硬物抓取、放置和门操作。

| 环节 | 必须具备的门控 | 禁止事项 |
|---|---|---|
| 难例入库 | 数据完整、时间同步、动作实际执行记录、传感器/隐私策略合规 | 只存高不确定性标量、不存上下文和原因。 |
| 标注/审核 | 人工或高置信自动标签；区分物体、形变、接触和系统故障 | 把 GPU 超时或相机掉帧误标为新物理规律。 |
| 离线微调 | replay、蒸馏、版本化 schema、独立验证集 | 在正在运行的 Jetson 上直接更改在线权重。 |
| 模型候选 | 安全回退、OOD、校准、原任务、柔软物体任务均达标 | 仅凭训练 loss 降低便替换模型。 |
| 发布 | 影子模式、canary、回滚包、人工审批 | 在未知接触任务中一次性全量启用。 |

## 5. 可执行的 90 天优先级

| 时间段 | 首要工作 | 成功判据 |
|---|---|---|
| 第 1–2 周 | 建立 Jetson 热稳态端到端 trace；定位 p99 热点；固定安全 TTL | 知道 p99 分解，而不只是平均 `session.run()`。 |
| 第 3–4 周 | FP16 静态 engine、特征缓存、latest-only 队列、热降级状态机 | 热稳态下无旧请求积压；超时只触发保持。 |
| 第 5–7 周 | 训练小学生模型与蒸馏；逐轴结构化裁剪 | 学生模型在正常/OOD/故障集上保持不确定性排序。 |
| 第 8–9 周 | 目标域选择性 INT8、engine 覆盖/缓存验证 | p99 下降且回退召回不下降。 |
| 第 10–12 周 | 难例采集协议、柔软物体影子模式、离线增量训练与 canary | 新版本在 replay 与压力测试中无灾难性遗忘。 |

## 参考资料

[1]: [NVIDIA, *Jetson Orin 系列：Power and Performance*, Jetson Linux Developer Guide](https://docs.nvidia.com/jetson/archives/r36.2/DeveloperGuide/SD/PlatformPowerAndPerformance/JetsonOrinNanoSeriesJetsonOrinNxSeriesAndJetsonAgxOrinSeries.html)

[2]: [NVIDIA, *Sparsity in INT8: Training Workflow and Best Practices for TensorRT Acceleration*](https://developer.nvidia.com/blog/sparsity-in-int8-training-workflow-and-best-practices-for-tensorrt-acceleration/)

[3]: [PyTorch TensorRT, *Post Training Quantization*](https://docs.pytorch.org/TensorRT/user_guide/ptq.html)

[4]: [Li & Silver, *Embodied Active Learning of Relational State Abstractions for Bilevel Planning*, CoLLAs 2023](https://proceedings.mlr.press/v232/li23a.html)
