# 软体抓取增量训练与发布：安全边界

1. 影子模式只读取相同的状态、候选动作和传感器回放/实时镜像，生成模型预测、风险与推荐结果；它不向机器人驱动器、轨迹队列或安全控制器写入任何控制命令。
2. 灰度发布只允许在已通过离线、仿真、故障注入和影子模式门控的候选模型中选择；且第一阶段只能影响低速、低力、短 TTL、预注册的原子技能。
3. 安全触发、厂商控制器告警、人/宠物近场、碰撞、急停、过期状态或模型进程异常始终优先于候选模型的评价结果，直接保持/停机并冻结发布扩大。
4. 模型版本、数据清单、软体物理先验、动作 schema、传感器/标定版本、ONNX/TensorRT engine、评测报告和审批记录必须共同版本化；任一不匹配均阻止部署。
5. 自动门控可决定“继续影子、暂停扩大、回滚到基线”，但不允许自动忽略安全失败或在生产机器人上直接执行在线权重更新。
