# NCCL 动态训练与 Human-in-the-Loop：安全边界

1. 同步 DDP/NCCL 的一个优化步必须让所有参与 rank 以相同参数版本进入 all-reduce。不得让慢节点静默跳过更新，也不得在同一 process group 中混用不同梯度累积步数而不做明确的加权归一化。
2. 负载均衡优先调整“每 rank 每次更新前的微批数量、样本 token/帧预算、预取与分桶”，而不是在单个 all-reduce 期间临时改变 world size。成员变化必须在安全检查点发生，并由弹性重启/新 process group 重新初始化。
3. 有效全局 batch 应显式记录并保持可解释：`sum_r(micro_batch_r × local_accumulated_microsteps_r)`。当该值改变时，需要学习率/调度器策略、验证基线和 run manifest 共同版本化。
4. 人工审核只能修正隔离数据的标签、任务语义、接触/事件边界、物理先验提案、隐私/许可状态或处置决定；审核者不能直接批准运动命令、放宽在线安全阈值或绕过影子/灰度门控。
5. 人工更正必须包含审核者角色、时间、原始证据哈希、修订版本、理由和复核状态。只有双重审核或预先授权的低风险类别才可进入可训练数据集提交。
6. 高风险柔性物体、接近人类、疑似硬件/传感器故障和不确定性保持漏失的难例默认保持隔离，不进入自动微调队列，直至根因和安全处置被明确批准。
