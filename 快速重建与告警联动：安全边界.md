# 快速重建与告警联动：安全边界

1. 优化目标是缩短从故障识别到“新 worker group 通过状态/计划验证”的时间，不是让旧 NCCL communicator 或未确认 update 继续运行。任何 in-flight 梯度、optimizer、EMA、scaler/FP8 metadata 和 cursor reservation 都必须以 COMMITTED checkpoint/ledger 为准。
2. Prometheus/Grafana/Alertmanager 联动只能执行非破坏性控制面动作：冻结新的 UpdatePlan、把 job 标记为 SUSPECT、写审计事件、捕获现有工件、创建通知/人工升级请求。不得直接修改网络、重启共享节点、变更 rendezvous、推进 cursor、批准模型发布或控制机器人。
3. 大规模抢占或分区应使用指数退避、抖动、分批恢复、checkpoint 读取限流和控制面容量保护，避免所有重启 worker 同时冲击 rendezvous、对象存储和 metrics 后端。
4. 任何“快速路径”须先验证 checkpoint hash、precision state、global cursor、topology/work manifest 与新 group identity；验证未通过即停止，而不是降级为使用旧 rank-local 状态。
5. 演练脚本默认 dry-run；若将来接入外部告警或基础设施端点，仍须在服务端独立验证工作负载身份、目标环境、批准、TTL、白名单与幂等性。
