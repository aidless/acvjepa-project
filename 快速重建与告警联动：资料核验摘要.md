# 快速重建与告警联动：资料核验摘要

1. PyTorch `torchrun` 2.13 文档说明：worker/agent/node 失败会导致 worker group 重启；成员变化会停止现存 worker 并生成新的 `RANK`/`WORLD_SIZE`。因此重建优化必须以 checkpoint 和新 group identity 为中心，不能缓存旧 rank。来源：https://docs.pytorch.org/docs/2.13/elastic/run.html
2. Prometheus alerting rules 文档说明：`for` 用于 pending 到 firing 的连续条件窗口；`keep_firing_for` 可减少抖动/假恢复；Prometheus 将告警交由 Alertmanager 进行汇总、限频、静默和依赖处理。来源：https://prometheus.io/docs/prometheus/latest/configuration/alerting_rules/
3. Prometheus recording rules 文档说明：recording rules 可预计算频繁或昂贵表达式；规则组按统一评估周期执行，`promtool check rules` 可作语法检查；可使用 `limit` 和 `query_offset` 管理过多 series 与采集延迟。来源：https://prometheus.io/docs/prometheus/latest/configuration/recording_rules/
4. Alertmanager 配置文档说明：路由可按 label 分组、限频、静默/抑制并发送到 receiver；webhook receiver 是通知/集成接口。该接口应接向受限、可审计的控制面，不应授予网络或集群任意执行权限。来源：https://prometheus.io/docs/alerting/latest/configuration/
