# 可恢复生成与端到端微调：恢复与安全边界

1. 每个 SimJob 的唯一键是 `job_id + simulator_version + DR_config_version + data_contract_version`。相同键的完成 commit 可被安全复用；不同配置绝不能因为同名 job 而覆盖。
2. worker 只对持有未过期租约的 job 写入；租约超时可被回收。完成状态必须通过本地质量门控、产物哈希和远端 episode commit 验证后才可标为完成。
3. 网络中断时，客户端只重试幂等的 staging 上传和 commit 验证，采用指数退避/抖动与有限重试次数。远端 commit 已存在且哈希一致时视为成功；哈希冲突时隔离，不覆盖。
4. 数据集提交是不可变对象。训练工作流只消费经过验证的 dataset commit，绝不扫描 staging 路径、临时目录或孤立 rank 输出。
5. 端到端编排只能自动执行可确定性的校验、生成、训练候选、离线/仿真/边缘评测和创建影子候选；进入 canary/生产仍需既有门控与授权。任何安全失败、质量拒绝、版本不兼容或恢复冲突都停止下游阶段。
6. 用于长期无人值守的大规模仿真、对象存储同步和 GPU 训练的执行环境必须是具有持久状态与故障观测的 CI/作业编排器或长期运行服务；本会话沙箱仅用于参考实现与测试，不应承担生产后台任务。
