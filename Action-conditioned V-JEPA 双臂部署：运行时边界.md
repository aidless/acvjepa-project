# Action-conditioned V-JEPA 双臂部署：运行时边界

## 不可违背的控制权分层

1. 机器人厂家安全控制器、急停、关节限位和碰撞保护拥有最高优先级，且不依赖 GPU、LLM、JEPA 或网络。
2. 本地伺服/阻抗控制器执行经批准的短期参考轨迹，不等待世界模型或 LLM 的响应。
3. Action-conditioned V-JEPA 只预测受限动作块的短期潜在后果、风险和不确定性，并影响候选排序与规划窗口；它不直接生成电机电流或关节力矩。
4. LLM 仅在任务时标生成/修改 PlanSpec、解释异常和请求确认；它不在运动控制关键路径内。

## 实时性原则

所有频率和时延以具体机械臂、控制器、传感器、GPU、动作空间和安全认证要求的实测 99 分位为准，不能从模型参数量直接保证。系统必须将 GPU 模型推理与安全/伺服环解耦；任何观测过期、推理超时、预测高不确定性、通信抖动或模型进程异常都应触发保持、撤退或回退到经验证的确定性局部控制，而不是继续执行旧的高层计划。
