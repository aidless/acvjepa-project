# LLM × JEPA 世界模型混合 Agent：可实施架构蓝图

## 1. 设计目标与一句话方案

这套架构面向**连续、视觉化、部分可观测且行动有成本**的环境，例如机械臂操控、视觉导航、工业检查和自动驾驶仿真。它的目标不是让 LLM 直接“思考后控制”，而是建立一个职责明确的闭环：**LLM 把人类目标编译为受约束的任务规范；JEPA 世界模型将观测压缩为预测性状态并模拟行动后果；规划器在潜在状态空间选择短程动作；安全层在动作落地前后执行硬验证。**

> **核心原则：LLM 决定“为什么做、做什么、哪些约束不能违反”；JEPA 与规划器决定“在当前环境中下一步如何做”；安全层决定“现在是否允许做”。**

这一职责分离与当前公开研究方向一致：V-JEPA 2-AC 将视频预训练表示后训练为动作条件的潜在世界模型用于机器人操控；JEPA-WMs 则公开了世界模型训练与物理规划评估设施。[1] [2]

## 2. 系统全景

下图的可编辑 D2 源文件与本文一同交付。系统分为语义与任务层、状态与预测层、规划验证执行层。任何真正改变外部环境的调用都必须从底部两层通过；LLM 不拥有绕过状态估计、预测、权限或安全门的直接执行权限。

| 层级 | 核心模块 | 主要输入 | 主要输出 | 责任边界 |
|---|---|---|---|---|
| **语义与任务层** | LLM 编排器、检索、语义记忆 | 用户目标、业务规则、任务历史 | `PlanSpec`、澄清问题、成功判据 | 理解意图、分层分解、知识调用；不直接下发原始控制量。 |
| **状态与预测层** | 观测适配器、JEPA 编码器、动作条件世界模型、不确定性模块 | 传感器流、截图、日志、已执行动作 | 潜在状态 `z_t`、候选未来轨迹、风险/置信度 | 维护对环境的预测性表征；不决定业务授权。 |
| **规划验证执行层** | MPC/采样规划器、安全验证器、执行器、事后校验器 | `PlanSpec`、`z_t`、预测轨迹、系统约束 | 批准动作、执行结果、偏差事件 | 在约束下选择和执行动作；拒绝风险过高或不可验证的计划。 |

### 2.1 为什么要显式区分三层

单体 Transformer Agent 往往把自然语言理解、环境估计、计划生成和工具调用压缩进同一上下文窗口。这在低风险、离散工具任务中很高效，但在连续环境中容易出现三个问题：任务历史成为非结构化文本、行动后果只能通过语言猜测、失败后只能把报错重新提示给模型。混合架构把这三类问题拆开处理：**语言事实放在语义记忆，环境事实放在状态估计，行动事实放在已验证的执行轨迹。**

## 3. 关键组件与接口协议

### 3.1 LLM 编排器：只输出受约束的任务规范

LLM 的输出不是自由文本动作，而是类型化 `PlanSpec`。它由任务编译器校验，任何未知工具、未声明动作、缺少成功判据或超越风险上限的请求都会被拒绝或要求澄清。

```json
{
  "mission_id": "m-2026-001",
  "goal": {
    "natural_language": "将蓝色杯子放入左侧托盘",
    "goal_type": "visual_object_placement",
    "goal_embedding_ref": "g://blue-cup-left-tray"
  },
  "subgoals": [
    {"id": "s1", "intent": "定位目标与托盘", "done_when": "object_pose_confidence >= 0.9"},
    {"id": "s2", "intent": "抓取杯子", "done_when": "grasp_verified == true"},
    {"id": "s3", "intent": "放入托盘", "done_when": "in_target_region == true"}
  ],
  "constraints": {
    "forbidden_regions": ["human_workspace"],
    "max_contact_force_n": 8,
    "must_keep_upright": true,
    "confirmation_required": false
  },
  "action_grammar_ref": "robot-v3",
  "success_criteria": ["cup_in_left_tray", "no_safety_violation"],
  "risk_tier": "medium"
}
```

LLM 还负责解释异常、调用知识库、选择高层技能和在必要时向用户澄清。它不应把“也许可以”直接转为执行命令；而应把不确定性转化为可检验问题，例如“杯子是否被遮挡”“左侧托盘是否可达”。

### 3.2 JEPA 状态编码器：将历史观测转为预测性状态

令多模态观测为 \(o_{t-k:t}\)，编码器将其压缩为 \(z_t=E_\phi(o_{t-k:t})\)。在视觉 Agent 中，\(o\) 可含连续图像、深度、触觉和机器人本体状态；在软件 Agent 中，\(o\) 应换成结构化 UI 树、DOM、API 响应、事件日志与数据库版本，而不是仅用屏幕截图。

此状态必须同时保存两种信息：一是供世界模型预测的**稠密潜在表征**，二是供规则、安全和 LLM 使用的**可解释状态摘要**。例如对象位姿、抓取状态、权限态、库存数量、当前页面提交态等。前者用于连续预测，后者避免系统在关键限制上依赖不可解释向量。

### 3.3 动作条件 JEPA 世界模型：预测可行动后果

世界模型不必渲染未来像素，而是在潜在空间预测：

> \(\hat{z}_{t+1:t+H}=F_\theta(z_t, a_{t:t+H-1}, g, c)\)

其中 \(a\) 是候选动作序列，\(g\) 是任务/子目标表示，\(c\) 是上下文约束。实际系统应输出的不只是单条预测轨迹，还包括下列字段。

| 输出字段 | 含义 | 在运行时如何使用 |
|---|---|---|
| `future_latents` | 候选动作下未来的潜在状态序列 | 由规划器比较候选路径是否接近目标。 |
| `goal_progress` | 每一步相对目标的进展 | 形成任务代价与终止条件。 |
| `constraint_risk` | 碰撞、越界、不可逆操作、策略违例的风险 | 传给安全门，禁止以高得分掩盖高风险。 |
| `uncertainty` | 模型分歧、分布外分数、预测残差 | 决定是否减小动作幅度、增大观测频率、请求人类确认或重新规划。 |
| `expected_observables` | 可抽取的预期事实，例如“夹爪闭合后应检测到物体” | 为事后校验器提供可执行断言。 |

V-JEPA 2-AC 已展示“视频预训练 + 少量机器人轨迹后训练”的动作条件世界模型路径；它是这个组件的现实参照，而不是完整方案的充分证明。[1]

### 3.4 规划器：短程预测、滚动执行、长程分层

规划器采用模型预测控制（MPC）或采样式规划。它不一次性承诺完成整项长任务，而是从当前状态评估长度为 \(H\) 的候选动作序列，执行第一个或前几个低风险动作后立刻观察与重规划。

可将每条候选轨迹 \(\tau\) 的代价定义为：

> \(J(\tau)=\lambda_g D(\hat{z}_{t+H},g)+\lambda_c C_{\text{constraint}}(\tau)+\lambda_u U(\tau)+\lambda_e C_{\text{effort}}(\tau)-\lambda_i I(\tau)\)

其中第一项衡量到目标的潜在距离，第二项为硬/软约束代价，第三项惩罚高不确定性，第四项计算时间、能耗或工具成本，第五项奖励能减少关键不确定性的“信息性动作”。在有遮挡或状态模糊时，系统不应盲目追求立即完成目标，而可主动选择观察、移动相机、读取传感器或查询系统状态。

### 3.5 安全验证器：把“预测正确”与“获准执行”分开

世界模型认为可行，不等于系统有资格执行。安全验证器使用确定性规则、权限系统、边界检测和人工确认，将规划动作转换为真正的执行命令。建议同时配置以下四道门。

| 门控类型 | 示例 | 拒绝后的动作 |
|---|---|---|
| **语法与权限门** | 动作是否属于允许的 DSL；调用者是否具备删除/提交/支付权限 | 拒绝，并将缺失权限回传给 LLM。 |
| **前置条件门** | 机械臂是否握住物体；网页是否处于草稿而非已提交态 | 重新观测或转到恢复子任务。 |
| **预测风险门** | 碰撞概率、不确定性、不可逆性是否超过阈值 | 降低速度、缩短规划时域、请求确认或改走保守路径。 |
| **事后验证门** | 实际观测是否满足预测的抓取/放置/写入结果 | 记录偏差、冻结后续高风险动作并触发重规划。 |

## 4. 运行时协同：从用户目标到可验证的行动闭环

### 4.1 双时标控制

LLM 与 JEPA 不应以同一频率运行。推荐采用**双时标架构**：LLM 在低频、高语义层面运行，更新任务图、约束和成功定义；JEPA 与规划器在高频环境循环中运行，处理状态估计、短期后果预测和控制。这样既避免让 LLM 承担实时控制，又不要求世界模型理解所有业务语言。

| 时标 | 典型频率 | 主导模块 | 产物 |
|---|---:|---|---|
| 任务时标 | 任务开始、子目标完成、异常发生时 | LLM 编排器 | 子目标图、约束、可调用技能、恢复策略。 |
| 规划时标 | 每个子目标内的数秒或数个离散动作 | MPC / JEPA 世界模型 | 候选轨迹、代价、最优短动作序列。 |
| 控制时标 | 每步动作或每个控制周期 | 执行器与安全验证器 | 已批准动作、紧急停止、传感器采样。 |
| 校验时标 | 每次动作后 | 状态估计器与验证器 | 预测残差、状态更新、继续/回退/重规划决定。 |

### 4.2 标准执行循环

1. **任务编译。** LLM 从用户目标、业务规则和语义记忆生成 `PlanSpec`；若成功条件或风险边界无法形式化，先询问用户而不执行。
2. **状态同步。** 观测适配器采集 \(o_t\)，JEPA 编码器输出 \(z_t\)，状态摘要器更新可解释状态表。
3. **候选产生。** LLM 或技能库只给出符合动作 DSL 的候选技能/动作空间，而不直接确定低层控制轨迹。
4. **潜在前瞻。** JEPA 世界模型对每条候选动作序列预测 \(\hat z\)、目标进展、风险、不确定性与预期可观察事实。
5. **约束规划。** MPC/采样规划器最小化 \(J\)，并把最优候选与安全证明材料提交安全门。
6. **批准与执行。** 安全层检查权限、前置条件、硬约束、置信度和是否需要人工确认；通过后仅执行短动作窗口。
7. **事后校验。** 系统读取真实 \(o_{t+1}\)，比较 \(E(o_{t+1})\) 与 \(\hat z_{t+1}\)，并逐项校验预期事实。
8. **偏差处理。** 若预测残差、风险或状态冲突超过阈值，执行器停在安全状态；LLM 收到结构化失败类型，重新选择技能、修改目标或请求用户澄清。

### 4.3 LLM 与世界模型的协同消息契约

| 发送方 → 接收方 | 消息 | 必填字段 | 规则 |
|---|---|---|---|
| LLM → 规划器 | `PlanSpec` | `goal`、`constraints`、`success_criteria`、`action_grammar_ref`、`risk_tier` | 必须通过 JSON Schema 与策略检查。 |
| 状态层 → 规划器 | `StateEstimate` | `state_id`、`z_ref`、`symbolic_facts`、`freshness`、`state_confidence` | 观测过期或置信度不足时不可进入高风险规划。 |
| 世界模型 → 规划器 | `PredictionReport` | `action_sequence`、`future_latents`、`goal_progress`、`uncertainty`、`constraint_risk` | 必须可追溯到版本化模型与状态快照。 |
| 规划器 → 安全层 | `ActionProposal` | `first_action`、`expected_effects`、`rollback`、`risk_summary` | 不带回滚或校验断言的高风险动作应拒绝。 |
| 验证器 → LLM/状态层 | `OutcomeEvent` | `observed_effects`、`prediction_error`、`failure_class`、`evidence_ref` | LLM 只能依据该事件改变任务计划，不能覆盖原始环境事实。 |

## 5. 一个工作示例：将杯子放入托盘

用户说“把蓝色杯子放入左侧托盘”。LLM 不下发机械臂坐标，而是生成物体定位、抓取、移动、放置四个子目标以及“保持直立、避开人类工作区、接触力不超过阈值”等约束。视觉状态层从连续相机帧和夹爪状态编码出 \(z_t\)，并在可解释状态表中记录杯子、托盘、障碍物与人手的位置置信度。

规划器从“接近—抓取—抬升—移动—放置”动作原语中采样候选序列。JEPA 世界模型预测每个序列的未来潜在状态；若某路径在潜在空间接近“杯在托盘中”的目标表征但碰撞风险或不确定性高，安全层仍会拒绝。系统选择风险最低的短动作窗口，执行后用视觉和夹爪触觉验证“已抓住”这一可观察事实。若杯子滑动导致实际状态偏离预测，系统停下，更新状态，不把“成功抓取”继续当成文本假设，而是触发局部恢复计划。

## 6. 软件 Agent 的等价改造

对于浏览器、数据库和业务流程 Agent，最不应做的是把视频 JEPA 生搬硬套。正确做法是将 JEPA 的思想迁移为**抽象状态预测**：编码器输入 DOM/UI 树、表单字段、API 响应、数据库版本和事件日志；世界模型预测“执行此工具调用后，哪些结构化状态会改变”；规划器在有限的状态转移图上做约束搜索；安全门控制提交、删除、支付、发布和权限变更。

在此场景中，JEPA 的价值不是“看懂截图”，而是学习或维护与决策相关的状态表示，例如草稿/已提交态、资源存在性、访问权限、数据版本与事务依赖。若系统状态本来就明确且规则稳定，确定性状态机或形式化工作流往往优于训练世界模型；只有当环境呈现高维、噪声、部分可观测或规则难以手工建模时，学习型潜在世界模型才具有明显优势。

## 7. 训练与更新：不要把整个系统端到端混成一个黑箱

推荐采用**模块化、分阶段、可回归评测**的训练路径。原因是 LLM、世界模型、策略/规划器失败时的责任完全不同：LLM 可能误解目标，世界模型可能失配，规划器可能探索不足，执行器可能发生硬件/工具错误。若把这些模块一次性端到端训练，系统虽然可能在训练集上获得较高回报，却很难定位失败根因，也更难安全更新。

| 阶段 | 训练对象 | 数据与目标 | 产物 | 上线条件 |
|---|---|---|---|---|
| **P0：先建显式闭环** | 动作 DSL、状态摘要器、规则校验、LLM `PlanSpec` 编译器 | 专家流程、工具契约、规则集、失败日志 | 可观测、可回放的 Agent；即便没有 JEPA 也能运行 | 每项高风险动作均有前置条件和事后断言。 |
| **P1：自监督表征学习** | JEPA 视觉/多模态编码器 | 大量未标注观察序列：视频、传感器流、UI/事件序列 | 可预测的潜在状态 \(z\) | 在跨场景状态任务上优于冻结视觉/规则基线。 |
| **P2：动作条件后训练** | 世界模型 \(F_\theta\) 与不确定性头 | 带动作、结果、失败与恢复标记的轨迹 | \((z,a)\rightarrow\hat z\) 及风险/不确定性预测 | 必须通过干预式、分布外和安全约束验证。 |
| **P3：规划与策略耦合** | MPC、价值/目标距离、候选动作生成器 | 任务成功轨迹与反事实回放 | 短程滚动规划器 | 相比无世界模型或随机动作先验，稳定提升闭环成功率。 |
| **P4：受控真实部署** | 仅小范围校准，不重写全局策略 | 影子模式、人类监督、小动作窗口 | 领域适配与安全阈值 | 通过回归套件后逐步放宽权限；任何退化自动回滚。 |

### 7.1 世界模型训练的关键数据结构

每条轨迹不应只是 \((o_t,a_t,o_{t+1})\)。为了让世界模型真正服务于 Agent，建议将轨迹记录为：

| 字段 | 作用 |
|---|---|
| `observation_window` | 原始视觉/传感器/结构化系统状态，用于重放和多模态状态估计。 |
| `action` 与 `action_parameters` | 使世界模型学习行动条件，而不是仅学习被动时间相关性。 |
| `preconditions`、`permissions` | 把状态可行性与授权可行性分开。 |
| `expected_effects` 与 `observed_effects` | 形成可监督的动作后果和校验断言。 |
| `failure_class` 与 `recovery_action` | 让系统学习失败后如何转入安全状态，而不只是学习成功示范。 |
| `intervention_id` | 标识同一初始状态下不同动作的反事实分支；这是检验行动因果性的关键。 |
| `environment_version`、`model_version` | 保证离线回放、再训练和安全审计可复现。 |

训练目标可由三部分组成：JEPA 潜在预测损失、与可观测事实对齐的辅助损失、以及不确定性校准损失。不要仅优化潜在距离：如果模型在潜在空间“看似接近”目标，却经常错过抓取、越界或权限错误，它对 Agent 没有价值。

### 7.2 LLM 不需要从头为 JEPA 训练

实践中，先冻结或轻量微调 LLM，使它稳定输出 `PlanSpec` 和结构化异常处置，比训练一个端到端多模态巨模型更可控。LLM 与 JEPA 之间的连接由三类对象完成：**目标嵌入**、**动作 DSL**、**可解释状态摘要**。LLM 把“把蓝杯放到左托盘”映射为目标与约束；JEPA 把当前观察和动作候选映射为未来状态与风险；规划器只在动作 DSL 所允许的空间内搜索。这样能够替换其中任意模型，而不会破坏全系统的行动协议。

## 8. 评估：必须以闭环、分布外与安全为中心

单独的 VQA、视频分类、语言基准或离线动作预测准确率，都不足以证明混合 Agent 的长程能力。评价设计应固定动作空间、传感器、任务分布和安全规则，同时比较四个系统：**LLM-only、JEPA-only planner、LLM + 无世界模型执行器、完整混合 Agent**。只有这样才能识别世界模型是否真的带来了增益。

| 评估层 | 核心指标 | 关键对照或扰动 |
|---|---|---|
| **状态与预测** | 潜在预测误差、目标进展预测误差、事件预测 F1、不确定性校准误差 | 光照/背景变化、遮挡、传感器延迟、未见物体或网页版本变化。 |
| **规划与泛化** | 任务成功率、规划时间、动作数、重规划次数、回退成功率 | 更长任务链、目标位置变化、无效工具、动作失败、中途环境改变。 |
| **安全与可靠性** | 约束违反率、危险动作拦截召回率、误拒绝率、紧急停止后恢复率 | 碰撞风险、权限缺失、数据版本冲突、不可逆操作请求。 |
| **LLM 编排质量** | `PlanSpec` schema 通过率、目标澄清率、工具/技能选择正确率、异常分类准确率 | 模糊指令、冲突约束、注入式无关文本、过期知识。 |
| **系统价值** | 每任务成本、时延、人工接管率、模型更新后回归失败率 | 统一算力预算与相同安全阈值下的消融比较。 |

**最重要的验收问题是：** 在未见环境、有限真实交互和相同安全约束下，完整混合架构能否比 LLM-only 或反应式策略，以更低的违规率完成更长的任务？如果答案是否定的，JEPA 模块就仍只是更好的表征器，而没有证明其作为 Agent 世界模型的新增价值。

## 9. 安全、失败处理与降级策略

高风险系统应把不确定性作为一等输入，而不是在模型输出后加一句“请谨慎”。推荐采用风险加权的降级状态机。

| 触发条件 | 系统状态 | 必须执行的动作 | LLM 的角色 |
|---|---|---|---|
| 低预测误差、低不确定性、约束满足 | `NORMAL` | 执行短动作窗口并持续校验 | 更新任务进展，保持低频监督。 |
| 置信度下降或观测轻度冲突 | `CAUTIOUS` | 缩短规划时域、降低速度/权限、增加观测频率 | 生成信息收集或保守替代子计划。 |
| 高不确定性、分布外、前置条件不成立 | `HOLD` | 停止不可逆动作，执行观察、诊断或恢复技能 | 解释缺失信息并提出澄清问题。 |
| 硬约束冲突、预测与现实严重偏离 | `SAFE_STOP` | 撤销/隔离、记录证据、触发人工接管 | 仅生成事故摘要；不能自行解除安全锁。 |
| 高风险外部操作 | `CONFIRM_REQUIRED` | 展示影响范围，等待明确授权 | 说明行动后果和可选替代路径。 |

还应建立三类“不可被 LLM 覆盖”的硬规则：权限边界、不可逆操作确认、物理/业务安全约束。LLM 可以建议变更计划，但不能伪造观测、降低风险等级或修改批准条件。

## 10. 实施路线：从小闭环开始，而不是先训练大世界模型

### 第一阶段：最小可行混合 Agent

选择一个单一、可复现环境，例如 Push-T、视觉抓取、单页面表单工作流或小型仓库操作。首先实现 `PlanSpec → StateEstimate → ActionProposal → OutcomeEvent` 四个数据对象；让 LLM 只能发出类型化任务与技能请求；每一个动作都要有前后状态快照、预期效果和事后断言。此阶段甚至可先用规则转移模型或已有仿真器替代 JEPA，以先验证接口和评估体系。

### 第二阶段：引入预训练 JEPA 与动作条件预测

复用 V-JEPA 2-AC、JEPA-WMs 或 EB-JEPA 的结构思路，对自己的环境轨迹执行动作条件后训练。先让世界模型只参与**旁路影子评估**：它预测执行结果，但不控制执行。只有在预测校准、风险提示和分布外检测达到预设阈值后，才让它影响规划器的候选排序。

### 第三阶段：让世界模型参与滚动规划

将动作窗口限制在短时域，使用 MPC 每个窗口后重新观测。LLM 只在子目标完成、异常、风险升级或需要重新分配资源时介入。与无世界模型的 LLM Agent 做严格消融：比较成功率、违规率、平均动作数、恢复能力与人工介入率。

### 第四阶段：扩展到长程任务与真实环境

逐步扩大任务组合、环境变化和行动权限，而不是一次性扩大模型规模。每一次模型、数据或提示更新都必须重跑固定回归集；对预测误差上升、风险检出率下降或恢复成功率变差建立自动回滚。此阶段的关键资产不是单一大模型，而是版本化轨迹数据、失败分类体系、约束库与可重复的闭环评测。

## 11. 关键决策摘要

| 决策 | 推荐方案 | 原因 |
|---|---|---|
| 高层任务规划 | LLM 生成结构化 `PlanSpec`，而非自由文本动作 | 可验证、可审计、可替换。 |
| 环境表征 | JEPA 潜在状态 + 可解释状态摘要并行 | 兼顾连续预测能力和安全/业务可解释性。 |
| 长程规划 | 分层任务图 + 短程 MPC 滚动预测 | 减少世界模型误差长期累积。 |
| 行动选择 | 动作 DSL/技能库约束候选空间 | 防止 LLM 将语言幻觉直接转换为控制命令。 |
| 安全机制 | 预测风险与确定性硬约束双重门控 | 避免“模型认为可行”被误当成“系统获准执行”。 |
| 模型更新 | 影子模式、闭环回归、渐进放权 | 控制真实环境中的分布漂移与回归风险。 |

## 12. 已知限制与参考资料

这一设计是可实施的系统蓝图，不是对 JEPA 已有能力的夸大承诺。当前公开工作已证明 JEPA 类表示可用于视频理解、动作条件预测与若干机器人/物理规划设置，但尚未证明一个 JEPA + LLM 系统已经具备通用、长期、鲁棒的现实世界智能。[1] [2] 设计重点因此应放在可证伪评估、保守降级与模块化替换，而不是把“世界模型”当作自动解决因果、规划和安全的标签。

[1]: [Meta FAIR, V-JEPA 2 官方开源仓库](https://github.com/facebookresearch/vjepa2)

[2]: [Meta FAIR, JEPA-WMs 官方开源仓库](https://github.com/facebookresearch/jepa-wms)

[3]: [Assran et al., *V-JEPA 2: Self-Supervised Video Models Enable Understanding, Prediction and Planning*, arXiv:2506.09985](https://arxiv.org/abs/2506.09985)

[4]: [Terver et al., *What Drives Success in Physical Planning with Joint-Embedding Predictive World Models?*, arXiv:2512.24497](https://arxiv.org/abs/2512.24497)

[5]: [VLA-JEPA 官方开源仓库](https://github.com/ginwind/VLA-JEPA)
