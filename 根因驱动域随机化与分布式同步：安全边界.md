# 根因驱动域随机化与分布式同步：安全边界

1. 自动化组件只能基于 `approved_bounds` 在参数范围内调整采样权重、覆盖配额或离散配置选择；任何扩大材质、接触、形变、相机或动作扰动边界的建议均输出为待审批提案，而不自动生效。
2. “视觉域漂移”优先影响光照、材质纹理、相机外参微扰、曝光/噪声、遮挡和预处理配置的覆盖权重；它不自动修改软体动力学先验。
3. “软体物理缺口”优先为经批准的 Young 模量、阻尼、摩擦、接触刚度、几何初态与低速动作回放边界重新加权；超出先验的参数范围或主动探索均需人工审核。
4. 分布式生成的每个任务必须有稳定 job_id、seed、配置版本、仿真器版本和产物哈希。每个 rank 只处理其确定性分片，不重复写同一 job。
5. 同步先在本地完成 quality gate 和 SHA-256；远端上传使用临时键—校验—原子提交清单的两阶段协议。未经验证、质量拒绝或 manifest 不完整的产物不得标为可训练数据。
6. 示例脚本只实现基于兼容 S3 的对象存储 URI 的命令行同步接口；它不读取、修改或暴露任何云端凭据，也不自动连接用户账户。
