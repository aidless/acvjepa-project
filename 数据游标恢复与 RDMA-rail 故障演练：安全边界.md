# 数据游标恢复与 RDMA/rail 故障演练：安全边界

1. 数据游标恢复只可从已完成哈希验证并在训练账本中标记 COMMITTED 的 checkpoint 开始。发生在 in-flight update 中的 work item 只能按明确的 at-least-once 重读策略重新进入候选清单；不得将其记为已训练或拼接局部 rank 状态。
2. RDMA/rail 演练自动化脚本默认只生成计划和干运行结果。真正的网络扰动必须满足：专用测试环境标记、允许的 experiment ID、双人批准、目标节点/接口/rail 白名单、有效期、最大时长和回滚动作均已在签名的实验清单中固定。
3. 脚本不包含直接操作生产网络、共享接口、交换机或任意主机的命令。其执行器仅调用由基础设施团队部署的受限测试执行端点；该端点还必须在服务器端重复验证授权、范围、TTL 与资源标签。
4. 任何检测到生产环境、非白名单接口、缺失批准、过期授权、正在进行机器人控制、未确认 checkpoint、GPU/网络健康告警、已有活跃故障或重复执行时，脚本必须拒绝注入并只输出原因。
5. 故障注入必须有有界 TTL，并在无论成功、异常还是超时的 finally 路径请求回滚。回滚未确认、演练时间超限、watchdog 无法收敛或全状态/数据账本校验失败时，升级人工处置，不自动重试。
6. 演练结果不能直接触发模型发布、修改机器人安全策略、绕过 HITL 或改变生产网络配置。
