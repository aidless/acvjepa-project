# AC-VJEPA：故障注入与分布式训练设计边界

## 测试安全边界

自动化脚本仅向离线回放张量、仿真传感器包、状态新鲜度和模型/规划协调器注入故障。它不连接厂商机械臂 SDK、不发布控制命令，也不替代硬件急停或实机安全验收。实机测试必须先在影子模式验证：模型只读取记录的传感器流和生成事件，不影响运动。

## 故障类别

| 类别 | 注入对象 | 预期系统反应 |
|---|---|---|
| 视觉噪声/亮度/模糊 | 视频张量 | 预测可继续时记录性能退化；不确定性应上升。 |
| 相机掉帧/冻结/时间戳抖动 | 传感器窗口与状态年龄 | 观测过期时禁止新轨迹并进入 `LOCAL_HOLD`。 |
| 本体噪声/量化/延迟 | 本体状态张量 | 重新估计状态；超过新鲜度/健康边界时保持。 |
| NaN/损坏数据 | 任意模型输入 | 拒绝模型输出，生成可审计回退事件。 |
| GPU deadline miss/排队饱和 | 单飞推理器 | 超时结果丢弃；后续请求在旧 GPU 工作完成前返回 `GPU_BUSY`。 |
| 高预测不确定性 | 预测报告/阈值 | 先本地保持，确认后进入 LLM 托管而非直接继续。 |

## DDP 同步原则

每个 rank 必须拥有完全相同的动作 schema、坐标系、归一化统计、相机预处理配置、事件标签词典和模型初始化。数据通过 `DistributedSampler` 进行无重叠分片；梯度通过 DDP 的 all-reduce 在每个梯度累积边界同步；EMA 目标编码器由相同学生权重更新，并定期从 rank 0 广播以避免长时间浮点漂移。检查点只由 rank 0 原子写入，但须包含动作/标定/数据/模型版本以便恢复可追溯性。
