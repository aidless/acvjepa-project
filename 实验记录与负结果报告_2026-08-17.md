# AC-VJEPA 实验记录与负结果报告（2026-08-17）

> 性质：**可复现技术报告**——汇总本项目已执行的预注册实验（H-T2 / H-T4 / H-D1）与工程链路验证（P2、M3 骨架、chain-live、vla-eval 对接），作为 M4 判定报告的前置材料。
> 纪律：所有结论均为**合成/链路级证据**，明确标注局限；**不进入任何路线判定**（X/Y/Z、H1/H0 均未裁决）。
> 复现：脚本与产物路径逐项列出，seed/口径冻结见 `决策记录.md`。

## 1. 已执行实验总览

| 假设 | 判据（预注册） | 结果 | 判定 | 证据 |
|---|---|---|---|---|
| H-T2 投影头容量 | 饱和点 <768：边际收益比 <0.2 且 d<0.2 | w32=-3.505 / w64=-3.300 / w128=-2.719 / w256=-1.732；比率 **4.80**、d=**9.9** | **refuted**（方向反转：窄头更优） | `experiments/ht2_ht4_2026-08-16/` |
| H-T4 EMA 无收益 | \|Δ\|<5% 且 CI 不含实际差异 | ema=-3.300 vs sync=0.204，\|Δ\|=**1715%** | **refuted**（EMA 收益巨大） | 同上 |
| H-D1 失败轨迹价值 | 配对 p<0.05 且 h>0.3（失败注入更优） | 全成功 0.05 vs 失败注入 0.05，p=**1.0**、h=**0.0** | **refuted**（合成链路级，地板效应） | `experiments/hd1_2026-08-17/` |

**共同局限（必须重申）**：①合成/演示数据（纯随机张量 or 结构化合成帧）；②ImageReach/2D 环境；③演示级训练规模（20 epochs）与粗放评分函数。三者都倾向「**弱条件下未检出效应**」，**不能**解释为对假设的一般性否定。

## 2. 三个负结果的诚实含义（对项目的技术判断修正）

1. **H-T4 refuted 修正了「冻结配置可去 EMA」的简化叙事**：冻结骨干 + 训练头配置下，同步目标（硬拷贝）训练崩溃——**EMA 保留**是必要配置；G2 的「去 EMA」选项被剔除。这与 LeWorldModel「无 EMA 端到端」形成对照臂（其从头训 vs 本配置冻结骨干的差异值得注意，但需真实数据）。
2. **H-T2 refuted 的「方向反转」是重要工程信号**：投影头在 32–64 处非瓶颈、加宽反而变差（合成数据上）——默认 `latent_dim 128→64` 为候选（G2 登记），待真实数据复验定案。
3. **H-D1 refuted 暴露的是「评测强度不足」而非「数据价值不存在」**：两模型成功率均 5%（地板），信号被淹没——真实失败轨迹复验（A26 类）与更强的闭环评测是下一步。

**方法论收获（负结果也是证据）**：三条 refuted 各自修正了一条技术判断；预注册纪律证明「正负同录 + 局限显式化」可行——这正是 M4 判定报告需要的姿态。

## 3. 工程链路验证（非假设检验，管道已通）

| 环节 | 交付 | 冒烟/验证 |
|---|---|---|
| P2 动作条件后训练（真实权重 391/395 键冻结骨干） | `train_ac_vjepa_ddp.py --init-from vjepa2hf:` | 600 结构化窗口 20 epochs，loss_total −0.03→−1.27 收敛，`last.pt` |
| M3 评测骨架（v1.2 协议实现） | `m3_mpc_eval.py` | Mock 链路 A 0.000<B 0.033<C 1.000；配对检验/ECE/结果表全通 |
| 真实权重 chain-live 演示 | `scripts/m3_live_demo.py` | ImageReach：A 0.0/B 0.20/C 0.067；**C 校准最优 ECE 0.027 且时间轴 ECE 上升**（呼应 H-T3 §7.3 设计） |
| vla-eval 对接骨架 | `scripts/m3_vla_eval_dock.py` | predict 契约 + 客户端四方法契约冒烟通过 |
| 数据生成（结构化合成 + 失败注入） | `scripts/make_structured_p2_data.py` | 600 窗口/数据集，`--failure-ratio` 支持 H-D1 |

**管道结论**：真实 V-JEPA 权重 → 真实 P2 训练 → 真实评测骨架 → vla-eval 对接面，**全链路已打通**；M3 真实执行只剩「真实 B/C 层数据 + RoboCasa 环境」两个外部前置（A26/A27）。

## 4. 关键数据/配置（复现要点）

- 数据：`demo_ddp_data_800`（随机张量，H-T2/H-T4）；`p2_structured_data` / `hd1_data_{success,fail30}`（结构化合成，P2/H-D1）。
- 模型：官方 V-JEPA 2.1 ViT-B/16 80M（`weights/vjepa2.1-vitb-fpc64-384`，SHA `77D2D116...C19D6035`，391/395 键冻结）。
- 训练：P2 20 epochs、batch 4×4、latent 128、seed 默认；消融 H-T2/H-T4 用 30 epochs。
- 统计：配对 Wilcoxon+Holm、Cohen's h / Cliff's delta（bootstrap 95%CI）、ECE 15-bin + 前/中/后时间轴。
- seed/口径冻结：全部见 `决策记录.md`（2026-08-15/16/17 各登记条目）。

## 5. 对 M4 判定报告的前置清单

- [x] 三条机制假设的预注册 + 负结果（合成级，明确局限）；
- [x] 评测管线（骨架 + 统计层 + vla-eval 对接面）就绪；
- [ ] **真实数据**（B 层视频 / C 层 RoboCasa / D 层真实轨迹）——外部依赖（A26/A27）；
- [ ] M2 真实训练 → C 组模型 → M3 真实判定（H1/H0 裁决）→ M4 判定报告。

> 当前项目处于「**管道全通、等真实数据**」的清晰状态：所有不依赖外部资源的动作已完成。

## 6. 运维教训（附，供后续云端执行）

- DataLoader worker fork 会以同名 cmdline 出现——判重复进程须看 **ppid**，勿凭 cmdline 相同就杀；
- 远端操作一律走「本地写脚本 → scp → bash」模式（pwsh→ssh 内联转义易碎）；
- 清理/删除产物前先确认无活动 fd 写入；误删后须对最终产物做完整性校验（loss 日志 + last.pt 可加载）。

## 附：本报告索引

- `HYPOTHESES.md`（11 条假设，3 条 tested/refuted）
- `M3_MPC_EVALUATION_DESIGN.md`（v1.2 预注册协议）
- `决策记录.md`（全部预注册/结果/运维登记）
- `experiments/`（ht2_ht4 / p2_chain_live / hd1 三套原始证据）
